# Feature Selection Handoff (2026-08-20)

This note closes the current feature-selection cycle and defines the
pre-tuning validation sequence.

## Confirmed outcomes

- Frontier broad search (`frontier_aug20`) completed.
- Frontier winner (`production_frontier42_aug20`) was registered and evaluated.
- Final apples-to-apples comparisons kept `production_sparse72` as winner under
  current open-skill-first governance protocol.
- Deterministic decision artifacts are generated by:
  - `production/ops/edge_floor_sweep_governance.py`
  - `artifacts/odds_log/champion_challenger_decision.json`
  - `artifacts/odds_log/model_freeze_card.md`

## Documentation that is up to date

- `docs/reference/governance_metric_stack.md`
- `docs/reference/champion_challenger_protocol.md`

## Documentation gaps still open

- `docs/research/final58_consensus_freeze_2026-08-20.md` still reflects an older
  winner framing (`production_oof72_monotone`) and should be aligned with the
  current `production_sparse72` champion framing.
- No dedicated "recent experiment index" page maps all newly added scripts and
  artifacts from this cycle (frontier, market-skill reruns, edge-floor sweep).
- Paper/manuscript files are not yet updated with this cycle's finalized winner
  and caveats.

## Validation state by dataset window

- Selection/tuning research windows: 2023-2024 walk-forward CV.
- 2025 data has not been used as a full, isolated final model holdout in this
  cycle's new open-skill-first decision lane.
- Settled replay slice currently has limited date depth, so recency split checks
  are constrained.

## Required next validation before major tuning campaign

1. Run strict 2025 holdout evaluation (no feature reselection):
   - compare `production_sparse72`, `production_sparse72_monotone`,
     `production_final58_consensus`
   - report open-skill and policy/risk stack with identical decision logic.
2. Integrate 2025-2026 "12h-to-close" market dataset as an external baseline
   lane:
   - use it for market-quality context and robustness checks
   - keep baseline definition explicit (12h pre-pitch, not true close).
3. Publish a single experiment matrix document linking each metric table to:
   - data window
   - market baseline definition
   - policy assumptions.

## Tuning readiness decision

- Feature-selection stage is sufficiently converged to start tuning.
- Recommended path: tune only on the locked finalist set(s), with 2025 holdout
  used as final acceptance gate to control overfitting risk.

