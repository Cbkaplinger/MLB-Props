# MLB Props — Execution Backlog & Directive
> **This is the ONE holy file.** Single source of truth for what is approved, blocked, waiting on the user, and parked. Do NOT create parallel backlog files. When the user drags in a markdown file, reconcile it INTO this file, then it / the old file goes away. Keep it evergreen (see Evergreen Rule below).
>
> **Always open this file first** (`docs/EXECUTION_BACKLOG.md`). After every user prompt / agent turn that changes work state, update the **Session Snapshot** before ending.
> Also linked from root `AGENTS.md` and `.cursor/rules/execution-backlog.mdc` so every agent session sees this as master.

### Document hierarchy (this file wins)
| Kind | Path | Role vs this backlog |
| --- | --- | --- |
| **Master work state** | `docs/EXECUTION_BACKLOG.md` (this file) | Approvals, Snapshot PAST/PRESENT/FORWARD/DEFERRED, standing ops rules |
| Agent entry pointer | `AGENTS.md`, `.cursor/rules/execution-backlog.mdc` | Point here only — no second queue |
| Research constraints | `docs/reference/research_assistant_instructions.md` | How to build/leakage-safe — **not** a todo list |
| Ops runbooks | `production/README.md`, `INDEX.md`, `RUNBOOK.md` | How to run commands |
| Dated evidence | `docs/reference/reports/*.md` | Point-in-time reports; must say if SUPERSEDED and point here for “what next” |
| OpenCode paste brief | `docs/reference/opencode_handoff.md` | Coworker briefing 2026-09-11 (replay era). **Not** a second queue. Pack #113 built; juiced #123 measured; totals #124 pulled. |
| Frozen-model replay spec | `docs/reference/oddsapi_replay_architecture.md` | How to backtest 2025-present Odds API open/close with the model frozen — **not** a queue. Freeze/work/product partition. Inventory: `reports/oddsapi_replay_inventory_2026-09-11.md`. |
| Golden metrics | `docs/reference/golden_metrics.md` | Canonical numbers + JSON sources. Cite, do not recompute. |
| Paper / resume | `docs/paper/manuscript.md`, `resume-summary.md` | Publication narrative — not the live work queue |
| Cleanup history | `docs/CLEANUP_LOG.md` | Historical cleanup log — not instructions |
| Canonical surfaces | `docs/reference/repo_canonical_map.md` | What code/docs are canonical vs archive |

If another doc’s “next steps” disagree with the Session Snapshot, **this file wins**. Fix the subordinate doc to redirect here.

## Directive (read this first, every session)
You have blanket approval for every item below marked APPROVED. Execute them **in order**; do not re-ask on anything already approved. Do not start new discovery/analysis/philosophizing until every APPROVED item is DONE or marked BLOCKED with a one-line reason. Notice something interesting mid-task? Put it in "Parking Lot," don't chase it. Report only on item completion or a true block — not every intermediate find.

### Iterative session loop (required)
1. **Read** this file (Snapshot → Waiting on user → Forward → **Deferred** → Standing Rules).
2. **Do** the next APPROVED item(s) only — **never** pull work from **DEFERRED** unless the user asks or the reopen condition is clearly met.
3. **Write back** before the turn ends — Snapshot must always have four labeled blocks:
   - **PAST** — durable completed work (what already shipped).
   - **PRESENT** — where we are now / what just finished this turn.
   - **FORWARD** — concrete ordered plan for the next session(s).
   - **DEFERRED** — approved-or-parked work that is *not* next (with why + reopen trigger).
   - Also refresh Waiting on user; add ideas to Parking Lot; never silent-drop threads.
4. Tell the user briefly what changed and point them back here for the plan.

**Evergreen Rule (every session):** after reporting, reconcile THIS file against what actually happened — update PAST/PRESENT/FORWARD/**DEFERRED**, item statuses, Parking Lot, and **pre-write the next session's plan** so no thread is lost. Close every session with this file reflecting "here is where we were, where we are, where we're heading next, and what we are deliberately not doing yet."

---

## Session Snapshot — 2026-09-11 (juiced 1u #123 green; totals #124 pulled; live BET set frozen)

### PAST — already shipped (durable)
| Track | Status | Notes |
| --- | --- | --- |
| SSAC27 #1–#8 audit + pre-commit gate | DONE | Modeling half honest; betting half demoted / non-claim |
| Phase A interim ops stance (docs) | DONE | Shadow-first framing |
| Phase B shadow asym / line-veto lanes | DONE | 4.5-over veto best narrow candidate |
| Deep post-freeze diagnosis | DONE | Edge≠ROI on overs; Brier skill over− / under+ |
| Item 14 granular open calib challenger | DONE (research) | Holdout preferred global_iso; still < market — **not** live-promoted |
| Pack A weekly settle pack + game_date bootstrap | DONE | Parallel ledgers for continuous A/B |
| Book-quality filter | WONT_DO | Books for lines only; usually synced |
| Live **over@4.5 hard veto** + soft probation 2.5/3.5 | **DONE 2026-09-01** | `kpi_policy` + `odds_board`; commits `7e03ced` / `0f8df76` / backlog follow-ups |
| 2026-09-04 zero-bet incident recovery | DONE | Stale-slate root cause; 4 BET logged/sent manually |
| **2026-09-06..08 morning-outage fix (Agent)** | **DONE 2026-09-08** | 5 automation bugs fixed + verified (see PRESENT). Full suite 280 green. |
| **2026-09-08 beat-the-books program (Agent+user)** | **DONE (plan)** | `docs/reference/beat_the_books_program.md` — diagnosis locked (ECE 0.159 vs 0.015; xK +0.7–0.9 on BET tickets = selection amplifier; capped = breakouts+declining vets; TBF ±5.1 symmetric shrinkage) + 7 ordered WS with kill criteria; xROI −0.07, no positive-EV promise |

### PRESENT — where we are / just completed
- **REPO QUALITY PASSTHROUGH 2026-09-17:** Low-risk hygiene only. Wiped local `__pycache__` / `.pytest_cache`. Retargeted remaining `results_gate_policy.ipynb` pointers to `results_bettable_cohort.ipynb` (canonical map, KPI protocol, diagrams, waste-sweep, README). New hold ledger + dated report under `docs/reference/reports/`. Did **not** delete production code, notebooks, data, or artifacts. Did **not** commit or revert the existing dirty tree (286 modified / 1 deleted / 28 untracked — pre-existing). Smoke: `py_compile` daily+grader + `grade_odds_ledger.py --status`.
- **REPO GC 2026-09-17 (owner: full execution as written):** skill `.agents/skills/repo-garbage-collector/` built (SKILL + 3 refs + 4 Polars scripts); 5 parallel specialists returned evidence (docs/Python/entry-points/notebooks/populations). Batch 1 docs: champion rebuilt-panel truth fixed in root README / manuscript Table 3 judge row / diagrams 00+04 (2025 n=505 +11.0% LCB +3.0% → 2026 n=460 +8.8% LCB +0.2%, White p=0.0075; 9/11 n=558/n=485 superseded), golden_metrics resim pointer + model-card WS1c-live pointer added; 42 fast tests green (incl. live-stack pin). Retain verdicts with evidence (no deletions): dev-notes/PAPER_NOTES/brief (live manuscript citations), all 8 production + src/analysis notebooks (`run_analysis_notebooks.ps1` + L3-audit wiring), Python inline dupes (frozen + tested; canonical homes filed). Secrets audit clean (env-var refs only, no topic URLs tracked, `.env.example` placeholders). New `tests/test_gc_population_truth.py` 5 green (460/505 disk-truth, CLV fraction-scale, void exclusion, `bet_pnl`). Temp audit CSVs in system temp (removed at closeout). No commits/pushes; all changes uncommitted for review.
- **PHASE 10 EXECUTED 2026-09-17 (owner: full execution):** 8 parallel read-only census workstreams → `docs/reference/pipeline_registry.json` (`mlb-props-pipelines-v1`: 9 pipelines P1–P9 + OPS control plane, 10 schedules, 6 datasets, 12 gaps, 42 discovery answers, prop contract, sim/eval specs) + pipeline catalog + Mermaid DAG in canonical map + sequenced ARCH/DATA/PROP/SIM/EVAL/OBS/NB/OPS/TEST IDs in FORWARD. Verdicts: collection = 1 pipeline/2 arms; transforms = 1 pipeline/2 heads; serving-to-drift = file-handoff stages, no state machine; replay = live-code family + 2 fork groups to unify; all 5 Modal crons already `modal.Cron` (Period risk n/a). Top gaps: Modal UTC-vs-EDT DST, no freshness gate on board, 5 unguarded invariants (probation→lean order, double-settle, close denominator, training PIT, live poll books). Design-only: no live/Modal/policy/model/schedule changes. Temp census outputs removed; changes uncommitted.
- **OUTS-NEXT + SEPTEMBER SAFETY PACKAGE (owner directives 2026-09-17):** outs confirmed as first adapter (batter after). Narrow September safety package authorized in this order: TEST-1 five invariants → OPS-1A tz-aware Modal Cron → DATA-1A hard freshness/schema gates → OBS-1 run manifests + no-bet codes → OPS-1B notification idempotency; then EVAL-1 → SIM-1 → NB-1 → ARCH-1 → PROP-1 outs shadow → DATA-1B → SIM-2. Standing constraints: no model/policy/calibration/threshold changes before October; alerts critical-only for untrustworthy state (warnings in digest); pre-fill sim reports multiple scenarios; external registry deferred; notebook deletion waits on eval engine; plugin framework waits on outs. FORWARD Phase 10 block reordered to match. Nothing executed yet — awaiting scope word to start TEST-1.
- **TEST-1 + OBS-1 EXECUTED 2026-09-17 (owner: start both):** new `src/Python/run_manifest.py` (statuses, 9 no-bet codes, new/finish/key/hash/path/write/emit + alert/drift builders; atomic writer reused, no bespoke path) + `tests/test_safety_invariants.py` (6: probation→lean order proof + end-to-end pin, double-settle, close denominator via live `summarize`, pregame-nine L3 join, book-universe filter) + `tests/test_run_manifest.py` (5: schema, validation, key determinism, round-trip + safe-emit, mappings). Guarded additive wiring only: drift emits `run_manifest_p6_settle_latest.json`, alert emits `run_manifest_p4_serve_latest.json` (try/except, no variable touched, exit codes + fail-loud intact). 11 new green; 108 green across affected suites (board/drift/alert/ledger/pin/grading/champion/truth); independent verifier PASS on tests/wiring with 2 honest caveats (retry/page hunks in the same file diffs are concurrent-session work, not this turn; full-tree attribution needs a commit). No selection/policy/calibration/schedule/model changes. Uncommitted.
- **OPS-1A EXECUTED 2026-09-17 (owner-approved DST correction):** all 5 Modal Crons converted to explicit `timezone="America/New_York"` with local-wall-clock rewrites (morning `0 8`, hourly `0 9-22`, sweep `*/20 12-22`, settle `0 3`, drift `30 5`; ET-window gate behavior unchanged). Before-state table reconciled against code comments + README timing + laptop tasks (README 08:30 staleness fixed to 08:00 in diagram + timing table with new Timezone column). SDK gate PASSED (modal 1.5.5 ≥ 1.0.3, `timezone` param present). New `tests/test_modal_schedules.py` 9 green (AST-exactness, no-implicit-UTC, registry match, summer-local no-change, 8-day DST matrix from tz database, once-daily, no 01:00–03:00 jobs, chain ordering, SDK gate). Registry schedules + G-OPS-1 (resolved) + status updated. Windows audit filed as OPS-1C (no edits). Caught+fixed own bugs: registry double-brace (re-validated), AST nested-decorator parse, aware-datetime subtraction in DST assert. No deploy, no alerts, no model/policy/calibration/data/selection/settlement changes. Uncommitted; deploy is a later separately-approved action.
- **OPS-1A DEPLOYED 2026-09-17 22:14 ET (owner-approved, closure-guarded):** predeploy audit first — entry `modal_app.py`, App `mlb-props` (`ap-ZdD4DU7Q6QPauvcmzX18kd`, single, no dup), closure = full `src/` + `production/` trees; `-w` content diff vs HEAD narrowed to 14 files, every hunk mapped to an explicit owner order (closeout utc_gd, lineup-status, veto-leak/page-on-red, catch-up paging, retries, stale repeal, atomic writes, game-stack flag) with redeploy records; policy JSONs content-identical (CRLF only), kpi clean; live was image d == file d (heartbeat 114 lines green). Zero unrelated/ambiguous reachable changes → PASS. `modal deploy` → v29 (first attempt's ✓-crash had actually deployed; caught via history) + v30 with `IMAGE_VERSION` bumped d→e per repo convention (modal_app.py sha `0599b948c4ae2a99` at deploy). Exactly 5 functions, mounts src+production as designed. Immediate verify: 22:20 ET close_sweep ran on image **e, ok=true** (115 lines). No manual invocation, no test alert, no secret/volume/pin changes, no ledger mutation by deploy. Rollback target: v28 via `modal app rollback mlb-props`. Status: **deployed, awaiting scheduled smoke verification** (tonight hourly ordering + tomorrow 08:00 board; true DST proof Nov 1). DATA-1A stays queued behind smoke per owner order.
- **48H-PLAN TRIAGE 2026-09-17 (stopped per plan's stop rule):** Item 1 partial smoke PASS tonight — single app (no dups), 22:20 + 22:40 sweeps on image e ok, same-behavior, heartbeat 116 lines, no unexpected ledger writes by this session; correction: no 23:00 ET hourly exists (hourly window is 09:00–22:00 ET; next hourly 09:00, next manifest emission with it). PENDING: 08:00 board (once, image e, clean output) + 09:00 hourly + manifests → then rollback-or-close call. Items 2+ NOT started. Plan-vs-repo conflicts filed for owner decisions: (a) commits need explicit order (standing rule; plus "don't commit before 1+3" paradox vs item order); (b) DATA-1A spec names nonexistent modules/paths (`mlb_selection`, `board.json`, `.board_fresh`, run_ids) — needs repo-adapted design on recommendations.parquet + manifest + poll/alert gates; (c) pre-commit + ci.yml already exist (extend, don't create; branch protection is owner clicks); `uv init` restructure out of scope; (d) `src/mlb_core`, `src/mlb_grading`, `tests/properties` don't exist — map mutation/property tests onto `src/Python/market.py` + `odds_ledger.py`; hypothesis not installed; cosmic-ray is hours-long — scope later.
- **SIX ANSWERS + WINDOW/QUOTA 2026-09-17 (owner, recorded):** (1) SLOs yes, record-only, folded into drift/grading chain (Modal 5-cron cap — no new schedules), 2-week baseline before any target; (2) DORA shelved till commits flow for weeks; (3) CI-deploys offseason atomic move, laptop deploys + closure audits till then; (4) mutation capped — one overnight run per module on `market.py`/`odds_ledger.py`, then targeted tests + close; (5) commit train as-is from dirty tree, pre-existing unattributed changes get own inspection pass first; (6) metric drift first (EVAL-1 after DATA-1A), docs-from-artifacts second; notebook/schedule drift already test-pinned. Window CONFIRMED: 08:00–22:00 America/New_York already live (morning 08:00 + hourly 09–22 + sweeps q20min 12–22:07 + watcher); hourly re-logs projections on fresh lineups (dynamic-lineup doctrine); OPS-1A holds the window fixed across Nov 1 EST. Quiet-hours CONFIRMED already-design: hourly flips/failure-only (silent when green), morning always-fires (kept deliberately — a silent zero-BET morning is indistinguishable from a broken pipeline). Quota: SharpAPI Free 12 req/min self-throttled 1/6s (`SHARPAPI_MIN_INTERVAL_S`), 429s back off loudly; ~17k/day capacity vs current ~40–120/day (~1%); per-cycle cost is 1 paginated fetch set via shared-fetch. Max sensible: sweeps q10m in windows (~84/day, still ~1% of quota); CPU chain cost (~2 min/run), not quota, is the binding constraint — no q5m board.
- **PREP + EARLY TESTS 2026-09-17 (owner: drafts + additive tests, zero prod changes):** hypothesis 6.168 installed (dev-only, pinned in `dev` extras); `tests/test_properties.py` 12 green in ~10s (11 property + 1 deterministic Kelly pin; devig sum/fairness, implied validity, odds roundtrip, CLV bounds, zero-stake PnL, Kelly breakeven/sign, settle-all-tickets idempotency, dedupe idempotency+keying, ROI order invariance — each labeled with what breaks in money terms). Verifier PASS on tests/canonical-calls/pyproject-one-liner with two notes adjudicated: (a) modal_app.py evening mtime is the earlier OPS-1A turn tonight, not this turn — this turn touched only test_properties.py + pyproject + backlog; (b) alleged `recommendations_meta.json` side effect does not exist anywhere in tree (glob-clean); `.hypothesis/` cache is self-ignored upstream.
- **MORNING SMOKE GATES 2026-09-18 (pending — go/no-go, nothing else matters):** (1) 08:00 board on image e, current slate, freshness real not tag-only, ordinary selection mix; (2) 09:00 hourly emits the first run manifest with real content (run IDs, row counts, cutoffs — no placeholders); (3) exactly one ntfy page from exactly one owner (double page reopens OPS-1B in place). All three clean → close OPS-1A + greenlight DATA-1A immediately. Any red → stop, diagnose, rollback decision; no DATA-1A on amber.
- **STANDING CONVENTIONS (owner 2026-09-17):** future property suites pair every sweep with one deterministic pin (catches vacuous properties); consider committing `.hypothesis/examples/` once the train lands so CI replays historical counterexamples (zero cost today — none found); T-H unattributed changes earn commit slots only after separate inspection, never smuggled into T-A..T-G.
- **SCORECARD 2026-09-17 (owner assessment, recorded):** 7/8 enterprise rows at/near bar; version control is the load-bearing gap — everything earned this week is uncommitted. Framing locked: adopt FAANG *properties* (reproducibility, attribution, reversibility, measured reliability) at solo scale, not the org-chart theater (DORA shelved, uv-restructure declined, CI-deploys deferred = evidence). Completion = smoke → DATA-1A → commit train. Sharpest fragility note: a `git clean -fdx` on the laptop would take ignored data/artifacts with it — the Modal volume copy is currently the only backstop; commit train + retention policy close it. Backlog now holds the commit-train manifest (T-A..T-G + deferred T-H) and the DATA-1A adapted design (`serving_gates.assert_serving_fresh` on real files, two choke points, raise-not-warn). No production, schedule, policy, or model file touched this turn.
- **ENTERPRISE PLAN TRIAGED 2026-09-17 (no execution):** `enterprise-maturity-plan.md` (outside repo) reconciled here per holy-file rule. Aligns: commit train (matches decision #1), property tests on `market.py`/`odds_ledger.py` (matches #5), extend pre-commit/CI (matches #3), keep Python (Polars already Rust-backed; no rewrite). Conflicts with standing decisions: uv-workspace/monorepo restructure (owner DECLINED this month — stays shelved), `mlb_core`/`mlb_grading` paths (don't exist). New proposals needing owner calls: SLOs + error budgets (would add a cron + digest content), DORA metrics job, lockfile strictness, deploy-from-CI vs laptop, mutation compute budget. Verified: no requirements*.txt/Pipfile/poetry.lock exist — single unpinned `pyproject.toml`, so env sprawl is just "no lockfile", not multi-env chaos. (Attribution note: this turn's hunks in modal_app.py are schedule/tz/comment-only — verified via diff-hunk inspection; the same file diff vs HEAD also carries 9/16 cutover work — IMAGE_VERSION, _run_steps, heartbeat ok/note, NO_ALERT removal, --page-on-red — from concurrent sessions, untouched here.)
- **DAILY GRADING FORMAT 2026-09-16:** `send_daily_grading.py` now separates each Yday/7d/30d/YTD row with a blank line in the ntfy body; every row includes realized dollar PnL. Targeted tests: 5 passed; linter clean.
- **OPENCODE OPS 2026-09-11 ($0, no live change):** Per directive (no pre-approved coding job) ran allowed hygiene only. Settle audit: 53 open ledger rows, all game_date 2026-09-11 (today) — nothing settles until post-game; `--auto-settle-api` correctly withheld. Weekly pack re-ran: status_quo n=125 ROI +7.2% vs veto_4_5_over n=95 ROI +14.3% (veto still ahead). Stacker gate re-ran: n=981, base 0.2472 → stacker 0.2435, gain +0.00362 SURVIVE (shadow only, stage 2/3 still need sign-off). Full test suite 324 passed. Closeout: cache thru 2026-09-09, gap = 2026-09-10 only — owner authorized pull 2026-09-11: ran live, 5 events x K+outs x close+morning, spent **0** (remaining 3,714,815), consensus still 70,337 thru 2026-09-09 (9/10 added no new consensus props — thin slate). Juiced sizing verdict re-verified from `juiced_replay_report.json`: flat +7.3% > 1/16-Kelly +6.6% in both years and DK+FD; robust +7.8% is a different taken set (n=1,811). Keep 1/16 live, flat as benchmark. Zero Odds API credits spent.
- **PLANNING 2026-09-11 (owner Qs, plan-first, no code):** Owner asked (a) what Kelly-zero means, (b) whether live policy is stale vs the OddsAPI lake (yes — 9/01 thin-n inputs; reset scoped as October nested juiced v2, not a live edit), (c) elbow verification (morning-only fair-price shape + juiced band-arm cross-check; display-first cap only), (d) BetOnline sharpness (no — n=5, offshore; sharp anchor is Kalshi, book benchmark is DK/FD devigged close), (e) consensus-morning-line grouping per player O/U (adopt as October v2 dimension; consensus for measurement, juiced book-pair for PnL; dedupe is money-track aggregation, not selection), (f) totals purpose (slate common-shock for correlation caps, not a K feature), (g) dawn probe blocked by RotoGrinders lineups stale till ~8:30 ET (lineup-freshness must gate any dawn edge; carriage/limits/quota/fills unverified). No credits spent on planning. Next: owner picks from the plan in chat.
- **OWNER DECISIONS 2026-09-11 (binding, executed same turn):** (1) Policy reset = **2025-lock now** — filter family locked on 2025 juiced only; 2026 is the disclosed-peek judge (blindness already broken, labeled everywhere). (2) Morning edge cap + robust refusal = **ENFORCE LIVE now** — named change: `kpi_policy` `edge_cap=0.20` (`edge_cap` HOLD) + `robust_shrink_refusal` (`robust_refusal` HOLD when 50%-shrunk edge < floor); sizing of survivors unchanged (1/16-Kelly on live p); full suite 331 green; pin updated same commit. Accepted cost: ~13% volume drop, remainder-of-season is the live judge. (3) Totals join = **GO now** — `slate_shock_join.py` → slate common-shock research, exposure caps only, never a K feature. (4) Shadow arms = **HOLD all** pending plain-English brief (delivered in chat; re-ask any time).
- **BOARD RERUN 2026-09-11 (new policy live-fired):** Backed up morning recs/quotes (`*_pre_cap_20260911.parquet`), re-ran exact morning args. Morning: 1 BET (Snell U7.5) + 1 HOLD + 24 skip → rerun: **0 BET + 3 HOLD + 24 skip** (27 rows, 2 newly quoted). Snell BET→HOLD `edge_cap;robust_refusal` (edge 0.215→0.22) — the lone morning BET was exactly the extreme-edge class the cap was built for. Lugo O4.5 HOLD unchanged (veto). Bradley O5.5 skip→HOLD via deploy matrix (intraday move, not the new rules). Offset clip bound on **19 rows** (|offset_value|=0.02). No skip→BET flips. Ledger untouched (no poll rerun — today's 53 opens stand).
- **NTFY RISK ACCEPTED 2026-09-11 (owner: not a concern).** Public-topic alerting stands as-is; no auth/self-host work. Live-fire verified same day (Snell U7.5 board, ntfy 200).
- **OCTOBER PROGRAM (parked till post-9/27, in order):**
  1. Final closeout sweep (`--end 2026-09-28`) + postseason HOLD takes effect (paper continues, BET stops — guard already live).
  2. Slate correlation caps from the joined totals (per-game / per-side / common-shock; replace heuristic 12u/20u).
  3. Full 2026 judge of the promoted champion on the complete season (max n, September included) + White/PBO over the 36-config family.
  4. Optional K-only −30h open pull (~47k) to cover post-7/10 if the judge needs it.
  5. Stacker ROI vehicle (shadow ROI lane on stacker probs — answers whether blending pays).
  6. Day-game policy gate (only if dawn probe shows carriage + fresh lineups).
  7. Re-gate legacy support rules (long-rest / matchup-tier) + deploy matrix on universe scale.
  8. Revisit 2.5-ban, 3.5-under (n=42, −8.2%), 9.5-cell with season-full n.
  Triggers that pull work forward: fills ≥50 (money-truth review), a second extreme-edge live loss (cap re-think), SharpAPI quoting a third book (universe guard fires).
- **CHOICE-DOCTRINE 2026-09-11 (owner-confirmed):** We do not beat books on probability (skill negative all 8 lines). The edge is choice: clock (−12h skill +0.044, gone by morning), tickets (veto/lean/cap/refusal), book (DK+FD-only champion; BetRivers soaked 48% of canonical volume at WR 0.429 vs DK+FD 0.58), fills (still 0 — the only thing that converts paper to bankroll). Selection engine + 2025-lock exist to keep choice honest.
- **SCHEDULER + MESSAGE SECURITY 2026-09-11 (reviewed, no secrets found):** 8 tasks Ready; NightlyDrift still unregistered (owner). `.env` untracked; no topic/keys committed; alert path is ntfy-only via `NTFY_TOPIC/URL` (user env + ignored `.env`). Findings: (1) ntfy.sh public topic = picks travel HTTPS to a guessable URL, readable/writable by anyone with the topic — accepted risk (time-decaying, low-sensitivity) unless owner wants authenticated/self-hosted ntfy; (2) tasks run Interactive-only (the 9/4 failure mode) — `setup_automation_tasks.ps1` supports password logon but `/RP` exposes it on the command line, so prefer interactive + keep-awake over stored passwords; (3) `Bypass` execution policy is local-scripts-only, no remote code. Nothing to fix in code.
- **MORNING 9/11 INCIDENT (degraded-by-design, machinery worked):** statcast refresh failed (empty error text — logging gap filed below) → best-effort board posted Valdez U4.5 + failure banner fired + self-check paged RISK correctly. Same log shows a live stale-lineup event: RotoGrinders PIT starter (Mlodzinski) vs MLB probable (696062/Ullola, quoteless) — exactly the dawn-probe failure mode the owner flagged. Follow-ups: (a) statcast step must log its exception text into the banner (currently blank); (b) NightlyDrift registration still open; (c) box stays Interactive-only by choice (see security note).
- **FRAMBER 2026-09-11 (paper ticket lifecycle, no action):** Morning BET U4.5 (DK, edge 16.7%, $43.19) → intraday price moved (+126→+112/+106) → current board skip (edge 13.1% < 0.14 floor). Ledger holds 2 open book-rows (FD/DK, unsettled — game pending). Doctrine holds: bet early numbers, paper stands, intraday decay never un-takes a logged ticket. Watch at settle: if it wins, the morning edge is validated; if not, it grades normally.
- **PIPELINE REVIEW 2026-09-11 (no deletes approved):** Tracked tree is clean (biggest files are live code: dashboard parked 91KB, rolling/features 37–41KB). Cleanup candidates PROPOSED only: one-off Aug sweep scripts (`compare_feature_set_governance`, `step9_metric_window_select`, `full_feature_importance_screen` — history, keep till offseason), metric-block dupes (4× Sharpe, 6× Brier — canonicalization queued, unapproved), orphan `regime_model_report.json` (no generator — do not cite). Ignored bulk (data/artifacts) is by design. Nothing gets deleted without a named order.
- **TRAIN/TEST + QUANT VERIFIED 2026-09-11 (from disk):** L3 18,263 rows 2023-03-30→2026-09-07 (train 2023–24, holdout 2025, clean 2026 — split discipline intact). MAE_K 1.780 (target ≤1.75 unmet — known, timing+selection game instead). Universe pooled n=19,533: live Brier 0.2204 vs book 0.2162 (skill −0.0043), ECE 0.021 vs 0.009, ship-lift +0.0016. Quant: paper full +2.7% → veto +6.5% (36d); juiced canonical +7.3% / DK+FD +12.3% / cap-only 2025 +8.0%; champion +15.5%→+15.6% (White p<0.0005).
- **BACKEND FREEZE VERDICT 2026-09-11:** Model: FROZEN (retrain closed #66; reopen only new-data/offseason). Metrics: COMPLETE (golden file cites all; Sharpe-decay + LogLoss remain specced TODOs — non-blocking). Policy: PROMOTED champion, pinned, 345+ green. Remaining work is all forward: October program, fills habit, NightlyDrift, push for today's uncommitted batch. Nothing in the backend needs building before the freeze.
- **CI FIX 2026-09-11 (emails stop):** CI failed every push because `artifacts/`+`data/` are gitignored and 4 tests read them (pin ws1c file + 3 selector tests) — FileNotFound on Ubuntu runners, never a logic failure. Fix: `tests/conftest.py` `artifact_path()` skips with reason when the lake is absent; full-suite-local stays the source of truth. Pushed (`da3f8bd`), CI **success** (41s). Are checks necessary? Yes, kept: they compile every production script and run the shared-logic suite on every push — the signal was real config debt (tests assuming local files), not noise. To silence failure mail without fixing: GitHub Settings → Notifications → Actions (owner clicks, not recommended while CI is Load-bearing).
- **PAPER RESTRUCTURE 2026-09-11 (publication standard, uncommitted):** Full rewrite to one spine (model → frozen eval → 2025-lock → disclosed peek → limits): new §1 (contributions + terminology) / §2 Related / §3 Data+leakage / §4 Methods + notation box / §5 Model Eval / §6 Decision-Policy (Table 3 champion table, Figs 3–7) / §7 Deployment / §8 Diagnostics (Table 4 per-line skill, KING+nulls condensed) / §9–10. n=26 numbers now ONLY in A.5 (+§9 one-liner, no numbers); §8.3/§8.4 deleted from body; equity fig → Fig A1; new A.7 provenance table; refs normalized + White [13]; notation unified; bold reduced to labels/terms; 28 pages (was 34). Validation: validator ALL PASS, banned strings absent, determinism confirmed, abstract 152 words. One caveat: page raster inspection is programmatic (ink/images/captions) — human eyeball on figure pages still recommended.
- **OPEN-METEO CORRECTED 2026-09-11:** Historical weather IS free (ERA5 back to 1940 + IFS 9km from 2017, no key, non-commercial license): hourly temp/humidity/wind/gusts/precip/pressure/cloud per ballpark coordinates. Pull plan: 30 stadium coords → per-game-day environment rows (wind vector, heat, marine-layer proxy) → join as slate environment (same table as totals), feature never. License fits personal research; cite Open-Meteo + ERA5.
- **EXTERNAL RESEARCH PROGRAM 2026-09-11 (regular-season, $0, free-only):** (A) Confounder audit — re-cut every headline claim within strata (edge-band ROI within book×clock; side effects within line; book ROI within snap), matched-pair same-ticket cross-book/clock tests, negative-control lanes at lake scale, shifted-distribution gates (2025→2026→September) as standard. (B) Free sources to evaluate: Open-Meteo weather API (no key — the one parked environment channel with a free tap), Retrosheet event files (lineup/score context depth), Chadwick register (verify age-table provenance), Brooks Baseball (pitch-shape cross-check), MLB Stats API expansion (lineups/schedule beyond settle). Paid or scraped sources stay out. (C) Literature to mine: Buchdahl/Monte Carlo (wisdom-of-crowds, simulation), Pinnacle Betting Resources (CLV canon, free), Unabited CLV methodology (free articles), Matej et al. 2021 full text (adaptive Kelly mechanics), Glicko-2 spec (rating-with-uncertainty design).   Kill rule per track: no chrono-gated gain → dead in writing like the 19. Nothing enters the trainer or the board without passing §8 SOP gates.
- **EXTERNAL VALIDATION 2026-09-11 (independent sources agree with our architecture):** BettorEdge (2026-09-04): books set K lines from ~10-start rolling averages rounded to .5 (exploitable staleness — our open thesis); over needs workload AND rate while under needs either (structural asymmetry — our veto/lean from first principles); TBF×opp-K multiplicative + Poisson (our stack); umpire/park are nudges, not inputs. Turtle +EV: whiff rate #1, chase matchup, pitch-count leash, K% stability. PredictionEngine: regressor→Poisson-CDF architecture (ours), CLV as gold standard, prop vig 108–112% (matches our translation). Kelly literature (Baker & McHale 2013 shrinkage; Swartz modified Kelly; Downey uncertainty/ruin/percentile analysis): fractional Kelly is robustness, not optimization — our 1/16 stands; backtest-haircut rule (4%→1–2%) and per-bet caps + correlation-cluster caps are adopted into the October slate-cap design (group correlated tickets, cap the cluster as one position).
- **ARXIV MINING 2026-09-11 (Galekwa et al. 2410.21484, 63pp, full-text):** Mostly game-outcome work (KBO-heavy) — different estimand, not comparable. Transferable hits filed: (1) Walsh & Joshi calibration>accuracy already cited [14]; (2) Matej et al. 2021 adaptive fractional Kelly validates 1/16 + robust-λ framing; (3) betting-portfolio/MPT framing → slate-as-portfolio selection (October, uses joined totals); (4) Glicko-style pitcher ratings with uncertainty as the unbuilt staleness/breakout signal (October research); (5) at-bat Monte Carlo with hook sim for tail/TBF uncertainty (post-season build); (6) heterogeneous ensembles = our stacker direction, not more boosters. Paper PDF + text in temp only (not repo — external, link suffices).
- **ARXIV 2410.21484 READ 2026-09-11 (uncommitted):** 63-page systematic review of ML in sports betting (Galekwa et al.). Mostly game-outcome work (KBO-heavy baseball section — not our market). One prize: Walsh & Joshi 2024 calibration-beats-accuracy (+69.86%) with full citation — now reference [14] in the manuscript (§4 rationale) and corrected in the replay inventory (was misattributed "Wagerproof/NBA"). Rest is background, not actionable. PDF + extracted text in temp (not repo).
- **PDF PIPELINE FIX 2026-09-11 (accepted, uncommitted):** Root causes: (1) CSS scoped `max-width` to `figure img` but markdown emits bare `<img>` — every figure overflowed; (2) 8.2–9.0in canvases on a 7.3in page; (3) captions/images as separate blocks paginated apart (5 splits). Fixes: bare-img CSS rule, all figs 7.0in + tight layout + pad 0.1 + 300dpi, short titles, `<figure>` caption+image wrapper (break-inside avoid), thead repeat, orphans/widows 3, code wrap-anywhere, page-number footers, P(K >= L) normalized. New `validate_pdf.py`: 34 Letter pages, 0 blank, 9/9 captions co-located in order, no �, text selectable, rebuild deterministic (identical extraction). Refs end clean (White [13] last). pymupdf installed as local validation tool only (not a project dep).
- **FIGURE REPAIR 2026-09-11 (uncommitted):** Root cause of the broken PDF graphics: `render_pdf.py` scoped `max-width` to `figure img`, but markdown emits bare `<img>` — every figure overflowed the page. Fixed with bare-`img` rule; narrowed all figs to 7.0in; re-laid-out fig1/fig9 coordinate systems for the new width; short fig8 title + smaller labels + explicit x-limits. Also embedded 3 orphaned PNGs (pipeline, ablation, calibration) and renumbered all captions in document order 1–9 (was tangled: reliability called "Fig 2", equity "Fig 3"). Re-rendered PDF (1257 KB) + resume. Verify visually on open; structural checks pass (6→9 embeds, CSS present).
- **GITHUB HYGIENE 2026-09-11 (uncommitted):** Ignore/yaml/toml audit: `.clineignore` ≡ `.cursorignore` (byte-identical) but both stay — different tools read different names, and Cline is parked-not-dead; `.cursorindexingignore`, `.pre-commit-config.yaml`, `pyproject.toml`, `.env.example`, `activate.ps1` all load-bearing. Nothing deleted. Pretty wins that matter: CI badge + Python badge atop root README (CI is green). PDFs rebuilt from the documented `docs/paper/README.md` commands (deterministic — same byte sizes, no drift).
- **PAPER PERFECTION 2026-09-11 (deprecation audit + validation, uncommitted):** Cut retired-lane weight: §8.2→one pointer paragraph, §8.3 slippage→cited paragraph, §8.4→superseded-method pointer, §7.3–7.4 halved. Fixed a real inconsistency: paper track was `n=973` rows against deduped `+$611` PnL — now `n=317` / CLV +0.75pp (n=195) from `paper_quant_report.json`. Added White [13] reference (all 13 refs cited), fig9 policy-governance flowchart (Fig 1 already covers the model pipeline), A.6 column contract stands as the data dictionary. Root README rewritten for resume traffic (champion up top, search lanes quarantined, isotonic retired). Flow: model (§1–7) → retired evidence (§8.2–8.4, labeled) → current measurement (§8.2.3) → live state (§8.5) → selection (§8.8) → limits (§9) — reads top to bottom. HTML/PDF/resume-PDF re-rendered.
- **PAPER REWRITE 2026-09-11 (portfolio framing, uncommitted):** Owner locked portfolio-only / promote-with-disclosure / balanced lens / paper+resume now. `manuscript.md`: abstract carries the replay era + champion (+15.5% LCB, White p<0.0005, 2026 +15.6% peek disclosed); §8.2.3 replaced with current measurement table; new §8.8 documents the 2025-lock design; §8.5 lists the promoted live stack (cap/lean/dkfd/clip; refusal reverted); §9–10 rewritten around choice-not-probability. 4 new figures generated (fig5 juiced ROI, fig6 edge band, fig7 White, fig8 over/under). `resume-summary.md`: live profile, champion, 4 role-ready bullets, 30-second framing. HTML/PDF not yet re-rendered. SECOND PASS (repo docs, then paper perfection): production READMEs, canonical map, stale report headers ("not live" lists), flow diagrams (pipeline + policy + data lineage), Appendix A map refresh.
- **FREE-ONLY 2026-09-11 (standing constraint, owner):** Every future vendor, host, and data choice stays $0. SharpAPI live poll (already paid/held — no new paid tier); 4C screen free cross-check; SportsGameOdds evaluated on free tier only if ever touched; Modal free credit for cloud; Postgres only on a free tier if ever needed. No $99+ spend, no new subscription, no paid tick pull — ever — without a separate explicit order. Quota/credits are an ops footnote, never a paper topic.
- **VENDOR 2026-09-11 (4codds.com evaluated, SharpAPI stays):** 4C Odds is a free JS-rendered screen (Pinnacle/DK/FD/Kalshi/Novig/Polymarket + limits/depth) with **no public API** — scraping it for automation means ToS risk + fragility + no SLA, wrong foundation for a hands-off suite. Verdict: SharpAPI stays the live poll (integrated, tested, working); 4C screen becomes a free manual cross-check (limits, Pinnacle, Novig depth for the fills program); SportsGameOdds ($0–99, per-event pricing, player props) is the cloud-era second-source candidate; OpticOdds on deck if a poll source ever dies. No code, no spend.
- **CLOUD HANDS-OFF PLAN 2026-09-11 (owner wants zero-touch; execute post-9/27):** Daily load is small and known: morning chain ~35 CPU-min + watcher ~8h idle + settle/drift minutes. (1) Host: Modal (cron native, $30 free credit covers it) with the existing `agentic_stack_final_plan.md` as the build doc; GitHub Actions cron stays the fallback. (2) State: DuckDB-over-parquet first (zero migration, originals kept), Postgres (Neon/Supabase free) only if relational pressure appears. (3) Secrets move to host env (SHARPAPI/OddsAPI/NTFY keys — never in repo, already true). (4) Locks travel as config: postseason HOLD, pins, failure banners → ntfy stay the kill-switch/SLA layer (board by 8:35 ET or page). (5) Cloud cron kills the entire Modern Standby/interactive-logon failure class permanently. Triggers: season end 9/27 AND fills habit running AND push current. Interactive-only laptop stays live until the cloud cutover proves itself over ≥7 green days.
- **REPO SECOND PASS 2026-09-11 (complete, uncommitted):** production README (champion policy block + replay research section) + RUNBOOK profile (cap/lean/dkfd/clip) + canonical map (select/slate/prereg/playbook/champion pin) + diagrams 00/01/03/04 refreshed to replay era (1/16-Kelly corrected, Poisson+WS1c, juiced→selection→live nodes) + stale-list sweep (1 correctly-dated 9/01 header left alone) + Appendix A.6 column contract. Paper perfection round: HTML + PDF + resume PDF re-rendered. Cost-benefit ledger for the turn: $0 spent, 0 credits, ~10 doc files touched, 0 live behavior changes (docs-only turn; no test run needed).
- **REPO SECOND PASS 2026-09-11 (launched):** Paper+resume rewritten; now docs hygiene: production READMEs (last touched pre-champion), canonical map (missing select/slate/prereg/freeze-playbook surfaces), stale "not live" lists across reports, `docs/diagrams/` (5 files, staleness unverified), Appendix A map. Then paper perfection round (HTML/PDF re-render).
- **STRESS 2026-09-11 (champion survives everything, uncommitted):** White-lite properly demeaned (first attempt uncentered, biased to fail — caught, fixed, documented in script): luck-max p50 +2.8% / p95 +7.4% / **p<0.0005** (0/2000 null resamples print the observed +15.5%). Exclusions: worst cell drop (4.5-under, 162 tickets) still +13.3%; worst month drop (2025-04) still +12.9%; September-2025 slice +22.0% (n=51, WR 0.63 — the remainder-of-season analog holds). Selection + stress + judge all in `select_2025_report.json`. 346 green. Is it optimal? Honest answer: it is the max-LCB config under pre-registered constraints on 2025 that repeats on 2026 — not a proven global optimum, and White only rules out luck-of-the-best, not regime change. That is the most "sure" this data can buy.
- **2025-LOCK RESULT 2026-09-11 (executed, no production change):** `select_2025_champion.py` scored all 36 prereg configs on 2025 only (exact in-memory filter; economics via canonical `bet_pnl` — caught + fixed a hand-rolled payout bug that printed +30% before it could mislead). 13/36 pass constraints. **Champion: floor 0.12 / cap 0.24 / under-lean (+0.04 over premium) / DK+FD-only** — 2025: n=558, ROI +15.5%, WR 0.60, LCB +8.0%, CLV +1.24pp, conc 0.39. Disclosed-peek 2026 judge (one look): n=485, ROI **+15.6%**, WR 0.60, LCB +7.7%, CLV +1.30 — repeats. Report: `artifacts/odds_log/select_2025_report.json`. Diffs vs live: cap 0.20→0.24, under-lean premium, DK+FD-only universe. Promotion is a separate sign-off (needs your word + peek disclosure in the commit).
- **SELECTION ENGINE 2026-09-11 (built + verified, uncommitted):** `juiced_replay_ledger.py` takes `--floor-uniform / --cap / --under-lean / --book-universe / --out-suffix` (veto+probation always on; default rerun reproduces canonical byte-identical: n=2,077, rejects 12542/3128/1866, ROI +7.28%). First selection sidecar: floor 0.08 + cap 0.20 → n=4,558, ROI +7.0%, WR 0.51, 635 `edge_cap` refusals as first-class reason. 337 tests green. Next: run the prereg family grid on 2025 only, pick by LCB rule, judge 2026 once. Say the word.
- **PROMOTION 2026-09-11 (executed, owner order):** Champion live: `edge_cap` 0.20→0.24, `under_lean_premium` 0.04 (overs clear floor+0.04, `below_lean_floor` skip), `fill_books` [DK, FD] (live SharpAPI already carries only DK+FD — enforced as a guard, 0 filtered today). Board-fired same evening: Snell U7.5 FD **BET** (edge 19.3%, under the 0.24 cap that refused it at 0.20 this morning — the promotion working as designed), Lugo O4.5 HOLD (veto). Stakes/veto/probation/clip/Kelly untouched. 345 green, pin green. Peek disclosure: selected on 2025 (+15.5% LCB +8.0%), judged once on 2026 (+15.6%).
- **PUSH 2026-09-11 III + STOPPING POINT:** 3 commits pushed, remote current: CI-safe tests (`da3f8bd`, CI green) → paper restructure + pipeline fix (`e7e3096`, 28 pages, validator pass) → badges + log (`91d1f82`). Tree clean. Session closes here.
- **BANKROLL + PIPELINE 2026-09-14 (owner order, executed):** Bankroll declared **$2,000** → `kpi_policy.json` new `bankroll` block: unit $50 (2.5%), single-ticket cap $100–$200 (5–10%), enforced via existing per-line $150 (7.5%) + per-game $200 (10%); 1/16-Kelly typical stakes $15–46 (0.8–2.3%) verified from `market.py`. Full suite **347 green**. Ntfy pipeline live-fired (test record `pipeline_test_20260914.json`, `ntfy_status=200`). Self-check: RISK explained — MorningWorkflow exit 1 = 9/11 degraded-by-design (statcast FAILED blank-error, board still posted + banner fired, machinery worked); EndOfDaySettle 1073807364 = box off overnight; NightlyDrift still unregistered (owner). Midday/Second/Watcher green. Open proposal (needs one-liner): daily cap $500→$400 (25%→20% of bankroll) + numeric stop-out (e.g. −20u pauses new policy).
- **BANKROLL $5,000 2026-09-14 (owner order):** unit $50 = 1.0% ≈ 1/16-Kelly anchor (1.05%) — essentially exact, prior 2.4x-aggressive note withdrawn. Single-ticket cap $250–$500 enforced tighter at $150/$200; daily $500 = 10% (reasonable — $400 proposal withdrawn).
- **CONFOUNDER AUDIT 2026-09-14 (built, $0, no live change):** `confounder_strata_audit.py` → `confounder_strata_report.json/.md` (taken n=2,077). Findings: (A) edge band survives within DK+FD open+morning; BR morning high-edge toxic. (B) 4.5-under +14.2% (n=475) within-cell; 6.5-over +26% (n=136, watch). (C) book effect survives within snap: BR morning −0.6% (n=804) vs DK+FD morning +12.2%. (D1) 135 morning price pairs: DK+FD pays better 77.8% (+0.056 decimal) — BR softer against us, matched-pair confirmed. (D2) paired CLV best at DK+FD (+1.37pp). (E) wins land on worst-paying tickets within cells (fav concentration). Caught+fixed mid-build: D-zero-pairs was structural (candidates hold one row/ticket — D now joins book_lines via join_keys), E recompute verified exact vs stored pnl. Supports DK+FD-only champion.
- **SLATE EXPOSURE 2026-09-14 (display-first, live):** `_attach_slate_exposure` in `src/Python/odds_board.py` → `n_game_bets` + `game_bet_stake` per game + meta; BET logic byte-identical, 2 new tests. Live-fired same day on the 9/14 board (Roupp row shows his game's $39.80).
- **STATCAST BLANK-ERROR 2026-09-14 (fixed, root cause):** `throw "string"` + `$_.Exception.Message` = $null → blank banner. Replaced with `$_` in all 4 workflow scripts; proven NON-BLANK via repro. Note: verification run fired a truthful live "No BET" morning alert (box off, stale slate).
- **MANUAL RECOVERY 2026-09-14 (box off 9/12–9/14, executed):** Morning tasks never ran (box off). Ran chain manually: statcast→09-13 (14,633 rows) → features → projections → board → poll → alert. Board: 19 scored, **1 BET (Mathews U4.5 DK +116, edge 15.2%, $39.80/0.80u)** + 2 veto HOLDs (4.5-over) + 16 skip. Ledger: 19 open rows for 9/14. Ntfy alert sent. Exposure columns live-fired correctly (Roupp row shows his game's $39.80).
- **AUDIT DOC 2026-09-14:** confounder report promoted to `docs/reference/reports/confounder_strata_audit_2026-09-14.md` (dated evidence + verdicts + method traps). Answer to "where is it documented": report md there, machine JSON in `artifacts/odds_log/`, generator script in `production/ops/market_research/`.
- **WAKE RECOVERY 2026-09-14 (built, parse-verified):** `run_wake_recovery.ps1` — one-button full chain (statcast→features→projections→board→poll→alert), best-effort + failure banner, never settles intraday, never fabricates missed slates. Use after any multi-day gap instead of the manual chain above.
- **GLICKO 2026-09-14 (research, no live change):** `glicko_pitcher_ratings.py` → 601 pitchers, league K-rate 0.219, YoY r=**0.938** (n=427, gate PASS). Top: Misiorowski/Crochet/Glasnow/Snell/Skubal; bottom: soft-tossers. 386 wide-RD arms (= the staleness tails, quantified). v1 opponent-unadjusted; no minors needed (minors only matter for call-up priors, still parked).
- **HOOK TABLE 2026-09-14 (research, no live change):** `hook_pull_table.py` on 2025–26 Savant (353,824 PAs): base pull 10.0%, 103 cells (54 thin, flagged). Hottest: 90–99 pitches + 3rd TTO ≈ 32–37% pull — matches baseball reality. This is the pull decision the post-season Monte Carlo needs; bullpen-rest + pitcher random effects scoped as follow-ups.
- **PUSH 2026-09-14 (owner order):** commit `4429206` (17 files, +771/−27: bankroll, audit+report, exposure, banner fix x4, wake recovery, glicko, hook, paper Walsh&Joshi cite). Hooks passed, remote current, **CI success (45s)**. No CI/CD issues.
- **GLICKO GATE 2026-09-14 (KILL-final, owner: wire evidence ASAP):** `ledger_gate_glicko.py` — pre-registered RD-shrinkage overlay on the bettable panel (n=1,008, as-of ratings, no lookahead): base 0.2481 → overlay 0.2647, gain **−0.0166** (33x bar, wrong way). Verdict: overlay vehicle dead — as-of RD is wide almost everywhere (prior 300 PA vs partial-season evidence), so it shrinks everything to league. Table stands (YoY 0.938); remaining path is R-as-retrain-feature (offseason) or nothing. No second rounds.
- **GAME CAP 2026-09-14 (SURVIVE, wired off-by-default):** `game_cap_audit.py` — keep-max-edge-per-event on juiced taken: ROI +7.28%→**+10.83%** (+3.55pp), WR 0.51→0.47, volume −48% (547 multi-ticket events, 1,683 pairs, 0 unmapped). Event-clustered bootstrap (right level): delta +3.5pp, 90% CI (+0.3, +6.8), positive 96.4%. Cap menu (same set): cap1 n=1078 +10.83% +$5,835 / cap2 n=1625 +8.88% +$7,218 / cap3 n=1913 +8.42% **+$8,056 (max dollars)** / none n=2077 +7.28% +$7,559 — third-tickets-per-game lose money. Enforcement built in board (`_apply_game_cap`, `game_cap_hold` reason, stakes untouched, fail-open when key absent) + 2 tests; full suite **351 green**; commit `79ee6af`, **CI success (54s)**. Activation is one policy key (`game_cap_max_bets: 1`) — owner's call, live BET set changes on activation.
- **DEFERRED REVIEW 2026-09-14 (owner Q):** Promote now: asym over-floor (shadow-green + same disease as veto — needs real-stake confirm, gate it, don't block on veto); champion→monotone only if unders stay soft 2–3 weeks under veto. Stay parked: MLflow (no train loop), hard-veto ≤3.5 overs (starves learning), retrain/count-layer fix (probs aren't the bind — selection is), live calib promote (still trails market). SSAC 9–13 = paper polish, after abstract.
- **MAX-PNL 2026-09-14 (owner: too conservative? wire evidence):** `pnl_sensitivity_audit.py` on juiced taken. (1) Floor grid: 0.05–0.12 identical (live floors bind ≈0.12); RAISING kills dollars monotonically (0.14 +$6,477 → 0.18 +$449 → 0.20 −$517). Not too conservative — correctly placed. Money engine: 0.12–0.18 band = **94% of PnL ($7,110/$7,559) on 54% of tickets**. (2) Single-slip top-1/day = −$1,082 (−6.9%): monster edge = we are wrong. In-band top-1/day = **+$3,008 (+20.7%, 291 days)** → wired as alert `Slip pick` line (`_slip_pick`, display-only, 3 tests). (3) TBF tails: hook n=13 (+55% thin), marathon n=0 — TBF≥15 gate already removed the risk; drop-tails delta −0.003 (hurts). No new TBF policy justified; TBF work stays modeling-side.
- **THESIS UPDATE 2026-09-14 (supersedes 9/01 working thesis in part):** Old: side error, veto the bleed. New, measured: (a) PnL concentrates in the 0.12–0.18 band — protect the band, not the floor; (b) single-slip rule is in-band top edge, never max edge; (c) third-ticket-per-game loses money (cap menu: cap3 max dollars +$8,056, cap1 max rate +10.83%); (d) TBF policy gate sufficient as-is; (e) book choice (DK+FD) + clock (early) + slip rule now outrank any modeling lever. Docs audit: golden_metrics current, handoff replay-era accurate, strategy-plan §9/01 kept as dated history (this entry is the live thesis).
- **REVIEW + SUSTAINABILITY 2026-09-14 (owner Q, executed):** Verified clean — banner `$_` in all 4 workflows (remaining `Exception.Message` hits are real-exception warnings + 1 comment); bankroll block read by nothing (declaration-only by design); wake task unregistered (the miss — see below). Full suite **349 green**. Doc: `docs/reference/reports/ratings_hook_wake_2026-09-14.md` (Glicko method + pipeline slot, hook base-pull meaning + TBF metric targets, wake status, revert map, direction). Revert map: bankroll=delete key, exposure=delete helper, banner=git revert, wake=`schtasks /Delete`, research=delete files.
- **NESTED HONESTY + FRESH JUDGE 2026-09-14 (owner: are numbers lying?):** The 2025-window→2026-sim design was ALREADY executed (`select_2025_report.json`: 36 configs, LCB rule, champion floor-0.12/cap-0.24/DK+FD, 2025 +15.5% LCB +8.0%, disclosed-peek 2026 +15.6% LCB +7.7%, White p<0.0005). A NEW family on the same data = snooping (anti-pattern, refused). Clean judge = rest of September: locked champion + fresh data + fills starting. Number hygiene: CLEAN = frozen model, vendor timestamps, rejected retention, White-lite, game-cap bootstrap; PEEKED (confirmatory, never promotion input) = all 2026 slices, harness fair ROI, asym/slip/cap rules measured pooled; FOLKLORE = fair-price harness +62–102% (open-soft, never a slip).
- **FIRST FILL 2026-09-14 (money truth starts):** owner firing Novig — Mathews U4.5 today. Needs fill price + stake to log in `real_bets` (paper had +116 / $39.80). Fills habit is now the #1 program input.
- **FILLS LOGGED + PAPER REPAIR 2026-09-14 (owner numbers, verified):** Mathews U4.5 +117 $40 (pending); Snell U7.5 −125 $75 = LOSS (8 Ks, by the hook); Brown U4.5 +117 $35 = LOSS (7 Ks — first write said win, caught + corrected with note tag); Tidwell U4.5 −108 $50 = WIN (SF start, 4IP 2K — see dispute entry). Real ledger: 1W-2L, −$63.70 on $160. Paper repair via grader-refused rows (already settled → direct patch, #83 precedent): Brown FD row settle 0→7 (win +$39.15 → loss −$37.64); Tidwell rows settle 0→2.0 (kept as wins, canonically recomputed). Paper settled PnL $629.66→$546.17; paper_quant re-ran: full n=324 +$619 (+3.0%), veto n=264 +$1,322 (+7.0%). Docs updated to match (manuscript Table 3, resume, golden_metrics Sharpe/DD) + PDFs re-rendered. All books assumed Novig — owner corrects if wrong.
- **TIDWELL DISPUTE 2026-09-14 (owner right, agent wrong — CORRECTED):** agent checked the WRONG team (NYM bullpen game, never verified team). Blade Tidwell started for SF (4IP, 2K) → U4.5 WINS. Real ledger: Tidwell win +$46.30; money truth **1W-2L, −$63.70 on $160**. Paper rows restored to settled@2.0 (+$124.26, canonically recomputed). Paper settled PnL now $546.17; paper_quant: full n=324 +$619 (+3.0%), veto n=264 +$1,322 (+7.0%); docs + PDFs updated. Rule upgraded: boxscore search is by PLAYER across the slate with team confirmed — never assume the club. Grader `--settle/--void` refuse settled rows (by design); settled-row repair stays direct-patch with note tags (#83 precedent).
- **GRADING AUDIT 2026-09-14 (owner: am I double-counted?):** verified clean. 48 exact-duplicate row groups in raw ledger (re-poll appends, e.g. Zebby 9/9 ×3) — all neutralized by canonical `dedupe_ledger_props` (1,746 raw → 1,011 props; 9/9: 56→32). Every cited money number rides the deduped track. Dupes are ingestion hygiene (poll replace-semantics), filed, not PnL-affecting. Settle-vs-actual rule restated: winner = side vs line vs verified Ks (under = K < line), DNP = void, never 0.0.
- **PAPER SCOPE 2026-09-14 (owner rule):** manuscript is general/aggregated only — no current recommendations or targets. Fixed the one borderline case: Snell vignette now labeled illustrative-mechanics-only with its realized loss stated. Backtests report ROI/WR + CLV; paper_quant adds Sharpe/Sortino/DD/Calmar; universe adds Brier/skill/ECE; weekly pack adds CIs. That is the full metric surface — nothing else is compared anywhere.
- **RECATEGORIZED 2026-09-14 (owner: follow the plan):** Kalshi-disagreement filter → FORWARD next build (cheapest unbuilt edge, lake-measurable $0, needs a kill rule). Alt-line measurement → FORWARD after Kalshi (descriptive first). September-dilution cohort + day-game policy stay owner-gated (dawn probe). Fills analytics stays at ~20 fills. Game-cap key + elevated setup still awaiting owner.
- **KALSHI FILTER 2026-09-14 (KILL-final):** `kalshi_disagreement_audit.py` — pre-registered refuse-if-|p−kalshi|>0.10 on close-anchored Kalshi (676 markets; settled ladder prices rejected as post-game lookahead). Matched 1,217 ledger rows: kept ROI −0.95% (n=611) vs full +0.66%, refused +0.98% — agreement is where profit ISN'T; our edge lives in disagreement (timing/selection thesis, not consensus-following). Caught+fixed mid-build: WR-via-pnl>0 garbage on unstaked rows (0.07!) → WR over decided-staked only. Vehicle dead, no live input. Next: alt-line measurement.
- **SEPTEMBER FLAG 2026-09-14 (measured):** `september_cohort.py` — frozen-projection errors September (n=802) vs rest (n=7,973): MAE 1.841 vs 1.765 (+0.076), bias +0.155 vs +0.113. Dilution is real — September starts are harder and we over-predict more. October: gate September separately. Live proposal (needs one-liner): September edge premium (+0.02, lean machinery) for the rest of the month.
- **PRICE BANDS 2026-09-14 (descriptive):** plus-money (n=1,266) ROI +8.3% PnL +$5,234 WR 0.419 vs short chalk +4.1%/+$925 vs heavy chalk +7.7%/+$1,399 WR 0.725. 69% of PnL lives in plus-money at 42% WR — payout selection, not a staking change (Kelly already scales; flat stays benchmark).
- **DIARY ARCHIVE 2026-09-14 (owner order):** FORWARD diary (sign-offs history, #113 steps, #66–#120, items 10–17; 145 lines) → `docs/reference/reports/forward_diary_archive_2026-09-14.md`. FORWARD now holds only live pointers (sign-offs, ops, agent rules, October, shelf). Backlog 686→568 lines. Rule going forward: session work lands in PRESENT bullets; FORWARD stays pointers-only so this never re-grows.
- **ALT CURVE 2026-09-14 (descriptive, done):** `alt_curve_measure.py` — live config scored at 0.5–14.5 over 8,775 starts, 6,236 with book alt-close. Skill negative at every covered line (worst mid-curve, near-tie 8.5); 11.5+ have zero book coverage (not dealt). No alt-line refuge — main-line game stands. Caught+fixed: pitcher-ID-vs-name join (0 rows → 8,775) + a stray-bug draft rewritten before first green run.
- **2026 SEASON METRICS 2026-09-14 (owner Q — full surface):** `season_metrics.py` → `season_report.json`. 2026 taken (n=769, 145d): ROI +12.6%, PnL +$4,841, WR 0.54, Sharpe 2.79, Sortino 5.28, maxDD 10.8u, Calmar 8.94, CLV +1.30pp (beat 63%), xROI-edge 0.184, conc 0.25. 2025 (n=1,308): +4.2%, +$2,717, Sharpe 0.95, DD 35.9u, CLV +0.95pp. 2026 confirmatory — readout, not a promotion input.
- **DISAGREEMENT FLAG 2026-09-14 (owner Q — deferred with reason):** no live Kalshi feed exists at board time (scaffold is historical pull only), so a live flag would be dead plumbing. Parked until Kalshi-at-board-time machinery is scoped; the slip-pick band rule already encodes humility at the board.
- **DISAGREEMENT SIGNAL 2026-09-14 (owner: is the gap itself a signal?):** distinct from the killed filter. `disagreement_signal_probe.py` on universe×Kalshi (n=658): corr(gap, residual) = **−0.1375** (2.75x bar) — when we exceed sharp, outcomes come in lower. `kalshi_overlay_gate.py` (beta=0.5 halfway-to-sharp): base 0.2832 → overlay **0.2569** (+0.0263, 52x bar, all 9 cells green). Shrinkage control: pure halfway-to-0.5 gives 0.2619 — ~80% is generic shrinkage (already owned via WS1c), Kalshi direction adds +0.005 (10x bar). Verdict: SURVIVE-narrow as display-only disagreement FLAG, NOT a scoring overlay (stacking shrinkers = double-shrink). Parked until live Kalshi-at-board-time machinery exists.
- **HYGIENE 2026-09-14 (owner: cut useless docs):** audited — no dead files created this cycle (every script has a report; artifacts/ ignored by design). One genuine cut queued for owner sign-off: archive FORWARD diary #10–#72 detail to a dated report (holy-file surgery, not done unilaterally). Backlog stays master; nothing parallel exists.
- **XREF + MONTHLY + PROBCURVE 2026-09-14 (owner: no hallucinations, show time + curve):** cross-ref audit — every body→appendix pointer resolves (Figs 1–9 + Tables 1–5 + A.1–A.7 all exist; A.3 all 11 paths verified on disk); fixed dead (§8.2–§8.4 → §8), pinned vague A.7 rows to real artifacts, deleted orphaned equity png. `monthly_prob_curves.py` → Figure 9 (monthly PnL bars + ROI line, thin months greyed) + Table 5 (side × prob-decile). Findings: CLV ≥ 0 almost every month incl. red ones (May-25 −11.5%, Sep-25 flat); longshot bands both sides carry return (over p30–50 +14.4%, under p0–30 +6.7% at WR 0.67); under p50+ thins to +1.8% — books price favorites efficiently, longshots poorly. Format verdict: bars + line for months (time), TABLE for prob curve (13 cells read better than any chart). Table aggregations hand-verified before print (caught two of my own rollup errors).
- **PAPER PASS 2026-09-14 (owner: timeless, metrics-rich, professional):** verified clean — no real-money/fills-count/current-picks language anywhere (vignette labeled + outcome stated); n=26 numbers live ONLY in A.5 (purge rule holds); Table 3b already carries Sharpe (no edit needed); §7 freeze-state accurate (config unchanged since 9/11 — date stands); intro nine-batter false gate fixed earlier. Backlog→paper decision: season table + LCB/White in; slip rule, cap menu, fills = live-ops, correctly EXCLUDED (timeless rule). Prompt-file note: `Downloads/manuscript_rewrite_prompt.md` does not exist locally — worked from pasted content; stale demands rejected with reason (POV stays v2026-09-14 not 9/11; LCB stays disk-truth +8.0% not +7.4%; no new family runs). PDF rebuilt.
- **XREF + MONTHLY + PROBCURVE 2026-09-14 (owner: no hallucinations, show time + curve):** cross-ref audit — every body→appendix pointer resolves (Figs 1–9 + Tables 1–5 + A.1–A.7 all exist; A.3 all 11 paths verified on disk); fixed dead (§8.2–§8.4 → §8), pinned vague A.7 rows to real artifacts, deleted orphaned equity png. `monthly_prob_curves.py` → Figure 9 (monthly PnL bars + ROI line, thin months greyed, incomplete 2026-09 excluded per owner) + Table 5 (side × prob-decile). Findings: CLV ≥ 0 almost every month incl. red ones (May-25 −11.5%, Sep-25 flat); longshot bands both sides carry return (over p30–50 +14.4%, under p30–50 core +7.7%); under p50+ thins to +1.8% — books price favorites efficiently, longshots poorly. Format verdict: bars + line for months (time), TABLE for prob curve (13 cells read better than any chart). Table aggregations hand-verified before print (caught two of my own rollup errors).
- **CLOUD ANSWERS 2026-09-14 (owner: credits, teardown, payment?):** NO payment required, ever — Starter needs no card and workloads stop (never bill) at $30. Four credit guards filed in cloud README (no-card ceiling, client caps, cron-shaped load, dashboard glance). Teardown runbook filed: postseason HOLD stops predictions 9/27 automatically; `app delete` → optional volume snapshot → secret delete after the October judge (ad-hoc compute only, no cron spend). URGENT owner money item (not cloud): verify OddsAPI $119 auto-renew is OFF in the dashboard — calendar note dates to the 9/08 buy week.
- **SUB + TOTALS 2026-09-14 (owner decisions):** (1) OddsAPI sub STAYS through season end for 2025–2026 open+close coverage (all pitcher + batter props) — cancel-after-closeout, not same-week; closeout cadence spends ~600/day from quota, floor 100k. (2) Totals rationale restated: featured-totals lake is SLATE environment (common-shock/pace for correlation exposure caps), never a K feature — it answers "how correlated is today's slate" (533 games with 2+ tickets), which per-game caps and the October slate-cap design need. If it never feeds a cap, it dies as an unused lake — the kill rule stands.
- **MODAL ACCOUNT LIVE 2026-09-14 (owner: $30.00 confirmed on dashboard):** yes — $30 free every month (resets monthly, no rollover; no card → workloads stop, never bill). Client installed locally (`modal` import ok). Next: owner runs token step → secrets → volume upload → deploy → parallel days.
- **MODAL DEPLOYED 2026-09-14 (owner ran token; agent deployed):** token verified (cameron-kaplinger workspace) → secret `mlb-props-keys` created (values never displayed) → volume `mlb-props-state` populated (processed/odds_log/models/live_scores/projection_log/dimensions/kpi_policy, verified via `volume ls`) → `modal deploy` green in 27s (app `mlb-props`, 3 crons 08:30/03:00/05:30 ET). First live fire tomorrow 8:30 (cloud board + ntfy should mirror laptop). Caveats filed in cloud README: code snapshots at deploy (redeploy after edits), schedules unverified until first fire, close-sweeps unbuilt, volume-put console crash cosmetic. Credit answer stands: no-card hard stop + code caps + ~$3–5/mo load. Laptop stays primary through ≥7 parallel days.
- **CARD ON FILE 2026-09-14 (owner info — guard rewrite):** no-card hard stop does NOT apply; overages can bill. Cloud README guards rewritten: $10 mental tripwire + dashboard glance. SharpAPI pricing researched (docs): rate-limited per minute (Free 12, 2 books DK+FD = our universe), NOT per-request metered — 10-min polling (≈1,440/day vs 17k capacity) is feasible and free; the metered one is OddsAPI (capped). Proposed next build: 10-min intraday harvester (frozen probs, fresh quotes, frozen_edge_watch pattern) for game windows only.
- **DASHBOARD 2026-09-15 (owner: reset? morning-vs-close?):** NO reset — resetting destroys history; the bad Mathews CLV is signal, not dirt. The morning-vs-close comparison already exists (`results_pnl_clv.ipynb`: rolling beat-CLV, stake-weighted hit-rate, side splits, availability) — re-executed clean (6 cells, 0 errors) instead. Mathews paper close still missing (sweeps/watcher fill it); real +117 vs paper +108 is the fill-vs-paper drift fills-analytics tracks.
- **AUTO-HEAL + HEARTBEAT 2026-09-15 (owner: 12:02 RISK still stale — fix causes):** the 12:02 lines were TRUE (tasks last ran 9/11–9/12, box off) — messenger innocent. Causes fixed: (1) Morning auto-heal: stale slate (>3d) triggers ONE deeper cycle (trailing re-pull + features + re-log) before stale_data HOLDs; single attempt, never loops. (2) Cloud heartbeat: every function appends to volume `cloud_heartbeat.jsonl` (proves firing silently; ntfy stays for boards/failures). (3) All 5 Modal functions now call `_link_state()` (morning/hourly were missing it — found by audit). Redeployed. Elevator still out (elevated setup = owner minutes); cloud covers future mornings regardless.
- **NIGHTLY TRUTH 2026-09-15 (owner: is the sweep right? does harvest change policy?):** (1) Overnight chain is NOT running: MorningWorkflow last ran 9/11 exit 1, settle last ran 9/12 (box off overnights); today's board came from the 11:00 hourly catch-up. Features thru 9/13 (2d weekend lag, labeled allow-stale) — not catastrophic, but the elevator is out: NightlyDrift + elevated setup remain THE fix. (2) PAPER HOLE found + repaired: 18 of 19 9/14 rows vanished (overnight re-polls dropped finished games before settle filled closes). Money-safe (all stake-0; Mathews settled −$39.80 correctly) but CLV history lost. Root fix in `replace_open_slate`: started-game rows are settler evidence, never poll fodder (event_start_time_utc gate) + test; old drop-test re-dated (fixed dates rot — new rule: far-future clocks in time-sensitive tests). (3) Harvest needs NO policy change: it executes inside policy (floors/veto/cap applied by the board; watch asserts veto) — only its yield is monitored. Real Mathews: LOSS −$40 (8 Ks); money truth 1W-3L.
- **STALE-1 + CLOUD PROOF 2026-09-15 (owner orders):** (1) Stale cap 3d->1d: anything over a day old HOLDs (consequence accepted: one missed day silences betting until refresh succeeds; auto-heal runs first). (2) Cloud heartbeat PROVES independence: sweeps q20min + hourly 13:00 ET all firing with laptop uninvolved -- Modal chain is fully automated with the computer closed (keys server-side, volume persistent). First 08:00 cloud morning tomorrow. (3) Silence explained: hourly exit 0 + zero flips = correct quiet (flips-only design); morning misses are box-off, covered by hourly catch-up + cloud from tomorrow. Next: laptop primary through parallel proof; cutover after 7 agreeing days.
- **DELETES 2026-09-15 (owner: clean what we do not need):** 0-byte projections corpse deleted; `scripts/longleaf/` (6 HPC files, former workflow, zero live refs) `git rm`d. KEPT with reason: playground/ (manual what-if tools, referenced), analysis/ (provenance), notebooks (deferred panel work), paper PDFs (portfolio), WS1c backups (revert), sidecars (evidence), archive/ (history). Untracked earlier (on disk): DailyPitcherModelTrainingData + Pitcher-Starts (zero refs).
- **FUTURE PLANS 2026-09-15 (owner: what is next -- from backlog+docs):** REST OF SEPTEMBER: September judges (automatic) + fills to 20 (analytics powers on) + 7 cloud-agreeing days. OCTOBER (post-9/27): slate caps from exposure columns -> full-season re-judge (fresh September included) -> stacker verdict -> legacy re-gates (floors, veto, deploy matrix) -> Modal cutover -> teardown runbook. OFFSEASON: MLflow v1 (retrain loops), Glicko R as retrain feature, Monte Carlo (hook table ready), batter markets (10.9M rows banked, needs a model), ticket-key market migration, filter-repo history slim. 2027: multi-market automated suite (K + outs + batters) on the ledger schema + dashboard v1 + workshop paper. Live decisions still only: flat sizing, game-cap key, dawn probe.
- **RISK REWRITE + STALE GATE 2026-09-15 (owner: gibberish alert, no stale boards):** (1) Self-check TASKS fixed (Midday/Second were deleted days ago -- the alert watched ghosts): now Morning/Hourly/Watcher/Settle/Drift/SelfCheck. Every risk line now says WHAT failed, WHEN, WHAT to do (per-task advice; unregistered tasks say so). (2) Stale-data fail-closed gate in the board: features older than 3d (override `max_stale_days`) -- BETs become HOLD tagged `stale_data`, math untouched + 1 test. Unknown freshness = fail-open. This morning would have held, not bet, on 8-day features. (3) Modal 1 cent confirmed -- bursty batch is exactly what serverless is cheap for.
- **DEDUP + CLEANUP 2026-09-15 (owner: one ping per event, delete list, plan):** (1) 8:00 collision fixed: hourly moved 09:00–22:00 (morning owns 08:00) on laptop + Modal; Midday/Second already deleted. (2) Cloud-vs-laptop doubles fixed: `MLB_PROPS_NO_ALERT=1` in Modal morning+hourly (preview-only, exit 0; tested + unit test) — laptop is the sole alerter until cutover. (3) Delete verdicts: UNTRACKED (kept on disk) `data/DailyPitcherModelTrainingData.csv` + `Pitcher-Starts-2023-2025-Data/` (both zero code refs; .gitignore extended) — future pulls slim down. KEEP: friend open CSVs (live open clock), notebooks (deferred panel work), paper PDFs (portfolio), WS1c backups (revert paths), sidecar parquets (evidence), archive dirs (provenance). (4) Slip pick = the single-slip choice: max edge INSIDE 0.12–0.18 (Manaea 13.4%), not max edge overall (Soroka 20.1% refused by the slip rule, still a BET). Separate line because the board ranks all BETs while the slip stars one. (5) Edge harvest = genuinely new lines: skip/HOLD→BET flips on intraday moves (vetoed lines can never flip — assert fail-loud). Plan: fills → September judge → October program; your words: flat sizing, game-cap, dawn probe.
- **DOC AUDIT 2026-09-14 (owner: every file gospel — executed):** all ~100 md inventoried. Fixed: RUNBOOK+INDEX MorningTime 08:30→08:00; market_research README (was missing all 9/14 probes — now lists every script with verdict); root README LCB +7.4%→+8.0%. Verified accurate: diagrams (03 shows Poisson+WS1c+champion; 00 self-labels its one stale line), A.3 all 11 paths resolve, root pipeline section, outs-expansion scope. Data verdicts: processed/lake/kalshi/command/dimensions/friend-CSVs/Savant all live-used; Pitcher-Starts-2023-2025-Data orphaned (0 code refs) → marked SUPERSEDED, archive offline in offseason, no delete. Nothing else dead; no moves/renames needed.
- **SLATE-FINAL + CONSOLIDATION 2026-09-14 (owner orders, executed):** (1) `_apply_slate_final` in the board: final game started → BETs demote to HOLD tagged `slate_final` (math untouched, meta `n_slate_final_hold`) + 2 tests; full suite 358 green. Wired where scanning stops: board (no new BETs), watch (no flips on started games) — poll keeps logging HOLD rows, ledger stakes persist. (2) MiddayRefresh + SecondRefresh DELETED (hourly covers them) + setup lines removed; 9 tasks → 7. One script per job holds except the intentional pair (refresh ps1 = board chain for all intraday tasks). (3) Modal timing table in cloud README (all ET). (4) ASCII purge: em-dashes in ps1 break PS5.1 string parsing (second occurrence — refresh script died on it); all 6 workflow scripts verified ASCII + parse-OK. Redeployed cloud (code changed).
- **FLIPS + PERSISTENCE 2026-09-14 (owner orders, executed):** (1) Hourly refresh alerts flips-only: watch pages flips itself; full board alert fires on failure OR flips, quiet hours silent (morning keeps always-fire). (2) Logged BETs persist in `replace_open_slate`: same-day open rows with stake>0 survive re-polls AND suppress fresh dupes (11am snipe counts at 1pm; no double-count). Merged with the stake-carry into one rule + 2 tests; full suite green. (3) RotoGrinders answer: NO — midday/hourly chains never re-pull lineups (no refresh_statcast/log_projections step); projections frozen AM by doctrine, only quotes refresh intraday. Fresh dawn lineups would need a dawn poll (still gated on the probe).
- **DYNAMIC LINEUPS + SWEEPS LIVE 2026-09-14 (owner: keep going until complete):** (1) Doctrine change: projections re-log on EVERY board run (step 0 of refresh + modal hourly) — frozen-AM retired, quotes AND projections refresh, watch diffs the combined result. (2) Close-sweep cron wired into Modal (5 functions live: morning/hourly/sweeps/settle/drift); laptop sweeps via `run_close_sweep.py`. (3) Persistence confirmed hour-agnostic: same-slate rule, all future hours covered (tested). (4) Fixed a real parse break: em-dashes in ps1 kill PS5.1 ANSI reading — ASCII-only rule now enforced in scripts by verification. (5) Deferred usefulness verdict: MLflow YES for offseason retrain loops (reopen then), retrains YES for new markets (2027), dashboard YES future — triggers filed, not started. Resume note: paper (Table 3/3b, White, LCB), selection engine, 20+ gates, cloud cutover, Cavs-fan-grade honesty discipline — portfolio-ready when fills attach. (6) Modal bulletproof list: redeploy-on-code-change (done twice today), volume backup = weekly `volume get` to laptop (runbook), schedules proven only by firing (tomorrow 8:00 first proof), no-card→card guard rewrite done, closeout stays manual. Fully automated for Ks = done except fills habit + parallel proof + cutover.
- **HOURLY + 08:00 + SWEEPS 2026-09-14 (owner orders, executed):** (1) Tong struck from real_bets + entry annotated (discussed, never fired); paper verified clean (0 Novig rows, no +117 anywhere — paper holds DK/FD paper prices only). (2) Stake-carry fix in `replace_open_slate` (midday re-polls zeroed morning paper stakes — Mathews $39.80 restored with tag; doctrine now enforced in code) + regression test; full suite green. (3) `run_close_sweep.py` built (ET-window-gated close fills, dry-run 16/19 live) — replaces the daemon at cutover. (4) Laptop: MorningWorkflow → 08:00 + new HourlyRefresh (60min × 14h, board+alert to 22:00). Modal: morning → 08:00 + new `hourly_refresh` cron; redeployed (4 functions confirmed). (5) Watcher audit: 4 files (close_watcher.py, run/start/watch scripts) — KEEP until sweeps proven; consolidation = deletion at cutover, no early deletes. Laptop automation untouched otherwise (owner: keep until Modal proven).
- **INTRADAY 2026-09-14 (machinery working):** 13:45 refresh re-ran board — Mathews 116→108 (edge 15.2%→13.5%) now skip via dynamic floor (elevated 0.14 on warns); paper ledger holds the morning take (doctrine: intraday decay never un-takes). Status alert sent live (ntfy 200; note: used failure-banner path for a status note — cosmetic misuse, morning record untouched; PS ate the $ signs — single-quote messages with $ henceforth).
- **MODAL SCAFFOLD 2026-09-14 (owner: move forward):** `production/cloud/modal_app.py` + README — image, volume, secrets, 3 crons shelling to the exact laptop scripts. Data design: hot state ~350 MB on volume (one upload; Savant raw + lake stay local, incremental pulls over network); parquet stays (18k-row scans, single writer — SQL buys nothing, Neon sleeps); env overrides fix the Data/data case gap on Linux; .ps1 wrappers replaced by direct .py calls; watcher becomes cron-sweeps at cutover. SQL verdict: NO (revisit only on contention). Parse-verified, undeployed (needs owner token). Go-live checklist in README; ≥7 parallel days before laptop retires.
- **CLOUD RESEARCH 2026-09-14 (owner Q — cost/benefit with sources):** `docs/reference/reports/cloud_hosting_2026-09-14.md`. Verdict: **Modal Starter** ($0, $30 credits, 5 crons, 1TB volumes free; our load ≈ $2–5/mo; no card, Slovakia-safe) beats Actions (private+free = cron DISABLED — the gotcha; needs Pro $4 or public repo) and Render/Fly (sleep kills cron). State: Modal Volumes (zero migration) > Neon (only if relational needed) > Actions cache (hacky) > DuckDB-in-repo (avoid). Cutover post-9/27 with ≥7 parallel days; scaffold may start anytime (zero policy surface). SharpAPI/ntfy travel as env secrets on any host. Owner gates: Modal signup (10 min) → scaffold → parallel → cutover.
- **OPTIONALITY + SIZING + FILLS 2026-09-14 (owner: books must deal, we may hold):** paper gains the optionality paragraph (§6: bet-or-hold as the edge books cannot have; bettor's ECE 0.084 vs universe 0.021; book comparison = Brier skill + CLV + ROI jointly, never one). Sizing arms risk-compared on replay: flat ROI 7.28%/Sharpe 1.61/DD 35.9u vs Kelly 6.65%/1.40/41.2u vs band +$11,562/1.38/69.9u vs robust ROI 7.8%/DD 27.3u (different set). Verdict: 1/16 stays as humility premium under 8pp bettor error (paper says so now); flat stays benchmark; band maximizes dollars at 2x pain (filed, not proposed). `fills_analytics.py` built: 3/50, verdict TOO THIN (ROI suppressed by design until n=20). Is 1/16 "correct"? As insurance, yes; as growth-optimal, no — and insurance is the job until fills arrive.
- **JUDGMENT 2026-09-14 (owner: judge now, not October):** `sizing_judgment.py` — nested bakeoff (pick by 2025 Sharpe): flat 0.95 wins → 2026 confirms (Sharpe 2.79, ROI +12.6%; robust +16% rejected, different set + worst 2025). PROPOSAL: switch live sizing to flat $50 (nested winner; simpler; better DD) — owner's word, changes stakes. September premium KILLED (delta −0.0005, drops 19, loses money — flag stands, vehicle dead). Day/night: night +8.2% vs day +6.2%, both green; morning weak in both (+2.3%/+0.9%) — clock, not day, is the problem; no day-game policy action, dawn probe still valid for carriage. Implement-now list: flat sizing (proposed), game cap (staged), NOTHING else changes. Hold: retrains, floor/veto edits, Kelly tweaks, postseason, cloud cutover (scaffold may start, cutover post-9/27).
- **BETTOR'S CALIBRATION 2026-09-14 (owner: ECE/MCE mislead — correct):** universe ECE averages over lines we'd never bet. `bettable_calibration.py` deciles on TAKEN tickets: ECE **0.084** (4x universe 0.021), MCE 0.148; overs 0.126 / unders 0.119. Selection concentrates miscalibration — universe ECE must never justify sizing; the 1/16 fraction + caps are priced for ~8pp error, and the numbers now say so. Paper §8 keeps universe ECE as a model diagnostic with this caveat filed.
- **SIZING 2026-09-14 (owner Q — confirmed live):** live board sizes in 1/16-Kelly units (Snell 1.47u, Mathews 0.80u); flat $50 is the replay benchmark that beats Kelly everywhere (+7.3 vs +6.6). Switching live to flat needs fills + sign-off (tails: Kelly shrinks crazy tickets, flat doesn't) — NO change now. Tails read with salt (thin n) per owner.
- **OVERFITTING 2026-09-14 (owner Q — verdict):** defenses holding: pre-reg family, LCB (not max-ROI) selection, White p<0.0005, disclosed peeks labeled, kill culture (Glicko/Kalshi/command died on gates), September fresh judge. Residual risks, stated: 2026 peeked (confirmatory only), WS1c ate 2025 (ranking unaffected, labeled), monthly cells thin, September partial. Net: guarded, not clean — size the haircut accordingly.
- **CLOUD PRIORITY 2026-09-14 (owner order):** cloud moves to #1 October item (was #7): Modal scaffold can start WITHOUT touching live (new infra, zero policy surface) — build pre-9/27, cutover after 9/27 + 7 green days. Deeper analysis runs in parallel (September gate, price bands filed; next: day/night split, fills analytics). Categories fixed: FORWARD holds pointers only; research scripts live-or-die by gates in PRESENT; owner minutes in Waiting.
- **- **MONTE CARLO v1 2026-09-15 (owner: start offseason now):** `monte_carlo_starts.py` -- numpy-vectorized PA-by-PA sims (k_rate draws + hook-table pull hazard, labeled approximations) over 8,775 starts x 1000 sims. Findings: MC mean 4.70 vs Poisson 4.99 (-0.29); right tail THINNER under MC (11.7 vs 14.7%), left tail FATTER (17.4 vs 14.4%); line gaps peak mid-curve 4.5-5.5 (~5pp, the bleed zone). Caught+fixed mid-build: np.math removal, inner-import scoping, dict-merge count overwrite. Descriptive only; policy use needs bullpen/pitcher effects + gate.
- **COVERAGE + MC GATE 2026-09-15 (owner: gaps? Pikkit -17%? wire MC?):** (1) Coverage: ledger dates miss 8/09, 8/16, 8/24, 9/05-07, 9/12-13 (all documented gaps, box-off); closes 54.7% overall, thin on 9/10-11 (11-14%) -- 9/10-11 close backfill BLOCKED (projections purged, no rows to join) + 9/14 Mathews close missing (sweeps cover forward only). Config regimes labeled per era; rescore panels keep comparisons honest. (2) Pikkit -17% EV: their pre-game model read Mathews opposite to ours (+15-19% edge); outcome favored them this once -- process over single tickets; both models can be wrong, tracking resolves it. (3) MC REALITY GATE: `mc_reality_gate.py` n=69,416 line-points -- MC loses Brier everywhere (-0.0025, 5x bar wrong way) but WINS calibration (ECE 0.0174 vs 0.0258, MCE 0.0255 vs 0.0369). Verdict: KILL as scorer, NOTED as shape -- hook-hazard mean is honest yet the RATE is what scores (WS5b epitaph again). No TBF-architecture change: MC rides ON the k_rate x TBF pair (same inputs, distribution view); it does not replace the ridge. MCE/ECE improve via calibration maps (WS1c owns), not via sims.
- **STEAM DIRECTION 2026-09-15 (owner: what have BETs taught us?):** ticket-level open->morning steam vs outcome is UNMEASURABLE: 0/1048 morning-taken match a same (date, player, line) friend open (friend covers ~6-10 featured arms/day; taken skews obscure + lines move open->morning). Killed for lack of data, not lack of effect -- WS6 same-game panels (+0.044 open -> ~0 morning) remain the timing evidence. BET learnings ledger (all measured, all filed): edge band 0.12-0.18 carries 94% of PnL; monster edge loses; unders default (lean live); DK+FD only (matched-pair confirmed); plus-money pays 69% of PnL at WR 0.42; chalk unders thin to +1.8%; third-ticket-per-game loses; wins land on worst-paying tickets in-cell; agreement-with-sharp loses (-0.95%), disagreement pays (+0.98%). Concerns: fills n=6, morning ECE 0.10 at decision time, FD sparsity, September dilution flagged, close coverage thin 9/10-11, disk space (qwt trees). Moves: flat sizing word, game-cap key, dawn probe, fills to 20, September judge, October program.
- **CLV FLAVORS 2026-09-15 (owner: soft-book vs sharp?):** historically BOTH exist -- live `clv_pp` mixes same-book and cross-book closes (`allow_cross_book=True`); paid columns use devigged-consensus (median) close; juiced uses book-pair devigged close. `clv_flavors.py` separates them: same-book +0.11pp/42% (headline, fillable), consensus +0.26pp/56% (sharp ref), cross-book +0.61pp/59% (5x inflated soft-read -- the cherrypick hazard, measured). Rule filed: headline same-book, consensus alongside, cross-book never headlined. SCALE TRAP FOUND: live ledger is FRACTION scale, paid/juiced are PERCENT -- three conventions, now labeled in golden_metrics; history left untouched, readings standardized going forward.
- **LINE-SIDE POLICY 2026-09-15 (owner: per-line/O-U/floor from 2025, judge 2026):** pre-registered rule (take cell iff 2025 ROI>0 and n>=30, no fit): take over@5.5/6.5 + under@4.5/5.5/6.5/7.5; drop over@2.5/3.5/7.5 + under@3.5/8.5. 2026 judge: ROI +12.6% -> +13.7% (+1.1pp, n=691) = SURVIVE, confirmatory (October re-judge decides). Wired as display-only `cell_gate_2025` column on every board row (fail-open unrated; never flips recommendations) + 2 tests. Definitions verified for paper: xCLV = mean(model-close) on taken side (pp, real metric); xROI = mean taken edge (correct EV proxy under flat stakes, labeled upper-bound at fair prices); paper_quant CLV already taken-only (units>0) + deduped (no double-count); consensus = 9 named books, median of per-book devigged pairs. Paper gains simulated-PnL paragraph (fair harness +$146k/+51% 2025, +$48k/+33% 2026 -- upper bound, never promoted from)  + consensus-books transparency (9 named, BR 3x QA flag, no Pinnacle) + CLV flavors; PDFs rebuilt.
- **SUMMARY TABLE 2026-09-15 (owner: how do we look?):** verdict table added to paper S8 -- out-predict closes NO (-0.0043); beat-the-close NARROW YES (+0.11pp same-book); paid on selections YES (+7.3% juiced, +15.5% champion LCB +8.0%); calibrated where bet WEAK (0.084, sized for it); timing YES early. Under the hood: MAE_K 1.78 (target 1.75 unmet), ship-lift +0.0016, WS1c+Poisson live, deduped props n=1,013. Sharp-model verdict: NO, not sharp on probabilities -- and that is measured, not modesty. The sharpness is in process (selection/timing/gates), which is where the P&L lives. PDFs rebuilt.
- **METRICS PASS 2026-09-15 (owner: define, prioritize, contextualize):** notation extended (xCLV/xROI/LCB/WR/Sharpe/Sortino/DD/Calmar/beat-rate/profit-factor); priority ladder filed (model metrics govern the model, LCB>CLV>ROI govern selection, Sharpe/DD/concentration govern survival). New tracked: profit factor (1.08 2025 -> 1.27 2026), avg taken price (179 -> 161), turnover ($65k -> $38k). Missing-data verdict: beat rate already beside every CLV; nothing else structural missing -- next metrics come from fills (fill rate, paper-vs-fill drift), not from new formulas. PDFs rebuilt.
- **FLAT RECOMMENDATION 2026-09-15 (owner: shift or Kelly?):** switch live sizing to FLAT $50. Evidence: nested bakeoff (2025 Sharpe picks flat, 2026 confirms Sharpe 2.79 vs Kelly 2.59, DD 10.8u vs 12.1u); simpler to operate; matches $5k bankroll at 1.0%. Kelly stays defensible only as tail insurance, and tails are thin-read. One-line activation on owner word (board sizing call). Policy otherwise complete: veto/lean/cap/DKFD all measured-correct; game-cap + September-premium(false) staged; nothing else adjusts.
- **FLAT LIVE 2026-09-15 (owner: new policy -- executed):** `flat_stakes: true` in kpi_policy + board override (passing tickets stake exactly $50/1.0u; gate logic untouched; absent key = legacy Kelly). Pin updated same commit (also corrected stale LCB comment). Full suite 363 green. Modal healthy: heartbeat shows sweeps q20min + hourly all ok, app deployed -- cloud runs correctly with laptop closed. Next board carries flat stakes.
- **GAME-CAP ANSWER 2026-09-15 (owner: how with 2 pitchers?):** tickets multiply across books x lines, not pitchers. Distribution re-verified from disk: 531x1 / 259x2 / 171x3 / 87x4 / 16x5 / 12x6 / 2x7-8 tickets per event. A clean 2-pitcher-2-ticket game (259 events) is NEVER touched by cap 3 -- it only binds 4th+ tickets in 117 events (line-disagreement duplicates across books, both-sides stacks = same-game exposure doubled). Cap menu re-verified tick-for-tick: cap1 (1078, +10.83%, +$5,835) / cap2 (1625, +8.88%, +$7,218) / cap3 (1913, +8.42%, **+$8,056 max dollars**) / none (2077, +7.28%, +$7,559). Cap 3 adds ~$497 by cutting 164 measured losers -- it does not cost PnL. Activation stays a one-liner (`game_cap_max_bets: 3`) on owner word.
- **SLATE-CAP RESEARCH 2026-09-15 (built, shadow, no live change):** `slate_cap_research.py` -> `slate_cap_report.json`. Count caps confirm the game-cap menu; stake caps mirror them exactly (flat $50: $100=2tk / $150=3tk / $200=4tk). Under the pre-registered 90%-band-share rule every binding cap KILLS (top-3 holds 88.8% of band dollars despite max PnL) -- only top-4/stake-200 SURVIVES (93.3%, barely binds). High-total >=9.5 flag stays descriptive (+21.5%, n=327). Verdict: wire NO count/stake cap yet; heuristic 500/150/200 stands until the October design. Tension filed honestly: cap3 = max dollars but trims band share 94->89%; activation is owner's PnL-vs-band call.
- **STACKER ROI 2026-09-15 (KILL-final):** `stacker_roi_lane.py` -> `stacker_roi_report.json`. Same 981 panel, live filter replayed: base n=88 ROI +10.1% vs stacker **n=0** (max stacker edge 0.120 never clears floors -- it hugs morning consensus, verified signal not bug). ROI vehicle dead; Brier-only stacker stays shadow Stage 2.
- **LEGACY RE-GATES 2026-09-15 (all KEEP / THIN, no shift):** `legacy_regate.py` -> `legacy_regate_report.json` (taken n=2,077, same-subset): matchup-tier KEEP (blocked +5.5% vs passed +9.3%), long-rest gates KEEP, TBF<15 THIN-n (n=0 in taken), 2025 cell gate KEEP (drop-list -3.2% vs take-list +8.7%), deploy matrix KEEP. Cells: 3.5-under still -8.2% (n=42), 2.5-over -4.9% (n=18), 9.5 n=1 no read. Runnable runbook: `docs/reference/reports/regates_runbook_2026-09-15.md` (re-run commands + decision table: DROP-data-says + n>=100 = propose shift, else hold). Full suite 363 green.
- **DAWN PROBE 2026-09-15 (built + proven, owner minutes next):** `production/ops/dawn_probe.py` -- RG half (per-game confirmed/projected, starters, mins-to-tip, RG-vs-MLB agreement) + SharpAPI dry-run half (preview only, `--allow-live-open-poll`, never ledger/alerts); writes dated JSON + appends `dawn_probe_log.jsonl`; always exit 0. Smoke-proven same day: RG 15 games / 11 confirmed, books exit 0 with 33 matched DK+FD quotes (afternoon carriage proven; dawn still unmeasured). RG TIMING ANSWER: 7 historical RG-vs-MLB disagreements ARE logged with UTC stamps (incl. 9/11 PIT 696062 at 12:31 UTC); confirmed-vs-projected status was NEVER logged -- the "~8:30 ET stale" is owner observation + the PIT incident, not a measured series. The probe closes exactly that gap: run at dawn ~5 days, read the log, GO (DK+FD at -6h+ AND confirmed) builds the dawn poll, NO-GO gates day-game policy.
- **JUDGE HARNESS 2026-09-15 (owner: deferred to postseason):** not built; October item stays as filed.
- **BOOK-MISALIGNMENT NOTE 2026-09-15 (owner Q):** yes -- cap-3's 4th+ tickets are mostly books dealing different lines on the same pitcher (DK 4.5 vs FD 5.5). The worse-number duplicates lose; the best number wins. Same evidence as the DK+FD champion, seen from the other side: take the best number, never every number. Game-cap stays STAGED (owner has not said trash).
- **OPS SMALLS 2026-09-15 (built, live-safe):** (1) fills drumbeat: `run_end_of_day_settle.ps1` now prints `fills_analytics.py` (4/50 TOO THIN) every settle -- display only. (2) lineup-status logging: `log_projections.py` joins per-game `lineup_status` + `lineups_confirmed` from the RG slate (worst side wins; fail-open) -- next mornings measure the 8:30 staleness claim directly. (3) game-stack watch: `_flag_game_stacks` in `odds_board.py` + 2 tests -- `game_stack_flag=true` on 4+ BET games, recommendations/stakes byte-identical. Full suite 365 green.
- **ALT-LADDER 2026-09-15 (KILL both):** fresh `alt_curve_measure.py` re-run confirms skill negative at every covered line; new `alt_ladder_probe.py` -> `alt_ladder_report.json`: blind over-alt ROI negative everywhere with real n (best 9.5 +0.4% n=107); UNDER side has NO alt price at any line 0.5-14.5 (over-only book) -- under-ladders unpriceable, over-ladders nothing to test. Laddering reshapes variance, never adds edge; with under-lean + over-bleed it points the wrong way. Runbook Appendix A added. No live change.
- **9/15 RESULTS + CLV 2026-09-16 (owner Q, verified to boxscores):** laptop (money truth), deduped 3W-2L +$8.57 on $283.28 (+3.0%): Manaea U6.5 FD -142 WIN +$34.31 (6 Ks, 5.1 IP); Sandoval U5.5 FD -136 WIN +$33.38 (5 Ks, 2.2 IP); Soroka U4.5 DK+FD LOSS -2x (6 Ks over 4.5 both books — the doubled ticket game-cap would cut); Brown U5.5 DK LOSS -$52.87 (10-K blowup); Sanchez U6.5 DK WIN +$62.53 (5 Ks verified, but laptop ledger shows 0.0/0 IP — settle-data wrong, PnL accidentally right, repair proposed). Raw double-counted: -$59.35 on $351.21 (-16.9%). CLV: laptop has NO closes after 9/11 (local watcher alive but box asleep in windows; close_status null on all 9/15 rows) — measurable only via cloud closes: Manaea 0.00pp, Sanchez 0.00pp, Soroka/Sandoval/Brown no close anywhere. Day verdict: no edge vs close captured; wins were sweat (6/5 Ks on 6.5/5.5), losses were tail + doubled exposure.
- **MODAL DIAGNOSIS 2026-09-16 (owner: what's wrong):** nothing is DOWN — all 5 crons firing, heartbeat ok=true (sweeps q20min, hourly, settle 07:00Z, drift 09:30Z, morning 12:00Z today), app `mlb-props` deployed. Real problems, all silent: (1) cloud statcast refresh fails EVERY run (`FileNotFoundError` Savant 2023 base — raw never uploaded by design, incremental has nothing to append to) → cloud features frozen at 9/14 upload (rolling_max 9/13, stale_days growing); (2) `_beat(job, True)` is hardcoded True — heartbeat can never report a step failure; (3) image predates 9/14-eve→9/15 code (flat stakes, stale-gate, cell gate, game-stack flag — cloud 9/15 board ran Kelly sizing: Bachar O1.5 L -$88.01/CLV -22.9pp, Misiorowski U8.5 W +$36.54/CLV +2.8pp, Sanchez FD W +$59.14 — a DIFFERENT ticket set than laptop's Sandoval/Brown). Cloud is preview-only (NO_ALERT) so no money impact, but parallel-proof is apples-to-oranges until redeploy + statcast fix. Proposals: redeploy image; fix cloud statcast (upload Savant base OR laptop pushes processed features, cloud skips pull); heartbeat reflects step exit codes; backfill laptop closes 9/12+ from cloud ledger.
- **9/16 MORNING DEGRADED-BUT-COVERED 2026-09-16:** laptop statcast died (pybaseball RemoteException, exit 1) on 3-day-stale features; stale gate held both would-be BETs (Javier U4.5 edge 14.8%, Wheeler U6.5 edge 13.0% → HOLD `stale_data`), board posted 28 open rows + failure banner (ntfy 200). Cloud morning mirrored: stale_days=3, 0 BET / 6 HOLD. RISK line is the machinery working, not a missed board. NightlyDrift still unregistered (owner minutes).
- **FIXES SHIPPED 2026-09-16 (owner: fix everything, alert on done):** (1) Modal statcast: uploaded Savant 2015–2025 base to the volume (was 2026-only) — cloud incrementals work again; (2) heartbeat honesty: `_run_steps` captures exit codes, `_beat` carries ok+note+`IMAGE_VERSION=2026-09-16a`; (3) redeployed `mlb-props` (7s, green) — image now includes flat stakes, stale gate, cell gate, catch-up watch; proven live via one-shot `close_sweep` run → heartbeat `ok=true note="all steps exit 0" image=2026-09-16a`, which also filled 29 more cloud closes; (4) laptop closes backfilled: new `backfill_closes_from_cloud.py` (canonical `apply_close`, CLV recomputed from laptop bet prices, never copied) merged 28 closes (27 same-book) — 9/15 now 4/6 measured (Soroka both books honestly missing; cloud missed them too); (5) catch-up alert gap closed in `frozen_edge_watch.py`: baseless frame with live BETs + no success board today → pages catch-up board once; all watch sends respect `MLB_PROPS_NO_ALERT=1` (closed a latent cloud double-page hole); (6) Sanchez 9/15 settle repaired 0.0→5 Ks/6.1 IP (boxscore-verified, note-tagged, PnL invariant +$62.53); (7) dedupe audit: every production money surface (operator summary, self-check, runtime snapshot) already rides `dedupe_ledger_props` — the double-count was my table only, reports were clean. Full suite **373 green** (365 + 8 new). 9/15 final CLV (deduped): Manaea 0.00 / Sandoval +0.15 / Sanchez 0.00 / Brown 0.00 / Soroka n/a → +0.04pp measured. Uncommitted batch grows (owner pushes). Next cron runs prove the cloud statcast end-to-end (tomorrow 8:00 ET board should show rolling_max 9/15+ and honest notes).
- **STANDBY CUTOVER 2026-09-16 (owner order):** cloud is now PRIMARY. Removed `MLB_PROPS_NO_ALERT` from Modal morning+hourly, bumped `IMAGE_VERSION=2026-09-16b`, redeployed (green), then disabled all 7 laptop tasks (`schtasks /Change /Disable` — definitions kept, re-enable in 2 min; NightlyDrift was never registered, cloud drift covers it). Page path proven from the container: manual cloud hourly run → real ntfy send `ntfy_status=200` ("No BET" board, expected) + heartbeat `image=2026-09-16b, all steps exit 0`. Repo + data stay as deploy source and fallback — Modal builds its image FROM local `src/`+`production/`, so the files can never be deleted. Ledger fork flagged for cutover: laptop 1,822 rows vs cloud 1,868 (cloud holds never-alerted preview stakes, e.g. Bachar/Misiorowski 9/15 — paper history must not absorb them unreviewed). First live proof: tomorrow 8am ET cloud morning (fresh statcast pull on the uploaded base).
- **Q&A + RE-SIM SCOPE 2026-09-16 (owner Qs):** stale gate = `_apply_stale_data_hold` (cap 1d on slate−rolling_max, BET→HOLD `stale_data`, math untouched); intraday lineup movement verified (9/16 morning 4 RG-vs-MLB disagreements/32 rows → 1pm 0/30, 8 lineups confirmed); Savant local 2026 max game 9/13 (3d stale = the weekend gap); BET persistence = `replace_open_slate` keeps staked + closed rows, drops only unstaked/closeless/unstarted opens (Soroka FD 16:01 was a new book-key row, not a dupe); metrics pinned (ROI = realized pnl/stake; xROI = mean taken edge, `xroi_model`, close-ref variant primary; dollar-EV = edge×stake, no separate EV column; CLV = realized same-book p-diff fraction scale; xCLV = retired fair-harness expectation, never a live input). Owner ordered full-2026 fresh-track re-sim — scoped, awaiting vintage/quotes choice.
- **CHAMPION 2026 FULL GRADE 2026-09-16 (owner: beyond PnL):** new `grade_champion_2026.py` reuses the SELECTOR's exact taken set (judge-identical n=485 — caught a ledger-sidecar impostor at n=844 with a looser take rule, deleted its artifacts). 2026 champion: ROI +15.6% / xROI 0.1645 (gap +0.008 — model expected 16.5%, got 15.6%) / CLV +1.30pp on 485-of-485 / **beat_rate 0.606** / WR 0.60. Unders 468/485 (+15.8%); opens out-CLV mornings 1.67 vs 0.60 (timing thesis again); CLV positive EVERY month incl. red June (−1.7% ROI) — execution holds while selection varies. Report: `champion_2026_grade.json`. Spec deltas filed: devig = multiplicative only (no Shin anywhere — correct for 2-way props); rolling calibration refit REFUSED (freeze doctrine — needs explicit order); xROI unweighted = stake-weighted under flat $50; xCLV stays unbuilt. Full suite **376 green**.
- **GRADE-VERIFY + DRIFT-PAGE + BOOK-FILTER FIX 2026-09-16 (owner: deeper dive):** (1) 844-vs-485 root-caused: `_two_way_paid` re-read the parquet AFTER applying `allowed_books`, silently discarding the dkfd restriction — 353 of 359 extra rows were BetRivers/BetMGM/BetOnline morning orphans. Fixed (deleted shadow re-read) + regression test (dkfd pools ⊆ {DK,FD}, unfiltered still sees BR). Canonical runs (allowed=None) were never affected. (2) Champion grade independently verified: exact-set PnL recomputes to the cent ($3,791.75); no dupes; monthly sums reconcile; the 14-row hand-rule gap = 2.5/3.5-overs needing 0.22 (probation max THEN lean premium — order matters). (3) WS1c = per-line Platt shrinkage of raw p toward empirical hit rates (Brier wins per line, still trails book); rolling refit scoped as October shadow via `promote_ws1c --no-swap`, gated on Brier-skill + ROI-neutral, live swap needs explicit order. (4) Daily grading live: `send_daily_grading.py` (settled+staked+deduped, THIN/CALIB/EXEC flags, trailing-200 beat rule) + tests; folded into Modal drift (5-cron free limit — no 6th function); proven with a live 9/15 page (ntfy 200). (5) Honest heartbeat caught 2 real faults same day: schtasks crash on Linux → self-check now cloud-aware; drift RED (cloud features 9d stale, pre-upload state, self-heals tomorrow) → drift now pages RED via `--page-on-red` (opt-in, laptop path unchanged). Live image 2026-09-16d. Full suite **386 green**.
- **4-WINDOW GRADE + ENSEMBLE CLV 2026-09-16 (owner: yesterday/7d/30d/YTD + EV):** daily grading now prints all four windows with EV dollars + same-book CLV/beat + consensus CLV per window (tests updated). Beat stats independently verified: 294/485, exact binomial p=1.68e-6, Wilson CI 56.2–64.9%. Ensemble-close CLV wired per owner (opens on DK/FD, grading vs 9-book devigged median, 2+ required): replay champion 2026 = +1.52pp / beat 0.69 on 456 (94% cover) — stronger than same-book +1.30/0.61, coherent (DK/FD move less than the offshore-inclusive average). Live YTD: same-book +0.73pp vs consensus +0.75pp (202 rows) — two independent close sources agree. Live image 2026-09-16e. Full suite **387 green**. Borrowed-claim corrections filed: Brier skill is vs the book (not base rate); Kelly already sizes on calibrated p and live is flat $50.
- **ECE PLAN 2026-09-16 (owner: build it out):** E0 DONE (selection sidesteps, 1/16+flat prices the error, per-line Platt live). E1 MEASURE ($0 replay, next): per-snap calibration split (open vs morning ECE/Brier on taken) + prob-curve refresh with September — kill E2 if no snap gap. E2 SHADOW (October): trailing-1000 monthly Platt via `promote_ws1c --no-swap`; gates = Brier-skill gain + ROI-neutral on frozen replay + taken-ECE cut; any gate fails = dead. E3 PROMOTE (E2 pass + explicit order only): versioned bundle, pointer backup, 2-week parallel watch, rollback ready. E4 OFFSEASON (the actual bind): k-rate signal features (Glicko R retrain) — maps can't fix missing signal. DON'T: live mid-season refits, refits on <500 outcomes, floor edits for calibration. Kelly verdict (owner Q): NO relook — nested bakeoff stands (flat Sharpe 2.79 vs Kelly 2.59, DD 10.8 vs 12.1); reopen only on fills n≥20 tail damage or DD breach.
- **4-WINDOW GRADE v2 2026-09-16 (owner: PnL + consensus books):** every window now prints PnL; consensus = named 9-book devig-median (DK/FD/MGM/Bovada/BetOnline/Fanatics/BR/WH/MyBookie) with median book depth per window (live depth d6 — healthy). n=200 answer: YTD 330 settled+staked+deduped, 200 with paid-consensus closes (130 missing = September closeout gap; beat/cons read over measured only). Live image 2026-09-16f. Full suite re-run below.
- **CLOSEOUT + E1 2026-09-16 (owner: pull closes + all markets):** 6-day closeout (9/10–9/15) all 15 markets close+morning for ~20k credits (remaining 3,694,690, floor 100k): pitcher lake 1.66M rows, batter lake 11.03M (batter suite banked for 2027). Caught+fixed 2 pipeline gaps: (1) event_date_map.json only builds when missing — new event IDs never entered it (map refreshed, cache 70,337→71,094); (2) ET/UTC join miss — ledger game_date is ET, consensus gd is UTC commence, so ET-evening games never matched (utc_gd fallback in attach + order/row-stability preserved + test). (3) Nickname gap: Mike→Michael Soroka (+2 rows, 9/15 now 6/6 paid-close), Daniel Lynch suffix (+3) via exact-match PLAYER_ALIASES in sorted_key + tests. Paid-close all-time: n=503+ of settled+staked. E1 RESULT: champion-taken ECE 0.086 overall; opens 0.086/Brier 0.248/CLV +1.67 vs mornings 0.118/0.253/+0.60 — snap gap EXISTS, E2 survives kill rule (morning-specific humility is the direction). Remaining honest gaps: 25 players (41 rows) with no paid turn at all (relievers/prospects — books don't deal them; Cantillo n=7 leads). Live image 2026-09-16g. Full suite **393 green**.
- **COUNT BUG + 2026 PAPER + FUZZY PORT 2026-09-16 (owner: n=200? paper season? RG fuzzy?):** (1) Daily grade UNDERCOUNTED (YTD 330 vs true 366): dedupe-before-stake kept highest-edge rows even when unstaked-skips, dropping 36 real BETs — fixed to stake-first (operator summary already had the right order; no historical money number changes). True YTD: 366 settled+staked+deduped (Jul 22 / Aug 264 / Sep 80), paid-consensus on 338 (92%), same-book CLV on 217. (2) 2026 paper season: Jul +$16 (+0.9%) / Aug +$74 (+0.4%, WR 0.49, xROI 0.170 — the gap month) / Sep +$409 (+7.0%) / season +$499 (+1.9%, WR 0.50, xROI 0.166, EV $4,994 vs realized $499 — the 10x calibration gap in dollars). (3) Methodology chain verified from docs: prereg (36-family + LCB + CLV≥0 + ≤40% cell + DKFD-sign, 2026 disclosed-peek) → calibration raw .2495 / isotonic .2455 / Platt .2421 / book .2354 (Platt shipped) → 2026 judge graded exact (485, PnL to the cent). (4) RG fuzzy ported to joins: `suggest_aliases` (nickname groups + suffix rule, uniqueness-gated, measure-only) run over all 41 miss rows — zero false merges (ambiguities like Arrighetti/Schwellenbach correctly rejected); human-verified entries only in PLAYER_ALIASES. Live image 2026-09-16h. Full suite **396 green**.
- **GRADE Q&A + MONTHLY QUANT 2026-09-17 (owner: ROI vs xROI? d6? cons? Sharpe by month?):** format already comma-spaced (`n=, PnL=, EV=,` + `CLV= (n=)` spacing, live image 2026-09-16h dry-run verified). ROI=x realized pnl/stake; xROI=mean taken edge (fair-price upper bound); gap Aug 0.170→0.004 / season 0.166→0.019 = the 10x ECE calibration gap, not a count bug. d6 = median 6 books behind the 9-book devig-median consensus (healthy). cons = consensus CLV vs market avg; same-book CLV = fillable execution. Monthly quant (settled+staked+deduped, $50u, sqrt162): Jul n=22 Sharpe 0.38 / Sortino 0.55 / maxDD 5.3u / Calmar 0.06; Aug n=264 0.16 / 0.25 / 17.6u / 0.08 (gap month); Sep n=80 3.35 / 5.83 / 5.6u / 1.47 (10d, thin); Season 0.77 / 1.24 / 17.6u / 0.57; trailing-30d Sharpe 0.71 vs season 0.77 (decay −0.06). Live YTD mixes regimes (Jul/Aug pre-champion, Sep champion) — pure-policy read is champion replay, NOT live +1.9%. Next builds per October program: monthly summary table + equity/DD graphs + paper §8 appendix.
- **MONTHLY SUMMARY + FIG11 2026-09-17 (owner: build it, verify, report):** DONE. `monthly_quant.py` re-ran (live + champion tables); new `fig11_live_equity` in `make_figures.py` (datetime x-axis — first draft had a categorical-string axis bug producing horizontal spaghetti, caught on eyeball + fixed; second catch: `$..$` mathtext ate the legend dollars, escaped). Report: `docs/reference/reports/monthly_summary_2026-09-17.md`. Tests: monthly_quant + daily_grading + grade_champion 12 green. Findings: (1) August live gap is regime, not policy — champion replay Aug +11.8%; (2) September diverges opposite (live +7.0% n=80 vs champion -9.2% n=26, both thin — watch); (3) CHAMPION REVISED DOWN: disk truth n=460 / +8.7% / +$2,012 / WR 0.574 / 2026-judge LCB +0.16% (three artifacts agree) supersedes backlog's n=485 / +15.6% / +$3,792; 2025 selection n=505 / +11.0% / LCB +3.0% (was cited n=558 / +15.5% / +8.0%). Cause: candidate rebuild post-9/16 book-filter fix + September closeout. Edge thinner than claimed — humility stands. (4) No Sharpe decay (expanding Sharpe_pb stable). Negative Sharpe_pb on green live months = equal-weight vs stake-weight (Kelly-era stakes larger on winners), labeled in report. Live negativity intact: July mean(r) -5.1% vs ROI +0.9% verified row-level (Skenes $189 W vs Lowder $106 L).
- **TABLE 6 + PAPER A.8 2026-09-17 (owner: month-by-month 2026, no lies, what's cons?):** DONE + VERIFIED. `monthly_quant.py` extended (WR, CLV+beat with correct per-track scales, Calmar, active days, bets/day) and re-ran; `monthly_quant` + `test_monthly_quant` green. Paper gains App. A.8 (Table 6: requested columns for live + champion; Fig. 10 = fig11 equity with regime labels Jul/Aug pre-champion → Sep transition); HTML + PDF rebuilt (1257.4 KB) and verified (Table 6 + fig11 in HTML). Reconciliation to the cent: live 366/+$499 vs daily-grading YTD; champion 460/+$2,012 vs grade file; champion Apr CLV reproduces exactly. Manuscript's 9/17 staleness disclosure already covers the +15.5%→+11.0% revision — no conflict. Report addendum filed in `monthly_summary_2026-09-17.md`. xROI dropped from the table per owner (diagnostic footnote only); cons explained once more in chat (below).
- **A/B + CI + FIG12 2026-09-17 (owner: champion-only, A/B? CIs? pro paper? graphs?):** DONE. New `ab_policy_audit.py` (+ 4 tests green): 5 legs on the IDENTICAL 2026 set at identical $50 stakes (status_quo / cap_only / lean_only / dkfd_only / champion; cap-off = 9.0 through the same refuse path, no selector change). Result: status_quo n=1195 +$6,234 +10.4% CI [+2.9%,+17.2%] vs champion n=460 +$2,012 +8.8% CI [+0.5%,+16.6%] — CIs overlap fully, no leg separates; refused 735 tickets earned +$4,222 (+11.5%): the 2025-selected filters cut WINNERS on 2026. Champion = max-LCB choice, not max-return — stated in paper. Monthly CIs clear zero only Mar-thin + May. New `fig12_ab` (monthly ROI whiskers + A/B equity) verified by eyeball; paper A.8 gains A/B paragraph + Fig. 11 + provenance row; HTML + PDF rebuilt (1440.9 KB) and verified. YTD champion row: 460 / +$2,012 / +8.7% / +1.14pp / 60% / 0.092 / 0.134 / 12.96u / 3.10 / 3.05-per-day (151 days).
- **BAND-OPTIMIZATION REFUSAL 2026-09-17 (owner: optimize floors/lines/sides per band for ROI/PnL/Sharpe):** REFUSED on peeked data (freeze doctrine; would be snooping). Repo ladder stands: LCB selects, CLV confirms, ROI scoreboards, Sharpe/DD govern survival — maxima are never selection criteria. Compliant path offered: preregister the band grid NOW (bands × floors, LCB + n≥200/cell), execute ONCE on fresh post-9/27 data. Eval surface locked to owner's 11 columns, xROI out.
- **OPS + DOCS 2026-09-17 (owner: docs for October, Modal health, grades):** (1) Pickup runbook `docs/reference/october_pickup_checklist.md` written (trigger-gated, ordered, kill rules, artifact map; archive after execution); backlog October line points at it. (2) Zero-bet forensics: 9/16 held by stale-data gate (Javier 14.8%, Wheeler 13.0% → HOLD); 9/17 top edge Dotel 0.428 refused by edge-cap, next six by deploy-matrix/veto, rest below floors — policy working, thin slate. REVISITED same day (see STALE-GATE repeal below): both held tickets WON (Javier 2Ks, Wheeler 5Ks) — stated as fact, not as justification (process over tickets).
- **RESILIENCE 2026-09-17 (owner: bets-but-no-recommendation scare + never-fail architecture):** verified NO missing BETs anywhere (0 staked both ledgers 9/16–9/17, 0 real fills, ntfy 200s flowing; latest page "No BET" is the true board). Built anyway: (1) `_send_ntfy` retries 3× (2s/4s) + 3 tests — a single blip can never eat a page; (2) cloud README gains manual-rerun commands (`modal run …::morning_workflow|hourly_refresh|end_of_day_settle`, with duplicate-page warning) + failure-workaround table + one-line heartbeat check. OPEN owner Q: where were the "bets this morning" seen (ntfy text vs ledger rows vs Novig vs 9/16 would-bes)? Nothing in any artifact matches. IMAGE_VERSION 2026-09-17d redeployed (3.3s).
- **STALE-GATE REPEAL 2026-09-17 (owner order: kill the no-bet-on-stale rule):** `_apply_stale_data_hold` → `_tag_stale_data` (BETs stand, carry `stale_data` tag for the ledger record; math untouched). New shared healer `production/ops/heal_stale_slate.py` (stale>1d → one trailing re-pull + features + re-log, always exit 0, never blocks board) wired into laptop morning ps1 (replaces >3d inline block that missed exactly-3d on 9/16) + Modal morning. Live-fired on laptop: 4d stale → HEAL_OK → 1d fresh. Consequence owned: when the vendor is down AND heal fails, we now bet on stale numbers with the tag on. Tests rewritten + added (tag-never-holds, heal threshold/matrix); 48 green (board+drift). IMAGE_VERSION 2026-09-17c redeployed (3.8s). Tomorrow's board is the first live proof. (3) Upstream empty-board cause: `write_recommendations` used raw `frame.write_parquet` (missed in 9/08 atomic hardening) → torn reads possible; switched to `atomic_write_parquet` + round-trip test; IMAGE_VERSION 2026-09-17b redeployed (3.1s). 47 tests green (board+drift). (2) Modal health from heartbeat+logs: all 5 crons firing, image 2026-09-16i; TWO faults — drift CRASHED 05:30 ET (0-row recommendations.parquet → ColumnNotFoundError, exit 3, no report; root-caused from container logs) and 10:00 ET hourly board failed on SharpAPI 502s (vendor restarting, self-healed 11:00). Fixed drift with never-raises `veto_leak_check` (missing/unreadable/empty → YELLOW) + 4 tests (incl. exact crash-signature repro); 11 green; IMAGE_VERSION 2026-09-17a redeployed (4.3s, live for all future runs; next heartbeat proves it). (3) Yesterday 9/16: true no-bet day (0 BET both ledgers, $0; grade NO_SETTLED). Today 9/17: cloud board 16 rows, 0 BET; hourly watch to 22:00 ET. (4) LAPTOP CLOCK ~12h slow (says 00:02 ET, cloud cron evidence says ~12:00 ET) — cosmetic locally, cloud unaffected; owner should let it NTP-sync.
- **EXEC TABLE 2026-09-17 (owner Q&A → build):** quant Sharpe is strategy-level on a time clock, never per-trade — headlining daily-PnL Sharpe × √162 (golden convention): YTD **2.13** / Sortino 3.67 on 151 days (`sharpe_d`/`sortino_d` added to `monthly_quant.py` + json; tests green). External critique reviewed 2026-09-17: ADOPTED seasonal-Sharpe labeling + capacity bound (personal-bankroll until fills log size) + concentration disclosure (Mar 19.6% of PnL; ex-March +7.2%/448/Sharpe 1.76 as sensitivity); REJECTED leading with trimmed number (reverse cherry-pick). PDF rebuilt (1446.5 KB), verified.
- **MARCH VERDICT 2026-09-17 (owner: keep March, how did it win, rolling data?):** March STAYS (never dropped). Forensics: 10-2, all DK, 10 unders, 8 off opens, six plus-money cashes — opening-week quick hooks + opener shading, baseball-plausible + small-n luck. Rolling data CORRECT as-of (current start + same-date excluded, prior-season shrinkage, season reset; no leakage). `{name}_std` expanding columns ARE in the model (features allow-list) — legal, caveat is early-season prior-dominance, not correctness. False alarm cleared + documented: under-`edge` looked inconsistent until `evaluate_side` read — `p_ours_cal` is over-basis, `p_mkt` taken-side; NO bug; basis asymmetry now pinned in A.6 contract.
- **UNITS + KELLY 2026-09-17 (owner orders):** whole manuscript $→u (38 asserted pairs; sole remaining $ is the 1u=$50 numeraire): Tables 3/3b/3c/6, harness lanes (+2,924u/+961u), vignette, A/B (+124.7u vs +40.2u), exec read, capacity (460u). Figs 10/11/12 regenerated in u + eyeballed. Kelly display-only mini-table added (same 460: +27.9u/+6.9% on 405u vs flat +40.2u/+8.7%; Mar Kelly +91.6% on 11u = thin exhibit ×2); `champion_kelly` block in monthly_quant.json; flat stays rule, sizing reopens on fills≥20 only. PDF rebuilt (1440.9 KB), HTML verified (Kelly/Table7/A6 present, no stray $).
- **MD CONSOLIDATION 2026-09-17 (owner: full):** 31→20 reports (14 merged content-intact into 3 packs: hold-inventory, ssac-audit, postfreeze; origin markers kept; git history retains originals). All citations repointed (backlog/CLEANUP/manuscript/README/passthrough + internal cross-links); zero dangling refs verified by grep. Forward diary archive untouched.
- **CALIB PROBES 2026-09-17 (owner: both):** `highconf_spotcheck.py` → top band p≥0.70 (n=177): pred 0.734 vs emp 0.593, gap +0.141, ROI +0.9% with CLV +0.81/60% — overconfident at top yet flat on money: independent support for the cap; filed, no live action. `calib_drift_sim.py` → taken-ECE doubles nowhere within ±0.05 shifts (1.54× at +0.05): monitors own this range. 5 tests green. No live surface touched. Paper A.8 gains exec read (alpha over books: +8.7% realized vs ~52.4% breakeven, 57.4% printed; CLV as execution alpha) + Table 7 (monthly strategy clock, Mar 31.55 exhibited grey as the thin-month warning, Sep partial); per-bet 0.092 footnote-only. HTML + PDF rebuilt (1446.4 KB) and verified.
- **BAND-GRID-V2 BUILT 2026-09-17 (owner: build everything possible now):** DONE without touching peeked data. Prereg `docs/reference/reports/band_grid_v2_prereg_2026-09-17.md` (80 configs: floors uniform{.08,.10,.12,.14}+file-map × caps{.18,.20,.24,none} × sides{both,lean} × books{next,dkfd}; strict superset of old 36 + no-cap; same LCB/CLV/conc/dkfd-sign/White gates). Executor `band_grid_v2.py` with MECHANICAL peek-gate (refuses select-from < 2026-09-28 without --allow-peeked; default run = valid empty selection, proven live). take_v2 ≡ selector row-for-row on 3 overlapping configs (test). 6 tests green. SUPERSEDES the "36+no-cap October" plan. Live untouched; first real execution post-closeout, winner binding.
- **2025 MIRROR 2026-09-17 (owner: why change? overfit? worse?):** status_quo-no-cap was NEVER in the 36-family — computed fresh on 2025: n=2015, ROI +3.3%, LCB **−2.8%** (fails worst-case bar). Family cap analogs +5–6% ROI, all LCB<0. Champion: +11.0%, LCB +3.0% — beat status quo on BOTH ROI (+7.7pp) and LCB (+5.8pp) in-sample. Change fully justified on 2025. On 2026 the gaps shrank to noise (+1.6pp ROI, overlapping CIs) = textbook shrinkage, not error. Overfit direction: status quo CANNOT be selection-overfit (never selected anywhere); the 2025-selected champion carries the winner's-curse risk, guarded by LCB/White, holding weakly (2026 LCB +0.16%). 2026 peeked for BOTH legs equally. DECISION (binding): no live switch on this audit; October re-judge on fresh rest-of-September closes decides — same 36-family PLUS no-cap arm, same LCB rule + constraints. No-cap blind spot filed. Live stays champion (continuity + fills).
- **STATUS-QUO AUDIT + XROI PURGE 2026-09-17 (owner: calculated wrong? regress? no xROI):** NOT wrong, no regression. (1) Candidates are 1 row/prop (6,138 uniq = 6,138 rows; book pre-assigned DK→FD→next) — no double-count; status_quo economics recompute to the cent ($6,233.72); champion ⊆ status_quo with refused math exact (735 / +$4,222 / +11.5%). (2) n gap vs juiced canonical (1,195 vs 917) is RULE, not bug: A/B uses uniform 0.12 so only cap/lean/books differ (fair test); juiced uses stricter per-line file floors (2.5→0.20 … 9.5→0.16). Caveat filed: uniform-0.12 is slightly looser than live pre-champion rule — biases mildly toward status_quo, disclosed. (3) 2026 confirmatory drift (+12.6%/769 → +9.5%/917) is data growth (September closeout + ET/UTC + nickname recoveries; September itself -$49), not code change. (4) xROI purged from the notebook monthly sim (code + markdown); ab report / Table 6 / Fig 12 never had it. Sim re-ran green.
- **NOTEBOOK GC 2026-09-17 (owner spec, executed to completion):** 9→8 notebooks. DELETED `results_gate_policy.ipynb` (simulator + sweep + risk cells merged into bettable_cohort as frozen-policy audit; regime/decile cells dropped as graded-pipeline duplicates). Kill-list audit: NO xCLV / p_model_raw / 844-sidecar references exist in any notebook source (sole xCLV grep hit was base64 PNG noise). Canonical money track: 5 notebooks switched `keep_best_available_lines` → `dedupe_ledger_props` (dashboard/pnl_clv/cohort/rec_audit/kpi_monitor). CLV now pp everywhere: central `side_clv_roi_health` ×100 + slippage/slice/window means fixed. Stale floor killed: dashboard reads base 0.12 from `kpi_policy.json` (was hardcoded 0.08 via DEFAULT_EDGE_FLOOR). Dead code: `df.is_empty` bug (cohort kpi), dup numpy imports, unused hashlib/imports, hardcoded C_over14_under10 variant removed. Wired in: 2025 policy explainer (cohort, 1,576 banked takes: side/book/snap) + 2026 monthly sim (pnl_clv, grade-file cadence). Date axes thinned to ≤10 ticks (MaxNLocator) in pnl_clv; process markers gain champion + flat eras. Refs updated (notebooks README, production README/INDEX/RUNBOOK, daily_projections + dashboard headers, `run_daily_operator_flow.ps1` -IncludeGatePolicy removed — no scheduler used it). VERIFIED: all 8 execute top-to-bottom via nbconvert, outputs to temp (repo notebooks untouched by runs). Two real bugs caught by execution: dashboard §18 null-tier crash (qcut edge rows → matplotlib TypeError, fixed with null-tier drop) + dashboard §14b false SyntaxError alarm (checker stripped indented %-line; file was balanced — no change needed). Side effect by design: dashboard run re-froze next_50_checkpoint.json to today + refreshed sweep/scorecard artifacts. Spec Q answered: no notebook had unclear purpose — nothing held for questions.
- **FULL-SEASON RESIM + PAPER REFRESH 2026-09-17 (owner: March-to-now, quant tables, graphs):** rebuilt scores (L3 full build; 9/08–9/13 scored) → universe panel (72,304 rows) → canonical candidates → selector re-judge. STALENESS FINDING: the 9/11 panel lagged the frozen scores (pre-WS1c values); faithful champion = SAME config, smaller margin: 2025 +11.0% LCB +3.0% (n=505), judge +8.8% LCB +0.2% (n=460), White p=0.0075. September replay thin+red (n=26, −9.2%). monthly_quant.py (per-bet Sharpe/Sortino/DD + expanding decay + tests): replay expanding Sharpe 0.81→0.09; live August proves sizing worked (unweighted −4.6% vs stake-weighted +0.4%). monthly_prob_curves --champion + fig10/fig6/fig4 regen on faithful data. Manuscript v2026-09-17: Table 3/3b/5, verdict row, White text, abstract, resume, new Table 3c + staleness disclosure; HTML+PDFs rebuilt and byte-verified (only intentional disclosure hits remain). ROI−xROI gap decomposed: August ran pre-champion policy (BR-soaked) + calibration overconfidence + September thin. Alert format revamped (commas, depth= label, cons legend). Live image 2026-09-16i. Full suite re-run below.
STANDING 2026-09-14 (owner directive):** respond as a professional quant researcher/trader (institutional bar): deep, metric-grounded, alpha-focused; critique honestly against trading/modeling/backtesting practice; propose detailed plans the owner picks apart. Recorded here so it survives session rolls.
- **REVIEW 2026-09-14 (owner: full review — done well / fix / steady?):** DONE WELL: kill culture with gates (Glicko/Kalshi filed same-day), vendor timestamps, rejected retention, White-lite, boxscore-outranks-ledger, dedupe audit (1,746→1,011, no double-count), CI green 7 straight. STEADY: all 8 tasks Ready; ledger current thru 9/14; diagrams + README champion-accurate; self-check RISK is a stale morning JSON (9/11 degraded run), not a live fault. FIXED: LCB numbers, handoff live-state, paper Table 3 + resume + golden to post-repair values, intro nine-batter false gate → TBF≥15 wording, Table 3b (2026 season surface) added, PDFs rebuilt. CLEANUP: no orphan scripts/tmp in prod or artifacts; WS1c backup pair kept (revert paths); sidecar parquets kept (evidence). MOVES/RENAMES: none needed. CONSOLIDATION: notebooks stay (23, stale outputs; panel-aware work deferred, not deleted); market_research scripts stay 1-script-1-report. PAPER: v2026-09-14, scope rule enforced (general/aggregated only, vignette labeled + outcome stated).
- **TONG 2026-09-14 (owner: NOT my bet — struck):** discussed as a spot (35% whiff, BAL matchup), never fired — removed from real_bets + entry annotated against re-adding. Real ledger: 1W-2L + Mathews pending. Conflict filed: our board had Tong 5.5 UNDER (edge 0.077, below floor, not a BET) vs owner over-lean on stuff+matchup; September judges. NFL note ignored per owner (later correction).
- **DOCS CONSOLIDATION 2026-09-14 (owner Q):** decision = NO new files, NO archive rewrite. Backlog stays the single master (labels fixed this turn: stale waiting items closed, FUTURE #2 retired, TBF/exposure states corrected). Dated reports stay where they are (they're evidence, not a queue). Hierarchy verified intact.
- **DOC CHALLENGE 2026-09-14 (owner: docs may be wrong):** real catch — paper + backlog cited champion LCB +7.4%/+7.7%, disk says **+8.0%/+7.7%** (`select_2025_report.json` has only one LCB field; +7.4% was stale from an earlier run). Fixed in manuscript (abstract/Table 3/§), resume, backlog; HTML/PDF/resume-PDF re-rendered (manuscript 1152.8 KB). Handoff stale blocks fixed: cap-0.20 + refusal described as live → corrected to champion (0.24/refusal reverted). Selection basis confirmed from disk: max LCB_95(ROI) under CLV≥0 + cell≤0.40 + DK+FD-sign + n≥200 (runner-up cap-0.18 had HIGHER ROI +16.8% but lower LCB +6.9% — why LCB, not max-ROI). Live == champion verified key-by-key (cap 0.24, lean 0.04, DK+FD, veto).
- **WAKE AUTO-RUN 2026-09-14 (staged, needs elevated setup):** `setup_automation_tasks.ps1` gains `New-Or-UpdateLogonTask` + `MLBProps_WakeRecovery` at logon +5min (parse OK). Direct registration failed Access denied (ONLOGON needs elevation) — activates on owner's next elevated setup run, batched with NightlyDrift. Deliberately unwatched by self-check (no Last Result pre-first-logon). Caught+fixed mid-build: (1) transient bad edit duplicating a function header — reverted, zero diff confirmed; (2) schtasks DELAY must be `0005:00` not `0005`.
- **DISK-FULL + PHANTOM REPAIR 2026-09-14 (pipeline saved):** 20:00 hourly died (projections + board exit 1) + double-ntfy reported. Root causes, both fixed: (1) C: hit 0 free → `projections.parquet` truncated to 0 bytes (atomic rename of empty content); freed 220MB via pip cache purge (regenerable; qwt/qcarm/qc206 trees untouched — owner's disk call); rebuilt projections from scratch (today's slate, 20 rows; history preserved in graded.parquet). Lesson: atomic writes don't survive a full disk — monitor free space, not just exit codes. (2) Double-send: watch pages flips + refresh re-pinged full board — refresh is now failure-BANNER ONLY (watch owns flips; morning owns the board). Slate-final idea considered and REJECTED as dead plumbing; instead the WATCH suppresses flips on started games (fail-open on missing clock). Ntfy quirks owned: failure-banner path misused for a status note once; PS eats `$` in double-quoted messages (single-quote henceforth).
- **PREVIOUS-PROMPT CLOSEOUT 2026-09-14 (owner: what was done?):** Tong fix attempt failed mid-write (Errno 28) and truncated the backlog to 0 bytes — restored from git (all prior commits intact; only the in-flight edit was lost, re-applied). Modal hourly/sweeps were deployed but schedules unproven until firing. Outstanding from that prompt, all now closed: Tong struck, backlog restored, projections rebuilt, double-send fixed, slate-final answered via watch-side filter.
- **FUTURE PLAN (ranked, next session picks from the top):**
  1. Remainder-of-season judging (automatic — boards log under the champion; no code).
  2. ~~Statcast blank-error logging fix~~ DONE 2026-09-14 (root cause: string throws).
  3. Metric canonicalization (4× Sharpe / 6× Brier dupe blocks).
  4. NightlyDrift registration + closeout cadence through 9/27 (owner minutes).
  5. October program as filed (slate caps → full-season judge → stacker ROI → legacy re-gates).
  6. Paper: 8-page workshop cut-down from the tech report; Fig 7 schematic-honesty clause; LaTeX/BibTeX only if submitting (needs toolchain).
  7. Cloud cutover post-season (7 green days before laptop retires).
  Standing: free-only, no retrains, no 2026 re-searches, fills logged manually if at all.
- **PUSH 2026-09-11 II (explicit owner order):** 4 family commits, remote current: champion promotion (`5ca49a7`) → selection engine (`4ce941e`) → paper rewrite (`eb19fa9`) → repo second pass (`8b8c630`). Hooks green on all four. Tree clean. Reproducible paths live in `freeze_playbook_2026-09-11.md` — any future edit narrows to one file, one test, one pin, one board-fire.
- **PUSH 2026-09-11 (explicit owner order, overrides never-push default):** 5 family commits pushed, remote current: live-policy cap/clip (`650c718`) → research replay era (`36a0a78`) → docs+playbook+prereg (`642f2d6`) → ops hardening (`5d08f21`) → notebooks refresh (`df13879`). Pre-commit hooks green on all five. Freeze signatures complete except NightlyDrift register + fills habit (owner minutes).
- **REFUSAL REVERT 2026-09-11 (same-day, measured):** Cap+refusal on the lake keeps 457/2,077 (+11.1% ROI) but WR collapses to 0.36, DK+FD starves 982→16, and 2025 goes +3.8% (below base +4.2%). Split attribution: cap-only 2025 **+8.0%** (n=912, WR 0.53, DK+FD 673 at +14.5%) — the keeper; refusal-only 2025 **−0.2%** (n=618, WR 0.35) — poison (longshot concentration, refused set still +6.2% at WR 0.55). `robust_shrink_refusal` key deleted from `kpi_policy.json` (code retained fail-open for the October dimension); scorer takes `kpi_policy_path` override (also serves the 2025-lock engine); pin asserts refusal OFF; 336 green. Live = floors + veto + probation + cap 0.20 + clip 0.02. Flat still beats Kelly on every set (+7.3 vs +6.6; +11.1 vs +10.1); live stays 1/16-Kelly (humility under ECE 0.10, no change without fills). 2.5-over is now de-facto banned (floor 0.20 meets cap 0.20 — documented intentional; n=18, −4.9%); 3.5-over window [0.18, 0.20) survives. Playbook: `docs/reference/reports/freeze_playbook_2026-09-11.md`.
- **FREEZE STATE 2026-09-11 (production ready to re-freeze):** Live stack = frozen k-rate/TBF + Poisson + WS1c + floors/probation/veto + edge_cap 0.20 + offset_cap 0.02 + 1/16-Kelly + postseason HOLD (refusal reverted same day — see above). Verified: 336 tests green, pin green (updated same commit), board rerun behaves as designed, weekly pack + stacker gate ran today, closeout current (9/10 cost 0), settle has nothing (53 opens are today's games). To complete the freeze: owner `git push` (tree ahead since #71), register NightlyDrift, keep the fills habit. Next coding job when named: ledger `--floors/--cap` overrides for the 2025-lock selection.
- **ARM VERDICTS 2026-09-11 (owner):** Stacker = HOLD (market-following compresses edge/CLV; ROI effect unmeasured — tying book Brier is not profit). Offset cap 0.02 = SHIP ("whatever helps long run") — LIVE same turn (`_clip_offset` in scorer, `offset_cap` key, silent shaping visible via offset columns; full suite **336 green**; pin updated). Brake ×0.5 = PARKED (owner: stay confident, follow the edge; no auto-derisk without a separate order).
- **EXECUTION 2026-09-11 (decisions → done):** Edge cap + robust refusal LIVE (`kpi_policy` edge_cap 0.20; `odds_board.py` helpers + score/gate wiring; 8 new tests; veto test re-isolated below cap; pin updated same commit; full suite **334 green**). Totals JOINED (`slate_shock_join.py` + 3 tests): 2,077/2,077 tickets matched to events, 2,019 with evening totals; **533 games carry 2+ tickets (1,504 tickets, 1,633 correlated pairs)** — the 12u/20u caps were ignoring this; high-total ≥9.5 split is +21.5% (n=327) vs +4.2% rest (descriptive, not a cap). 2025-lock PRE-REGISTERED (`policy_reset_2025lock_prereg_2026-09-11.md`; family + LCB selection rule + disclosed-peek judge; `--floors/--cap` engine overrides still to build). Zero credits spent (9/10 pull cost 0).
- **SIZING DESIGN 2026-09-11 (owner Q, no live change):** Four shadow arms already exist on the juiced ledger (same taken set). Flat beat 1/16-Kelly on ROI in **both** years (2025 4.2% vs 3.5%; 2026 12.6% vs 11.9%) and on DK+FD. CLV/WR are stake-invariant on that set — they cannot pick a sizing rule. Decision clock = open/morning juiced; close = CLV only. Nested 2025-select / 2026-judge is the right design to *choose* a sizing policy, but 2026 flat-vs-Kelly is already peeked → confirmatory, not a promotion. Pooled 2025+2026 is for measurement/power, not winner-picking. Keep 1/16 live. Do not compose a sizing search with a floor search. Fills still unlabeled.
- **JUICED POLISH + TOTALS GO 2026-09-11 (#124 DONE):** Owner locked (1) canonical book = next-US-book fallback, DK+FD is sensitivity only; (2) polish this ledger then stop policy coding; (3) featured totals pull; (4) asked to retune floors/veto from this run — **refused**. 2026 +12.6% is confirmatory peek; 2025 +4.2% is the selection-year number on the softer mix they kept. Named change + written peek-acceptance required before any `kpi_policy` / floor edit. Dated report: `docs/reference/reports/juiced_replay_ledger_2026-09-11.md`. Totals pulled: `featured_totals.parquet` 220,482 rows / 4,749 events / 363 days, remaining quota **3,714,815**. Clocks are calendar 12:00 ET / 19:55 ET (envelopes kept). Close-invalid already omitted from CLV (n_clv 2,075/2,077). By-book ROI now in the JSON.
- **JUICED REPLAY LEDGER 2026-09-11 (#123 GO, MEASURED):** `production/ops/market_research/juiced_replay_ledger.py` + 4 unit tests. Frozen `p_ours_cal` vs juiced two-way (friend OPEN if both sides else paid MORNING; DK else FD else next US book). Live policy as FILTER only (floors + 4.5-over veto + 2.5/3.5 bump 0.18). Rejected rows kept. Four shadow sizing arms. 2026 labeled confirmatory — **do not retune live floors/veto/Kelly from this run.** Artifacts: `juiced_replay_candidates.parquet` + `juiced_replay_report.json`.
  - All-books flat $50: n=2,077, ROI **+7.3%**, WR 0.506, CLV +1.08pp, over_share 0.21 (~50% open). 1/16-Kelly +6.6%; edge-band +6.4%; robust Kelly +7.8% on n=1,811 (266 Kelly-zero after shrink).
  - Split: 2025 n=1,308 +4.2% / 2026 n=769 +12.6% (confirmatory). Rejects: below_floor 12,542 / no_two_way 3,128 / veto_4_5_over 1,866.
  - **Book mix is the finding:** BetRivers 1,001 (+2.3% ROI, WR 0.429) vs DK+FD-only n=982 (**+12.3%**, WR 0.580, CLV +1.37pp, 80% open). GO said DK→FD→median; script implemented next-book fillable fallback, which soaks BetRivers at morning when DK/FD lack two-way. DK+FD-only is the honest target-book arm.
  - **Probation (data, not a live change):** skip-all 2.5/3.5-over n=1,999 ROI +7.41% vs live +7.28% = noise. 2.5-over n=18 ROI −4.9% (thin, keep hard floor). 3.5-over pooled +6.5% but 2025 −10% (n=46) vs 2026 +61% (n=14). 3.5-under n=42 ROI −8.2%. Recommend: freeze current probation; do not skip-all; do not half-size live; revisit 3.5-under at October juiced nested judge.
- **NEW incident (2026-09-06..08): morning workflow died 3 days straight** in step 1a with 412-byte logs (9/6 catch-up run 20:55, 9/7 catch-up 12:23, 9/8 on-time 8:30 all exit 1). No board/poll/alert those mornings → **no paper slates for 9/5–9/7, "No BET" midday alerts only**. Midday/settle tasks stayed green. Root causes: (a) box re-enters Modern Standby mid-run with no keep-alive held; (b) `AutomationSelfCheck` scheduler action mis-quoted (`--notify-on-red` parsed as wrapper param → always exit 1, never notified); (c) fail-fast chain skipped the alert step on ANY failure = silent mornings; (d) `run_morning_workflow.ps1` missing RUNBOOK's `--allow-stale`; (e) buffered python stdout hid kill evidence.
- **Fixes shipped (all verified live):** `run_task_captured.ps1` holds `SetThreadExecutionState` keep-awake + exit logging; new no-arg `run_self_check_task.ps1` (scheduler arg-parsing can't break it) re-registered → `Last Result 0`; morning + midday scripts run best-effort with **always-fire failure-banner alert** (`send_morning_alert --failure-message`), `python -u`, `--allow-stale` on log_projections, `--retries 3` on refresh_statcast; dry-runs now write `morning_alert_preview.json` (stop clobbering live send record).
- **Manual recovery run 2026-09-08:** full morning chain green → statcast→09-07, features, 9/8 slate (30), board **2 BET (Skubal U8.5, Schlittler U7.5)**, ledger 23 open, ntfy alert sent (200).
- **Grading audit verdict: CORRECT, no fix needed.** 0/1569 settled result mismatches vs `settle_side`; 12/12 sampled 9/4 settle Ks match MLB Stats API; no whole-lines; no null pnl/settle. Perceived wrongness = missing days, not mis-grades.
- **Backfill status:** graded actuals now complete 9/1–9/4 (9/3 18/18, 9/4 30/32; 2 missing = scratches); 9/4 ledger fully settled; 357 old settled rows with unrecoverable closes marked `close_unavailable=window_expired` (stop retry waste). **9/5–9/7 paper slates are unrecoverable by design** (no morning lines/projections logged; rescoring now would fabricate) — documented gap, not attempted.
- **2026-09-08 self-check noise fix (Agent):** RISK alerts now notify on first sighting + daily reminder only (fingerprint dedupe via `automation_self_check_notify_state.json`; state clears when green), with a readable body (which task + last_result, stale days + last date, what-to-do). Self-check task added to its own watch list. New `run_catchup.ps1` (settle→grade→self-check→alert) for after the box was off a day or two.
- **2026-09-08 ntfy-only strip (Agent, per user):** deleted Telegram/webhook/Twilio senders from `send_morning_alert.py` + `build_automation_self_check.py`; `set_alert_env.ps1` now ntfy-only. 28 fast tests green.
- **2026-09-08 audits (Agent, findings):** (a) metric inventory: dead-in-prod = Calmar, CVaR95, AUC/bias_pp, rolling_roi_50, --curve output, RMSE/R², ICC/split-half, BCa/stake-weighted CIs (spec'd, unwired — gates still percentile); dupes = 4× Sharpe blocks, 6× Brier/ECE blocks, 2 bootstrap stacks, mean_clv inline ×8. (b) CLV coverage patchy by date (0% on 8/14, 8/22–8/23, 8/25; ~3% 8/27–8/29; 60% overall) — watcher only fills inside tip window while alive. (c) watcher single-instance lock CORRECT; launcher/watchdog TOCTOU racy but contained by it; **ledger writes had no lock + no atomic temp+rename — FIXED 2026-09-08** (`atomic_write_parquet/text` in `odds_ledger.py`, wired into all ledger/real-bets/curve/meta writers + 2 regression tests; 30 fast tests green). Gap behavior: skip/stranded, never corrupt.
- **2026-09-08 git bloat fix (Agent, per user):** `data/Savant-Data/` untracked (`git rm --cached`, kept on disk, gitignored with rebuild note) — was bloating `.git` to 2.1GB with a daily-rewritten 2026 file, likely the real push blocker. History still fat until optional filter-repo (needs sign-off). Small static CSVs stay tracked. No other dead tracked files found (no .pyc/probes; artifacts/ already ignored).
- **2026-09-08 hosting/storage verdict (Agent research, per user Q&A):** Oracle ruled OUT (capacity games + June 2026 halving to 2 OCPU/12GB); PythonAnywhere free ruled OUT (no scheduled tasks for new accounts since 2026-01-15). Primary: **Modal** ($30/mo free ≈ covers ~35 CPU-min/day chains + 8h watcher ≈ $12; cron native, python-native). Fallback: **GitHub Actions cron** (reliable, free minutes, stateless → needs DB). Storage downstream of host: **DuckDB-over-parquet interim (zero migration, originals kept)** → Postgres (Neon/Supabase free) if/when cloud. Stack plan filed at `docs/reference/agentic_stack_final_plan.md` (model/harness decision explicitly DEFERRED per user).
- **Queued (user-approved direction, not started):** README-per-folder refresh + full docs/diagrams/paper sweep; historical-CLV-buy pipeline prep; metric canonicalization + dead-list deletion; drawdown brake.

## CLV + Hosting Plan — 2026-09-08 (user decisions locked)
> Pick-apart-able. Decisions: stay on laptop (Modal DEFERRED, revisit after CLV work); friend 12h opens treated as owned (26k K + 13k outs rows, 2025→2026-07-10, 9 books); closer = cron-sweeps redesign (daemon retired when host moves); **plan scope evolved 2026-09-08 → $119/5M, all-confirmed-markets, regular-season only, us region only.** See §CLV Execution state below + `docs/reference/clv_backfill_plan.md`.

### Odds API execution plan (account EXISTS — key emailed 2026-08-12… verify, then buy)
> Full stable reference: `docs/reference/clv_backfill_plan.md` (scope, math, book
> doctrine, timestamp doctrine, pull protocol, pre-registered cells, expansion,
> risks, done-criteria). This section tracks execution state only.
> Owner tags: **You** = human (account/billing/browser steps); **Agent** = terminal steps.

1. **You — recover key (5 min, $0):** search `cameron.kaplinger@gmail.com` for the-odds-api mail (2026-08-12); else log in at the-odds-api.com → dashboard shows key + current plan. Paste key into repo `.env` as `THEODDSAPI_KEY=<key>` (gitignored — never commit). ✅ DONE 2026-09-08 (key valid, Starter 500/mo).
2. **Agent — validate key + quota ($0–2 credits):** ✅ DONE 2026-09-08 (200, 500 remaining).
3. **Agent — free verification calls (4 credits, 496 left):** ✅ DONE 2026-09-08. Props need the event-odds endpoint (live `/odds` 422s on props — $0, uncharged). Books in scope: DK/FD/BMGM/Bovada/BetOnline (+ESPN/Fliff/HardRock via us2). **Pinnacle ABSENT for MLB props on every US region** ⇒ Kalshi is the sharp reference; DK/FD devigged close is the book benchmark. Schema verified matching `pull_oddsapi_historical.py` normalizer (description/point/name/price).
4. **You — decision gate:** subscribe **$119/5M** (all confirmed markets; $59 can't fit the bundle — see credit math §3 of plan doc).
5. **You — subscribe $119/5M in dashboard** (same day as pull; calendar-note the cancel date; you're handling cancel).
6. **Agent — estimate → pull (closes-first, 10% floor) → normalize** via `pull_oddsapi_historical.py`. Start 2025 season, then 2026. Watch remaining headers.
7. **You — cancel subscription same week** (keep key; free tier remains for plumbing).
8. **Agent — rerun Kalshi+book skill report side-by-side** → 4.5-over probability-vs-selection verdict → floor/calibration recommendations under standing rules (chrono-safe splits; post-freeze eval-only; no live edits without sign-off).
- **2026-09-08 event-count estimate (Agent, $0 spent):** season game counts via weekly MLB-schedule sampling ⇒ ~2,430 (2025) + ~2,000 (2026 thru Sept) ≈ 4.4k events ⇒ close-only ≈ 44k credits/market + discovery. **Credit-model correction (Agent): NOT per-book — 10 × regions × markets × event × snapshot.** Regions MULTIPLY: `us`+`us2` doubles cost for marginal books (ESPN/Fliff/HardRock) — **pulled `us` only.** (An earlier note wrongly called us2 free; corrected in plan doc §3.) Consensus via devig-mediian is $0 extra within `us`. Pinnacle absent for MLB props ⇒ Kalshi = sharp anchor. Standing rule: consensusCLV reported as "beat soft consensus", never true-probability proof.
- **Calibration challenger protocol (user Q, locked):** NO live reset. Retrospective 3-way on the new close-anchored panel — RAW vs Platt (20260803 bundle) vs isotonic (20260821 bundle) — scored on a held-out chrono split; champion freezes until a challenger clears the gate with sign-off. Timeline clarified: pre-8/21 ledger rows carry Platt-or-raw by date, post-8/21 isotonic; per-regime split (not pooled Brier) is the comparison unit. The 8/21 open-fit stays labeled imperfect (open-only, no closes) until the challenger test.

- **Phase 0 — free shakedown (next):** scaffold `production/ops/market_research/pull_historical_clv.py`: SmartStake HF ticks + friend CSVs → raw cache → normalized parquet (`data/Odds-Historical/`, ignored) → join (norm name+date+line) → existing `clv_pp` + dedupe. Proves schema at $0. Friend-data gap noted: opens end 2026-07-10; live ledger covers 7/30+; 7/11–7/29 thin.
- **Phase 1 — $119 buy (scope evolved):** all confirmed-available pitcher+batter markets, closes-first, regular-season 2025+2026, `us` only, `x-requests-remaining` hard stop at 10% buffer, raw JSON kept forever, cancel same week. Analysis under standing rules (chrono-safe splits; post-freeze eval-only; no live calib edits without sign-off). First question: 4.5-over bleed = probability (Brier skill) vs selection (side) problem.
- **Phase 2 — cron-sweep closes (replaces daemon):** sweep q15–20min in game windows; close = latest pre-first-pitch quote per ticket, anchored to per-game first-pitch ts (Statcast/MLB API), NEVER cron alignment — odd start times handled by construction. Next-morning sweep finalizes stragglers; then `unavailable`. Daemon + lock retire when this ships.
- **Phase 3 — expansion:** outs/hits/walks = pull #2 (verify market keys then); PropLine graded dump as alt; metric canonicalization; drawdown brake.
- **Phase 4 — Modal (DEFERRED, reference only):** Dockerfile + volumes + cron map + $30 spend cap + billing alerts; laptop stays live until cutover proven. Oracle ruled out (capacity games + halving); PythonAnywhere-free ruled out (no scheduled tasks for new accounts). Code stays host-agnostic (env secrets, file/DB state) so the move is a redeploy.
- **2026-09-08 Kalshi scaffold BUILT + first honest skill read (Agent):** `production/ops/market_research/pull_kalshi_k_history.py` (events→ladders→trades-closes→report, checkpointed, first-pitch-anchored, close = last pre-scheduled-start trade; whole-number rungs map directly to .5 lines, no interpolation). Caught + fixed 3 bugs (dollar-vs-cents, post-game lookahead via last_price, gameDate field). First read 1,428/91% (superseded by hardening below). SmartStake parked (batter-only; cache cleared).
- **2026-09-08 skill-read hardening (Agent):** join → 1,512 / 96.3% via accent-fold in `norm_player_name` + 4-entry Kalshi alias map; remaining misses are genuine per-game absences. **Kalshi pre-game Brier 0.219 vs model 0.287 (skill −0.077); model mean_p 0.60 vs emp 0.42 = quantified over-bias.** Ledger p_model is 83% calibrated (444 Platt + 408 isotonic; 17% raw) ⇒ gap is post-calibration. Error attribution (n=938): K-residual corr 0.93 k-rate vs 0.37 TBF; TBF explains 21.5% of K variance; xK bias ≈ 0 ⇒ **k-rate ability, not workload, is the bind.**
- **2026-09-08 Odds API adapter BUILT (Agent):** `production/ops/market_research/pull_oddsapi_historical.py` (estimate/pull/normalize; closes-first; max-credits + 10% floor from live billing headers; raw JSON kept; fails clean without key).
- **2026-09-08 selection-amplifier finding (Agent):** on BET tickets model OVER-predicts xK +0.7–0.9 (vs +0.05 globally) — edge floor SELECTS over-predicted tickets. Over-bleed = selection amplifier × probability problem. "Capped" arms = breakouts/call-ups (Seymour −4.8, Lopez, Rogers, Luzardo, Detmers) + declining vets over-predicted (Sale, Eovaldi): STALENESS + age curves, not aces. Mid-bin xK 4–6 (n=1,168) bleeds spread-wide. TBF error symmetric shrinkage (+5.2 hooks / −5.1 marathons) ⇒ two-stage workload confirmed.
- **2026-09-08 program launched (Agent+user):** `docs/reference/beat_the_books_program.md` — 7 ordered workstreams with kill criteria (blend → staleness → TBF two-stage → k-rate tails → NB challenger → steam overlay → floor raise), disregard list, measurement contract, money-truth definition. Positive-EV goal, no promises: current xROI −0.07. Fixes ordered: (1) tactical shrink-to-consensus blend; (2) tail-aware k-rate (quantile/focal emphasis, elite-whiff interactions); (3) TBF long-outing handling (CG/hook two-stage); (4) NB count-layer challenger (unparks SSAC future-work #9 as research only).
- **New questions the bought data can answer:** (a) where exactly does model-vs-book divergence concentrate in xK space (saturation curve)? (b) which pitchers get capped hardest (residual vs xK level)? (c) does TBF error concentrate in CG/SHO vs early hooks? (d) do ace games drive the over-bleed (selection) or is it spread-wide (probabilities)?
- **2026-09-08 pull inventory (Agent):** 4,660 events, 2025-03-27→2026-09-09; pitcher 1.64M rows / 5MB (K 151k close + 148k morning; alt 738k; outs 175k; hits-allowed 198k; ER 165k; walks 59k), batter 10.9M / 35MB (9 markets × 2 snaps), 9 books both. Raw 1GB ignored. **Partitioning verdict: keep 2 files** (40MB total scans in seconds with predicate pushdown; month-split = premature optimization; revisit only on measured pain or winter batter growth — then split batter by market).
- **2026-09-08 book-skill verdict (Agent, `analyze_book_skill.py`, n=1,049 consensus-joined):** model Brier 0.285 vs consensus 0.244 (skill −0.041); overs ROI −23% (n=524) vs unders +10% (n=525); smashed: under 3.5 +44% (n=75), under 7.5 +42% (n=36); smashed-against: over 4.5 −55% (n=179), over 5.5 −87% (n=127) ⇒ veto thesis CONFIRMED on big-N book data, and it's BOTH probability (worse Brier on overs too) and selection. Floor sweep: ROI turns positive ≥0.16 (+3.7%, n=205; +18% at 0.20/n=122 thin) vs −2% at live 0.12 ⇒ supports raising over floor, but same-data sweep = selection ⇒ chrono-split gate before promoting. Calib split (era-confounded, NOT same-game): raw 0.277 < platt 0.281 < isotonic 0.291 ⇒ do NOT read as "isotonic worse"; needs same-game challenger. Money: Sharpe −0.03, Sortino −0.05, decay negative throughout, CLV +0.0016/46% beat, xROI −0.07 ≈ realized −0.06 (model knows it's marginal).
- **2026-09-08 market availability VERIFIED live (Agent, ~15 credits):** per-event endpoint; `us` region. ✅ pitcher_strikeouts(5), pitcher_outs(6), pitcher_hits_allowed(3), pitcher_earned_runs(3), pitcher_strikeouts_alternate(7), batter_home_runs(1), batter_hits(4), batter_total_bases(5), batter_rbis(3), batter_walks(1), batter_hits_runs_rbis(4), batter_runs_scored(2), batter_singles(2). ❌ **pitcher_walks_allowed 422 (does NOT exist under that key).** ⚠️ batter_strikeouts 200 but 0 markets (sparse).
- **2026-09-08 dead-market resolution (Agent):** pitcher_walks_allowed → real key **`pitcher_walks`** (valid but 0 markets = sparse); batter_strikeouts valid but 0 markets = sparse. **Historical 2025 depth NOT verifiable for free** — historical endpoint 401s on free tier (`HISTORICAL_UNAVAILABLE_ON_FREE_USAGE_PLAN`). Thin markets burn full credits (10/event) for little data — include for cheap insurance at 24% utilization. **Credit estimate (4k reg events):** 15 markets × 40k = 600k close-only; close+morning = **1.2M (24% of 5M)**; +open = 1.8M. All fit $119/5M. **Governance decision (Agent, user-notified):** NO repo/folder rename — MLB-Props is the all-props umbrella; strikeouts-only is a model scope, not repo scope. Data layer partitioned by market under `data/Odds-Historical/theoddsapi/raw/{market}/{snapshot}/{event}.json`; **normalize SPLITS into two parquets — `book_lines_pitcher.parquet` + `book_lines_batter.parquet` (each with a `market` column; unknown markets → `book_lines_other.parquet`, audited never dropped)** — all Polars-native (v1.43). Raw JSON = source of truth (re-normalizable). Batter suite = future model namespace (`src/Python/props/` someday), not data-storage change today.

### PRESENT — where we are / just completed
- **REPLAY ARCHITECTURE DOCS 2026-09-11 (#122):** standing spec `docs/reference/oddsapi_replay_architecture.md` (freeze / work / product; floors ≠ ROI maxima; 1/16-Kelly conservative not optimal; vendor-timestamp doctrine; juiced 1u as the real next measurement; White/PBO on policy configs; 15-step capability map explicitly **not** a queue). Wired into AGENTS.md, canonical map, SOP §7–§8 (reconstructed snapshot_ts is **not** a true close), golden metrics, clv_backfill_plan (buy DONE), market_clv_gates, beat-the-books money-truth, production README, handoff START HERE, research_assistant_instructions. Inventory report remains the dated evidence. No live policy change. No juiced-attach code this turn.
- **ODDS API REPLAY INVENTORY 2026-09-11 (#121, user-unparked research):** frozen-model 2025-present open/close backtest does **not** need a new buy. Lake already on disk; remaining quota **3.72M / 5M**. Paid snaps = close+morning only (4,509 / 4,680 raw files); friend −12h opens cover 2025-03-27→2026-07-10 (26,147 K rows, 2,928 event overlap). No paid `open` folder. **$0 integrity find:** every raw JSON already has vendor `timestamp/previous/next`; normalizer dropped the wrapper and reconstructed commence−5min/−5h. Recovered `snapshot_envelope.parquet` (9,189 rows, 100% envelope). Close p50 lag 5.4 min but **12 closes AFTER commence** (not closes) and 173 reconstructed clocks >10 min off vendor. Do **not** 5-min-tick globally (~6.7M credits). Cheap optional later: featured `totals` (slate environment, thousands of credits) and K-only −30h open (~47k). Next $0 build: juiced DK/FD/median attach on **both years** + rejected-candidate 1u ledger. Nested juiced v2 still post-9/27. Report: `docs/reference/reports/oddsapi_replay_inventory_2026-09-11.md`. No live policy change.
- **REPO QUALITY 2026-09-11 (#119):** passthrough only. Ignored+removed `tmp_nb_figs/` (769 KB notebook PNG dump). SOP next-build line retargeted to this Snapshot. Canonical map + hold inventory `docs/reference/reports/repo_quality_hold_inventory.md`. No production deletes. Untracked ops/research scripts stay **keep** until you commit.
- **CURSOR VERIFY 2026-09-11 (#118):** golden file + #116 numbers match disk with two lane-tags OpenCode under-labeled: (1) paid CLV +0.26/+0.21 is **undeduped** (n=1105/1112); `dedupe_ledger_props` settled paid-close is n=634 / +0.64pp. Raw `clv_pp` n=975 is the balloon; canonical paper CLV is still n=546 / +0.59pp. (2) harness CLV +8.53 / xCLV +8.46 / skill −0.0021 is snap=best judge, not live money. MAE_K 1.78 verified (`grade_summary_all.json` `mae_K_mean_over_dates`=1.780, 40 dates). Universe table exact. paper_quant full/veto exact vs brake 25.84/11.1. Handoff START HERE retargeted: 0–2 done, wait `go step 3`.
- **BACKEND FIRST 2026-09-10 (#113, owner):** Dashboard v1 PARKED. Steps 0–2 DONE 2026-09-11. Next is #113.3 (shared SharpAPI fetch) after `go step 3`.
- **OPENCODE CONTRACT TIGHTENED 2026-09-10 (#112):** past nested analysis was in-sample 2026 (documented #109, not to be "fixed" live). Do not re-run #66–#110; do not retune veto/floors. SOP §8 gained the nested-hole rule. Pickup box: FORWARD wall is a diary, not a queue.
- **PROCESS HOLES 2026-09-10 (#109):** nested 2025-select / 2026-judge was endorsed but live veto/floors came from 2026 paper; WS1c/stacker cuts ate 2025; harness peeked 2026 twice; notebooks still SharpAPI ledger. Full write-up in `docs/reference/opencode_handoff.md`.
- **OPENCODE HANDOFF 2026-09-10 (#110):** wrote `docs/reference/opencode_handoff.md` — paste-ready review (nested-design holes, golden-CLV vs notebooks, ROI ranking, dashboard spec, allowed-vs-gated contract). `cursor_deep_dive_brief.md` now points there. AGENTS.md pointer added. Not a second queue.
- **Program is live (2026-09-08/09):** `docs/reference/beat_the_books_program.md` guides all model work. Master diagnosis: books ECE 0.015 vs model 0.159 (post-calibration gap); BET-ticket xK +0.7–0.9 vs +0.05 global = edge floor selects over-predicted tickets (two-layer disease: miscalibrated probs × selector); capped = breakouts/call-ups (Seymour −4.8, Lopez, Rogers, Luzardo, Detmers) + declining vets (Sale, Eovaldi) = staleness, not ace saturation; TBF ±5.1 symmetric shrinkage = single-ridge artifact; mid-bin xK 4–6 (n=1,168) bleeds spread-wide. Current xROI −0.07 — no positive-EV promise.
- **WS1 GO (user 2026-09-09):** shrink-to-consensus blend weight is the next build — days, highest EV/cost, shadow-first, fit on close panel only, kill = no Brier gain on held-out chrono split. WS2 breakout-detection scopes in parallel (no code until WS1 shadow is running).
- **WS1b RESULT 2026-09-09 (Agent):** morning-blend confirms closes — best w=0 on deployable T-5h consensus too (n=1,093, Brier 0.2495 vs 0.3051). No live blend; model-error work is the only path. Next: historical projection feasibility (#15), then WS2.
- **ALT-HARNESS RESULT 2026-09-09 (Agent):** mid-curve is the disease (worst skill 2.5–5.5, our bias +4.6pp @4.5, gap 0.10); tails already fine. Universe-scale curve work APPROVED (#18 next: time-travel test first). Rerun-model Q answered: yes — frozen-bundle inference on 2025–2026 is legitimate eval (no retraining); 2025 = benchmark, 2026-to-date = clean.
- **2025-PEEK RECEIPTS 2026-09-09 (Agent, user Q):** (a) pipeline keeps 2025 rows by design (`config.py:53-60`, HOLDOUT_SEASON=2025; train/research locked 2023-24); (b) selection-influencing peeks — `multi_lift_pre_freeze.py:293` one-shot 2025 confirm + sparse40 holdout_2025 permutation (configs kept that confirmed well); (c) Item-14 open-calibration challenger fit on 2025–2026 opens (research only, never promoted). LIVE PATH CLEAN: isotonic-20260821 bundle fit_cutoff 2024-09-30 on 2024 walk-forward preds; `train.py:256` metadata itself forbids pristine-2025 claims. Hence 2025=benchmark / 2026-live=clean stands.
- **TIME-TRAVEL RESULT 2026-09-09 (Agent):** 2025-09-20 frozen-bundle slate (32 rows) → joined 2025 closes → pooled skill −0.047 (books win in 2025 too). Full hist-run UNBLOCKED and APPROVED (#18). Next: date-loop scorer + join + pre-registered cells (#17).
- **UNIVERSE RESULT 2026-09-09 (Agent):** 8,775 starts scored → `universe_panel.parquet` (70,200 line-points, audit `universe_join_audit.json`). We BEAT the opener (+0.044 skill, n=7,986) but trail closes/mornings slightly (−0.003..−0.006). Edge lives early — timing thesis confirmed. Next: pre-registered cells (#17) → WS2.
- **CELLS RESULT 2026-09-09 (Agent, #17 DONE):** bias flips +4.8pp @2.5 → −14.5pp @9.5 (under-dispersed curve → WS5); mid-bin worst; TBF tails +9.5/−5.4 (WS3); debuts +0.49/+0.28 (WS2); seasons identical; isotonic loses 5.5/6.5 (line-specific calib scoped). Bins-first doctrine law (#21). Next: WS1c mid-curve correction → WS2 features.
- **RACE + WS1c RESULT 2026-09-09 (Agent):** Poisson > binomial 7/8 lines (zero-param shadow challenger); WS1c per-line Platt beats isotonic 6/6 (slopes 0.62–0.96 = shrinkage quantified). Live bundle untouched. Next: WS2 staleness.
- **GATE RESULT 2026-09-09 (Agent):** ledger common-subset n=994 — poisson +0.028, ws1c +0.022 over bundle. SHIP PROPOSAL filed (#23): Poisson swap + WS1c overlay await weekly-pack confirm + explicit go. Next: WS2 rescoped (no minors).
- **MATRIX RESULT 2026-09-09 (Agent, #24):** combo ≈ WS1c alone — ship WS1c first, Poisson as fallback; WS2 (new info) still required to actually beat books. SOP explained; all documented.
- **PRE-WS3 SWEEP 2026-09-09 (Agent, #26 DONE):** veto validated (+9.6% vs +3.5%); snapshot_ts fixed $0; consensus cached (70k); velo-cliffs dead. WS2 parked. Decks clear — WS3 TBF two-stage is next and it's the biggest error mass on the board.
- **WS3v1 RESULT 2026-09-09 (Agent, #27 KILL):** naive two-stage ties overall, helps hooks slightly, HURTS marathons — dead. Fix needs pitcher-level hook structure, not global mixture. Reflection (user Q): n explained (91 = live bets, 70k = universe — different jobs); backlog current (27 items, snapshot every turn); governance_good (ignored outputs, atomic writes, resume, lineage); file practices need one fix (research scripts must read consensus_cache, retire per-script devig).
- **SHIPPED 2026-09-10 — WS1c LIVE (#28):** production calibrator is now per-line Platt (8 lines, n≈8.7k each). Gates passed, backup + one-line revert filed, weekly pack green. Tomorrow's board scores under the new maps. Next: Poisson package → WS3 v2.
- **SHIPPED 2026-09-10 — POISSON LIVE (#29):** count family flipped via one constant (tests green, lineage in report, one-line revert). Both ships live; next boards carry them.
- **WS3v2 RESULT 2026-09-10 (#30 KILL):** EB hooks help hooks (−1.07) but crater overall/marathons — mixture misapplied; marathon error is MEAN shrinkage of workhorses. v3 direction parked. TBF tails remain the open war.
- **WS6 + AGE RESULTS 2026-09-10 (#31, #32 DONE):** edge fully decays −12h→−5h (bet at open, morning ≈ close, never chase); age U-shape (young −0.0090, prime −0.0033, 33+ −0.0058) with bias ≈0 prime vs +0.16 tails — WS2-age real signal. Next: age features → retrain; WS7 floors on morning numbers.
- **AGE-CORRECT KILL 2026-09-10 (#33):** offsets help MAE a hair but HURT Brier — age belongs in retrain, not overlay. Remaining queue: WS7 floors on mornings, retrain spec (age+workhorse features), live-ship monitoring, consensus-cache migration, alt-harness refactor.
- **AGE/TRUTH + WS7 2026-09-10 (#34, #35):** no linear age edge (r≈−0.02); U-shape + survivorship (38+ elite xK, −0.45 resid) confirmed; age candidates registered for retrain (age, age², age×recency/prior/velo); WS7 challenger loses to live policy — no change. Post hoc pipeline hardening queued before retrain.
- **HARDEN + WS3v3 2026-09-10 (#36):** umpires OUT (ABS), weather/lineup parked (reasons in program §3); cache migration bit-identical; workhorse KILL — marathons irreducible pregame → policy-side + fat tails, not point estimates. TBF modeling exhausted pre-retrain; remaining: alt-harness rewrite, nightly cron (user scheduler action), retrain spec.
- **FS REVIEW 2026-09-10 (#37):** code tracked + tiny; bulk ignored correctly; .git fat until filter-repo sign-off; dimensions local-only (rebuild cmd filed); 70 paths await YOUR commit+push (commands in #37); harness rewritten bit-identical. Next: nightly cron commands → retrain spec.
- **RETRAIN CHALLENGER 2026-09-10 (#38 SURVIVE-narrow):** age features live in L3 + new set resolves (62 feats); beats production on 2024 holdout (+0.0009), 2025 (+0.0001), 3/4 age cohorts. Not promotion — needs universe Brier + confirm + sign-off. Pipeline now age-capable; retrain door open.
- **REVIEW 2026-09-10 (#39):** KSplit teardown validates us + assigns homework (WS5b Poisson-binomial, kAdj residuals, leash-as-distribution); open-command assessed (CV command scores, NC-license flag — research only); single-feature rule law (SOP); ensemble = 1 frame → 3 boosters (0.0/0.6/0.4) → TBF ridge → count layer; edit-failures owned (read-first + small boundaries; tests green). Post hoc ~90% done. SOLID PLAN: Phase A freeze post hoc (cron+monitoring) → Phase B single-feature ablations (age terms first) → Phase C KSplit homework (WS5b, kAdj) → Phase D retrain discipline → Phase E money truth (fills).
- **DOCS SWEEP 2026-09-10 (#47):** program carries universe diagnosis, KSplit targets, WS5b/WS8/WS9, bet-at-open doctrine, +3 disregards; SOP carries Berkson/stability/external-data/single-feature/ensemble rules (deduped); backlog #40–47. Docs match code. Build whenever signed.
- **WS5b RESULT 2026-09-10 (#48 KILL-v1):** 9-slot Poisson-binomial loses to binomial all 8 lines — 50/50 blend confounded mean with spread. v2 = fit blend + calibrate first, then judge spread. Reference docs pulled + verified. Next: open-command pull or WS5b-v2.
- **WS5b-v2 RESULT 2026-09-10 (#49 KILL-final):** best blend w=0.0 — batter slots add ZERO; PB≡binomial. Aggregation retired; KSplit's edge must be matchup weighting (kAdj), not the sum. Learnings: mean dominates variance; our aggregates already absorb slots. Next: open-command pull → kAdj.
- **AUDIT + COMMAND 2026-09-10 (#51, #52):** board fallback unified to Poisson (test updated, 20 green); cache superset explained; universe frozen-config documented; L3 growth explained. Command ablation KILLED-narrow (2024 win, 2025 loss; option-A retest queued). Slow-down rules filed. Next: kAdj residuals → nightly cron commands.
- **HANDOFF 2026-09-10 (#53):** session summary filed for new-chat context (ships, kills, panels, files, ROI path, metrics, architecture, review, ablation tables — see chat brief). Live: WS1c + Poisson + veto green; weekly pack green. Uncommitted work stands (70+ paths — owner commits per #37). Resume: kAdj residuals → nightly cron → option-A command retest → retrain spec → fills.
- **COMMAND STAGE-1 2026-09-10 (#50):** 440MB pulled, 56k start-rows, 93% join; roll30cmd vs residual −0.039 (−0.055 stuff-conditioned) — weak, right-signed, stable → single-feature ablation queued next.
- **REVIEW 2026-09-10 (user Q):** live ledger 1,676 settled / +$309 paper PnL. Edge verdict: NONE vs closes (−0.006 universe, −0.041 selected); opener-beating (+0.044) + veto pockets (+9.6%) + timing doctrine are the only green. Docket left: harden (cache migration, harness rewrite, nightly cron) → retrain spec (age+workhorse) → WS6 operational timing → floors already right. Eye-test backed: age×interaction registered.
- **WS8-kAdj v1 RESULT 2026-09-10 (#54 SURVIVE-narrow):** `ws8_kadj_probe.py` → `ws8_kadj_report.json` — usage-weighted per-pitch CSW residual vs league (prior-only, 93% coverage, YoY r=0.75 PASS). Corr raw 0.113 / stuff-conditioned 0.054 (new info survives Berkson) / within-pitcher −0.06 (levels, not changes). 1-param OLS beta=0.49 → TEST MAE 1.7594→1.7497 (matches KSplit 1.749) + Brier gain +0.00096 vs live-equivalent (Poisson+WS1c) on n=2,452 (clears 0.0005 bar). Shape: helps tails 6.5/7.5 big, hurts mid 4.5/5.5 — WS4 kin, not the mid-curve fix. Still trails book (−0.0049). Next: ledger-gate overlay as shadow → register kadj as retrain candidate (single-feature rule); v2 idea parked (usage-fixed weights to split quality vs mix). No live change.
- **NIGHTLY CRON 2026-09-10 (#55 SHIPPED-pending-register):** `check_nightly_drift.py` (7 checks: settle freshness, model-error drift, side drift, veto-leak on live board, WS1c tail watch, feature freshness w/ #45 2x-null rule, ship watch incl. Poisson-default tripwire) + `run_nightly_drift.ps1` (no-arg, keep-awake, best-effort settle→grade→drift→self-check, pages ntfy on RED only) + `MLBProps_NightlyDrift @05:30` in setup + self-check watch entry + 5 unit tests green. Live run: YELLOW (L3 3d stale, true) else GREEN incl. veto 0 leaks. Caught+fixed in build: ledger 4.5-overs are paper-by-design (tripwire moved to board), pointer carries no line maps (read joblib bundle). OWNER ACTION: re-run `setup_automation_tasks.ps1` to register the task. Next: option-A command retest → retrain spec.
- **REVIEW 2026-09-10 (#56 cron deep-review, 4 fixes):** (a) REAL BUG — py crash exited 1, identical to quiet-YELLOW: added exit-3 wrapper + runner buckets 3+ as failure; (b) `max(_to_date…)` crashed on any malformed date: `_max_date` helper drops unparseables (tests added, 6 green); (c) nightly RED banner overwrote `morning_alert_latest.json`: new `--record-name` (default unchanged, all callers safe) + nightly uses its own record; (d) dead `passes_floor` column dropped from drift ledger select. Verified: live drift re-run same verdict, alert dry-run clean, ps1 parse OK. Non-issues confirmed: YELLOW-night exit-1 normalized by runner (self-check sees exit 0, no double-page); baseline-side leniency documented; settle/backfill overlap contained by atomic writes. RUNBOOK+INDEX document the new task.
- **OPTION-A RESULT 2026-09-10 (#57 KILL-final):** `command_optionA_retest.py` → `model_quality/command_optionA/report.json` — train 2024+25 (6,605) / test 2026 (4,139), base58 vs +cmd_roll30, same seeds. MAE delta +0.00013 (noise, 7x smaller than age's narrow-survive). Universe Brier main-only n=6,866: base 0.2378 vs cmd 0.2392, gain −0.00141 (hurts, 3x bar wrong way). Production ensemble 0.2248 still beats both single-member retrains (expected). Caught+fixed mid-build: alt-line join flattered challengers (main-only filter now, SOP contract). Command RETIRED as k-rate feature ( correlates −0.039 with residual but the model absorbs it — same epitaph as #52). Next: retrain spec (age+workhorse+kadj) → kAdj ledger-gate → fills.
- **RETRAIN SPEC 2026-09-10 (#58):** `docs/reference/retrain_spec.md` — candidates age-per-term (owed) → kadj (L3 impl + member test + parallel overlay gate) → workhorse; command/slots/recency/cliffs retired in writing; native splits (no exceptions); member-vs-member judging only (#57 lesson); best-iteration inflation as fail signal; WS1c refit scoped at promotion; order fixed. Next: per-term age ablation.
- **AGE PER-TERM 2026-09-10 (#59 SURVIVE-2-of-4):** `age_per_term_ablation.py` → `model_quality/age_per_term/report.json` — base 0.07874/0.07935 reproduces #38 exactly. KEEP `pitcher_age` (+0.00066/+0.00033) + `age_x_whiff_gap` (+0.00065/+0.00010 hair-thin); DROP `age_x_velo_gap` (−0.00001/−0.00009); RETIRE `pitcher_age2` (bit-identical to age — monotone transform, trees can't split them apart). Self-caught process bug: new script shipped without `__main__` guard (silent exit 0, no output) — burned 4 debug rounds before reading the file tail; rule: read back the tail of every new file + `--help` smoke before first real run. Spec table amended. Next: kadj L3 + member test.
- **KADJ L3 2026-09-10 (#60 IMPLEMENTED-UNVERIFIED):** `_join_kadj_features` in `pipeline/training.py` (exact probe port: prior-20 roll, expanding league priors, prior-5 usage, ≥5 starts/≥300 pitches else null+`kadj_missing`) wired into `build_pitcher_training` (graceful-null fallback like command) + `kadj`/`kadj_missing` allow-listed in `features.py` (no regex conflicts — verified; unlisted numerics fail loudly so all three edits were atomic-required) + `tests/test_kadj_join.py` (hand-computed 0.05, same-date exclusion, gate null+flag, dup fail-loud — math hand-verified). NOT YET RUN: shell died mid-turn (even `echo` fails) before pytest/parity/L3-rebuild. Re-run: `python -m pytest tests/test_kadj_join.py -q`, then parity (coverage ~93%, YoY ~0.75 on scored starts), then L3 rebuild + member test. Note: kadj auto-enters default candidates on rebuild (approved, non-experimental) — gated by spec, live bundle frozen unaffected.
- **VERIFY + PARITY 2026-09-10 (#60 closed):** shell recovered; `test_kadj_join` 3 passed (+1 new: leading-null schema regression caught real L3 ComputeError — explicit schema fix). `kadj_l3_parity.py` → behavioral parity EXACT (coverage 0.930=0.930 on scored population; YoY 0.748 vs 0.751). Coverage gap full-L3 (0.863) is population (2023 rows lack priors), not algorithm — parity script compares on scored overlap. SOP §8 opened (hardening log — see below).
- **KADJ MEMBER 2026-09-10 (#61 KILL-narrow):** `kadj_member_test.py` → `model_quality/kadj_member/report.json` — base58 vs +kadj, native splits, same seeds. MAE better BOTH windows (test +0.00063, hold +0.00046); universe Brier 2025-holdout main-only n=12,667: 0.2320→0.2319, gain +0.00015 (< 0.0005 bar). iters 211 vs 166 (no capacity inflation). prod ensemble 0.2181 beats both singles again; book 0.2129. Rule binds: retrain vehicle dead. OVERLAY vehicle (beta, +0.00096 on 2026-heavy split) stays alive → ledger-gate shadow next. Regime note: member judged on 2025, overlay on 2026 — part of the gap may be regime; the gate decides, no chasing. Spec table amended. Next: kAdj ledger-gate overlay → workhorse.
- **LEDGER GATE 2026-09-10 (#62 KILL):** `ledger_gate_kadj.py` → `ledger_gate_report_kadj.json` — pre-registered beta on bettable panel n=969: base 0.2479 → overlay 0.2481, gain −0.00018. kadj RETIRED fully (both vehicles dead; signal real only in unselected data — selection strikes again). Caught+fixed mid-build (2, both mine): (a) game_pk join fanned out ×1.8 (2 starters/game — first run's +0.00206 SURVIVE was noise, overturned); (b) cell-n incremented per-candidate (×3, cell Briers read 1/3 scale). Cells now sum to n. No weekly-pack confirm needed (KILL needs none). Next: workhorse terms — the last unbuilt feature idea.
- **WS9 FAMVULN 2026-09-10 (#63 KILL):** `ws9_famvuln_probe.py` → `ws9_famvuln_report.json` — batter whiff-vs-family × arsenal usage over 7.8M Savant pitches (user-unparked shelf). Coverage 98%; assoc survives Berkson (raw 0.069 / stuff-cond 0.065 / within-pitcher +0.074 — genuinely matchup-driven, unlike kadj); MAE −0.008 BUT Brier −0.00071 → KILL. Methodology correction: pitcher-season YoY (0.085 FAIL) is the wrong gate for lineup-driven features — batter-level whiff YoY is 0.84 (n=337, stable inputs). Verdict stands: stable inputs, real residual signal, unconvertible (mean-help/curve-hurt + selection — same epitaph as kadj/command). Caught+fixed mid-build (2): yearly game_date type split (per-file cast), lowercase pitch codes in pitch_type_games (upper-normalize). Spec retired-list amended. Pregame features effectively EXHAUSTED (see #63 note).
- **AGE BRIER GATE 2026-09-10 (#64 SURVIVE-both):** `age_member_brier.py` → `model_quality/age_member_brier/report.json` — base58 vs +age vs +whiffgap, native splits, same seeds, universe Brier 2025-holdout main-only n=12,667: age +0.00064, whiffgap +0.00107 (both clear bar; whiffgap weakest-on-MAE yet strongest-on-Brier). Gains concentrate 2.5–5.5 (the bleed zone — right shape); tails neutral. Combined-60 justified next. WORKHORSE CORRECTION (honest): spec's "unbuilt workhorse" was wrong — WS3v3 already tested PA_P20/Outs/efficiency in-ridge and killed it; redirect stood on a false premise, corrected before building. Remaining sliver (k-rate-side workhorse) thin-theoried → retired. Caught+fixed: 3-tuple/2-tuple key bug (zero-match Brier, loud) + O(n²) index (dict). Spec tables + build order amended. Next: combined-60 → walk-forward → gate.
- **COMBINED-60 2026-09-10 (#65 KILL-final):** `combined60_final.py` → `model_quality/combined60/report.json` — base58 vs +age+whiffgap, same frozen challengers judged on 2025-holdout AND 2026-clean (no refit between windows — design fixed pre-run). MAE worse BOTH (hold −0.00057, clean −0.00080); Brier worse BOTH (−0.00214 / −0.00259). Per-term wins DON'T compose (age×whiff interaction overfits: val-l2 worse + 349-vs-211 tree inflation — the spec §4 fail signals fired exactly as designed). Pregame retrain path NARROWED to base+age(59) alone (+0.00064, #64) → walk-forward next. If that dies, pregame modeling is EXHAUSTED and the program is timing+selection+fills. Next: base+age walk-forward → gate.
- **WALK-FORWARD 2026-09-10 (#66 KILL-final):** `age_walkforward.py` → `model_quality/age_walkforward/report.json` — 5 rolling folds, pre-registered (pooled ≥0.0005 AND no fold < −0.001). F1/F2 dead-on-arrival (no 2024 paid closes exist — panel coverage starts 2025; fold-design flaw, noted). F3 +0.00029, F4 +0.00021 (positive, under bar), F5 −0.00145 (freshest fold rejects; MAE-negative all late folds). Pooled −0.00004. KILL. Retrain path EXHAUSTED (spec §7 closure filed): no bundle, no re-sweep, no refit. Program = timing + selection + fills + monitoring. Remaining tracks: WS6-open-timing operational, owner actions (cron register, push, fills, weekly pack). Reopen modeling only on new data or offseason full-season review.
- **TIMING AUDIT 2026-09-10 (#67):** live ledger timing explains the decay finding. Night tickets (n=1,244) bet at median −6.4h, WR 0.564; day tickets (n=448) at −2.6h, WR 0.433 (day also takes more overs: 0.538 vs 0.473 — selection confound labeled). Within-night gradient same direction, flatter: Q4-early (−8.75h+) 0.597 → Q1-late (−4.9h) 0.553 with over-share falling 0.50→0.44. Cleanest evidence stays the same-game decay panel (+0.044→−0.005). WS6-OPERATIONAL PLAN (owner-gated): (a) YOU probe SharpAPI at dawn (~05:00-06:00 ET) for K-prop availability on day games (friend −12h opens prove books post overnight; SharpAPI carriage unmeasured); (b) if yes, Agent adds append-only dawn open-poll (scheduler + WakeToRun, no morning-lock touch); (c) gate dawn-vs-morning WR going forward; (d) if no, day games stay disadvantaged → day-game policy needs its own gate + sign-off. BLOCKERS: SharpAPI quota for extra polls (yours); Novig fill availability at dawn (edge without fills = paper). Self-critique filed: timing implication sat since #31 while features burned 16 kills — operations should have moved first.
- **DAWN PROBE SPEC 2026-09-10 (runs tomorrow ~05:00-06:00 ET, owner):** (1) check SharpAPI quota dashboard first (extra polls must fit); (2) run gate-aware dry-run open poll (`poll_odds.py --snapshot open --unit 50 --quality-gate --dry-run` — fetches, writes preview only, never touches the live send record); (3) record per day-game: K props present? which books (DK/FD)? lines + prices? minutes-to-tip?; (4) report back coverage + times. GO criterion: DK+FD K props visible at −6h+ → Agent builds dawn poll. NO-GO: absent → day-game policy gate instead. Also check Novig fill availability at dawn (paper edge ≠ money).
- **TAIL RECAL 2026-09-10 (#68 TAILS-STAND):** `tail_recal.py` → `tail_recal_report.json` — live 8.5/9.5 maps NEVER held-out tested until now. Verdict: healthy. 8.5 live ECE 0.008 (raw 0.029, 3.6x working), 9.5 ECE 0.002 ties refit exactly; refit slopes ≈1 (tails need no correction — mid-curve overconfidence doesn't extend outward); gains all < bar. "Adjust our tails" answered: nothing to adjust. Next: NB fat-tail challenger (last tails item) → drawdown brake.
- **NB-TBF 2026-09-10 (#69 KILL-final):** `nb_tbf_mixture.py` → `nb_tbf_report.json` — workload-uncertainty mixture (first genuine WS5 test; race only did K|xK). Moment-match finds NO overdispersion (r→500 fallback — the ridge's PA is Poisson-tight). Technical gain +0.00052 BUT flat across r=50..1000 → mechanism falsified: not a fat-tail effect, residual Jensen gap from mixing per se (untunable, 4% above bar). KILL with the sensitivity as the reason (conservative direction; letter-of-rule would've passed). v2 parked unpromised (per-xK-bin heterogeneous dispersion). WS5 closed; spec amended. Tails trilogy complete: recal ✓ / NB ✗ / brake next.
- **BRAKE + REVIEW 2026-09-10 (#70):** `check_drawdown_brake.py` (+3 tests green) → deduped paper track: +12.2u profit, 24.4u max DD, 10.5u current hole → CAUTION x0.5 (shadow; live stakes need sign-off). Slate caps 12u soft/20u hard (p50/p90). Tails trilogy complete: recal ✓ / NB ✗ / brake ✓. REVIEW findings: (a) 9/10 08:30+08:50 tasks MISSED (box off/asleep; no logs) — midday 11:00 covered, self-check RISK paged via ntfy correctly (new fingerprint), NightlyDrift correctly flagged missing (owner setup re-run still open); (b) 9/09 statcast failed but best-effort board still posted 2 unders + failure banner — hardening works as designed; (c) git ahead now 100 paths (owner push backlog grows); (d) L2/L3 fresh thru 9/07, stored L3 sans-kadj as expected; (e) skill gate MET (n_clv 186, beat-close 55.4%, WR 53.8% ≥ 52.4). OPEN: was box off this morning (owner)?; recommendations.parquet found EMPTY at review time (board wrote nothing today — verify).
- **PUSH + CI 2026-09-10 (#71):** 9 family commits pushed (main 18fd67a→54f178c): Savant-untrack, CI-fix, live-ops, nightly-ops, research-infra, ws-probes, retrain/kadj-L3, docs, gitignore-vendors. CI GREEN (296 passed, 6.6s) after months of red. Root causes: (a) statsmodels missing from CI deps; (b) `Models/` vs `models/` case paths (invisible on Windows, fatal on Ubuntu — 5 sites); (c) Sept test debt (registries tuple + pipeline fixture needed Swings). Plus: age/kadj joins degrade gracefully without data files (CI-sim verified), hook lesson (staged-tree testing → src+test ship together). Push per explicit owner request (noted vs never-push default). Remote current. Next: dawn probe tomorrow; distillation or drawdown-brake promotion.
- **HANDOFF 2026-09-10 (#72 chat rollover):** new chat to shed tokens. State: modeling CLOSED (#66), tails trilogy done (recal ✓ #68 / NB ✗ #69 / brake ✓ #70), push+CI green (#71). Live: WS1c + Poisson + veto + CAUTION brake (shadow). Owner queue: dawn probe tomorrow, cron re-register, fills, weekly pack. Next builds: distillation (only unbuilt learn-from-books idea) → day-game policy (dawn-gated) → coverage refresh. Full brief in chat (#72 text).
- **DISTILL-STACKER 2026-09-10 (#73 SURVIVE-challenger):** `distill_stacker.py` → `distill_stacker_report.json` — 4-param logit overlay (model + morning-cons + line), morning-matched universe n=17,338, cut 2026-05-21, held-out n=2,725. Stacker 0.23650 beats model (+0.00651) AND morning-cons (+0.00122, 2.4x bar). Coefs trust morning 2.4x (b2=0.81 vs b1=0.34) but model weight nonzero — conditional trust, not WS1-redux. Per-line beats morning 5/8 (loses hair 2.5/5.5, thin-8.5); ties close-oracle on matched subset (0.23945 vs 0.23917). ECE 0.020 (model 0.043, morn 0.015). HONEST CAVEATS: frozen-config model arm (isotonic-era; live is Poisson+WS1c now — gain vs live likely smaller); 80% market-following (edge-preservation unproven, quant descriptive per #21). No live change. Next: ledger-gate on bettable panel (transfer across configs — pre-register) → weekly-pack → sign-off. SOP §8 +2 rules (ASCII-only scripts, to_numpy astype).
- **STACKER GATE 2026-09-10 (#74 SURVIVE):** `ledger_gate_stacker.py` → `ledger_gate_stacker_report.json` — frozen universe coefs transferred to live-equiv ledger bettable panel (n=981, 2+ books both snaps). Base 0.2472 → stacker 0.2435, gain +0.00362 (7x bar); stacker ties book close (0.2433) on bettable. Cells: over@4.5 gains big (0.254→0.240), under@4.5/under@7.5 hurt slightly. Caught+fixed: ledger's own nullable close_over shadowed the join (drop-before-join rule — same _right-trap family as #51/universe_panel). No live change. PROMOTION PROPOSAL (needs your sign-off): stacker overlay to shadow scoring → weekly-pack confirm → live. Else next: coverage-refresh or day-game policy (dawn-gated).
- **WEEKLY PACK 2026-09-10 (#75 FRESH):** thru 9/09 — veto_4_5_over n=92 ROI +12.6% (CI −8.6/+36.6) vs status-quo n=121 +6.0% (CI −10.9/+24.6): veto validated 4th time, CIs still overlapping (risk control, not proven edge). CLV soft both lanes (veto beat-close 43%, n=37) — ROI green + CLV soft = fills decide. NEW WATCH (not a signal): asym16_plus_veto_4_5 n=94 ROI +24.1% CI entirely positive (+7.7/+40.9) — first all-green lane, but searched-lane lineage (sweep-born) → pre-registered confirm only, never promote-from-pack per anti-patterns.
- **WS2-PROBE RESULT 2026-09-09 (Agent, #25 KILL-narrow):** linear recency re-weighting dead (|corr|≤0.027, +0.007 MAE) — model already absorbs windows. WS2 narrowed to threshold events + age (blocked) + TBF-side. PACE NOTE (user Q): backlog followed in order every turn w/ snapshot; discoveries every step (no circles — see #10→#25 chain); shipping 1+ probes/turn. Next: WS3 TBF probe (biggest error mass).
- **SELF-AUDIT 2026-09-09 (Agent, user Q — critical review):** stored p_ours raw == recomputed binomial to 0.0 (clean). LIMITATIONS FILED: (a) BB kappa fit on actual-PA trials but scored on projected-TBF — train/serve mismatch, BB parked (moot: ≈ Poisson anyway); (b) NB r moment-match hit cap 500 (crude, moot); (c) alt-harness still per-row loops (tech debt; join_universe is the set-based pattern); (d) ECE compared across different n in places — labeled, SOP now requires same-subset. WHY BB ORIGINALLY: Step-5 rate-training likelihood (overdispersed PA trials) + count-layer fat-tail option; live was ALWAYS binomial — nothing to "revert." SOP written (`docs/reference/experiment_sop.md`): panel→bins→contract→small challenger→shadow→gate→promote; replay it for every future feature. DEBUTS PARKED per user (minors too costly): WS2 rescoped to whiff-velocity/recency staleness for established arms + age-gap fill; debut cohort stays metric-only. PROGRESS VERDICT: 8 shipped artifacts (blend×2, harness, cells, race, WS1c, universe panel+join, SOP), 5 killed/parked cleanly, 0 live changes — science is compounding; money still red (xROI −0.07), honestly.
- **LOGIC VERDICT 2026-09-09 (Agent, user Q):** broken SYSTEMATICALLY, not noise — bias monotonic across lines, TBF symmetric ±, debut bias ordered, seasons identical, skill negative everywhere but structured. Structure is good news: every pattern has a named fix. **FAMILY RACE** (`count_family_race.py`, train 6,184/test 2,591, cut 2026-05-21): kappa=93 (mild overdispersion), r_nb capped 500 (K|xK ≈ Poisson). Poisson beats binomial 7/8 lines (mean 0.1398 vs 0.1404; low lines + far tail), BB/NB tie Poisson. Gains are basis points vs book gap 5x bigger → mean error dominates variance error. Verdict: Poisson = zero-param shadow challenger (no live swap without gate + sign-off); WS1c per-line MEAN correction is the bigger lever. Normal/Gaussian: rejected without trial (discrete low-count, bounded-below — wrong tool). Conway-Maxwell-Poisson: parked (Poisson already ≈ truth; complexity unjustified).
- **DEBUT PRIORS CONFIRMED 2026-09-09 (Agent, user Q):** yes — no-prior-season arms fall back to league starter rate (`pitcher_rolling.py:95-139`, prior_strength_pa pseudo-PA). We literally ignore the start and play league-average. WS2 fix: call-up priors (minor-league translation) + live whiff-velocity so one start of real data can override the prior.
- **CLOCKS + RAW-vs-CAL 2026-09-09 (Agent, user Q):** morning = commence−5h exactly (`pull_oddsapi_historical.py:174`): night game (19:05 ET) → ~14:05 ET; day game (13:05 ET) → ~08:05 ET. So "morning" = T-5h decision window (post-lineups mostly, pre-pitch always). Friend open = −12h → ~07:05 ET same-day (overnight lines). Close = −5min. LINEAGE GAP: normalized `snapshot_ts` column is EMPTY (request ts lives only in raw JSON cache) — queue normalizer fix or drop column. Raw-vs-cal on universe (n=19,533 vs closes): raw Brier 0.2235 → cal 0.2221 (helps +0.0014, story unchanged); ECE raw 0.023 → cal 0.012 (halved). REFRAME: calibration works on unselected data — the 0.159–0.20 ECE disasters were BET-selected tickets. Disease = selection × mid-curve bias, not the calibrator.
- **REMEDIATION LIST 2026-09-09 (Agent, user Q — the fuck-up fixes, ordered):** (1) Selection: veto stays; add early-timing doctrine — score/edge on morning numbers, never chase close; WS6 steam work decides how early. (2) Calibration: keep isotonic-20260821 live; scope universe-refit challenger (chrono gate, no live swap without sign-off). (3) Mid-curve bias: WS1c slope/stacker correction aimed 4.5–5.5 (bias +4.6pp @4.5). (4) Staleness WS2 (breakout/age features, now powered at n=70k). (5) TBF two-stage WS3. (6) Lineage: snapshot_ts fix + precomputed consensus parquet. (7) Run pre-registered cells (#17) NOW — panel ready.
- **OPEN-TIMING AUDIT 2026-09-09 (Agent, user Q):** three distinct clocks confirmed — friend open = commence−12h/−6h (`pitcher_strikeouts_early_open_2025_2026.csv`, 26,147 rows, 2025-03-27→2026-07-10, 9 books) → paid morning = commence−5h → paid close = commence−5min (`pull_oddsapi_historical.py:174`). Same vendor ID space (2,928/2,964 friend events overlap paid). Join coverage friend→paid 90.5% morning / 82.0% close on event+player+line+book; prices move hard intraday (only ~26% identical open→morning, ~14% open→close; mean |move| ~25c/40c) — timestamps carry genuinely different info. Enables open→close decomposition + WS6 steam work + morning-as-decision-time proxy. Stacker verdict (user Q): xK-residual stacker = legitimate WS1-generalization IF decision-time features only + linear/L2 + chrono gate + kill; CLV-prediction = descriptive-only (WS6), never a live selector without gate (steam-chasing risk). SCOPED ONLY (WS1c), no code until WS1b runs.
- **MARKET-COVERAGE AUDIT 2026-09-09 (Agent, user Q):** friend opens = K + outs ONLY (strikeouts 26,147 rows/302 games; outs 12,920/303; both 2025-03-27→2026-07-10, opens only). Paid pull = morning+close for 6 pitcher markets (K, outs, hits-allowed, ER, walks, K-alt) + 9 batter markets (hits, HRR, HR, RBI, runs, singles, Ks, total bases, walks). True −30h vendor opens pulled for NOTHING. So: open→morning→close full lifecycle exists only for K/outs; all batter + other pitcher markets have morning→close only.
- **FUNNEL + BOOK-RANK AUDIT 2026-09-09 (Agent, user Q):** paid K-close universe = 26,117 distinct props / 4,459 events / 9 books; consensus-eligible (2+ books) = 9,964. n=1,049 is NOT the data — it is our 6-week paper history (ledger settled 2026-07-30→09-09, DK+FD only) intersected with closes. 25x unlock SCOPED: score frozen bundle over 2025-2026 historical slates → full 26k-prop model-vs-book panel, research-legal (post hoc; train seasons 2023-24). Needs historical projection run; no spend. Book softness (fair-over bias vs median): DK +0.0027 shades-over, Fanatics −0.0020 shades-under, rest ~0; deviation leader BetRivers 0.0122 (suspect alt-line bleed: 24,666 props vs ~8k/book — QA flag, consensus median robust). Our paper CLV by fill book: DK +0.33pp/50.8% (n=490) vs FD +0.12pp/41.1% (n=470) — DK softer, consistent with shade direction. williamhill_us thin (795 props). Question made complete sense; book choice is a real lever (line shopping), second to model error.
- **WHY-SMALL-SAMPLE + ALT-MULTIPLIER 2026-09-09 (Agent, user Q):** our probs exist for 39 games only (graded 1,056 rows, 2026-07-28→09-09) vs 4,459 book events — the join, not the data, is small. We already emit p_over at 2.5–9.5 (+cal variants); books' alt market offers 0.5–14.5 (78,275 props) at a modal 9–10 lines/start → ~9x eval points per start wherever our xK exists (curve-shape test, not just one line). Main market lines run 0.5–10.5. Adjustments queued: (a) WS1b morning-blend; (b) historical projection run → 26k-prop panel (feasibility check first: 2025-2026 feature rebuild); (c) alt-line curve eval (Brier/ECE per line cell across full curve); (d) learn-from-books distillation SCOPED — consensus-as-teacher (morning only, never close), alt-curve shape matching, WS1c residual stacker; all chrono-gated, shadow, killed on held-out Brier.
- **Live betting stance is promoted**, not shadow-only: recommendations should HOLD/skip **4.5 overs** (`veto_4_5_over`); 2.5/3.5 overs on soft probation (higher edge floor 0.18).
- Unders stay on the existing floor; **no stake-up**; **no live calib swap**; **no monotone champion swap**.
- **Holy-file cleanup:** backlog is explicitly master (`AGENTS.md` + `.cursor/rules/execution-backlog.mdc`; subordinate docs redirect here).
- **Repo-quality passthrough:** expanded `.gitignore`; skill/reference/READMEs/diagrams retargeted to this backlog; hold inventory at `docs/reference/reports/repo_quality_hold_inventory.md`. Local caches wiped. Resume/manuscript HTML+PDF **kept**. No production deletes.
- Working thesis unchanged: binding constraint was **toxic over lines / side error**, not “need another floor sweep.” Veto is **risk control** (bootstrap still wide) — keep measuring weekly.
- **Publish gap:** local `main` ahead of `origin` — push still waiting on you.
- **2026-09-02 Mac handoff (DONE on Lenovo):** `.cursorignore` + `.clineignore` + refreshed `.cursorindexingignore` + `.cursor/rules/agent-context.mdc` + `AGENTS.md` pointer. Agents should stop eating data/artifacts. Harness migrate (Cline/DeepSeek) still parked.
- **2026-09-02 historical CLV API research (DONE):** report at `docs/reference/reports/historical_clv_odds_apis_2026-09-02.md`. Verdict: free SmartStake HF (2026 Mar–Jul) → OddsPapi smoke → paid burst **the-odds-api.com** (~$59 / 100k credits) for full 2025–2026 open/close K props → parquet → cancel. Skip SharpAPI Enterprise / OddsBlaze / SportsGameOdds for backfill. Live path stays SharpAPI.
- **2026-09-04 INCIDENT (RESOLVED):** zero bets all day — alerts fired but no recs. Root cause: `MLBProps_MorningWorkflow` **missed 9/4** (`Last Result: 1`, `Next Run` slipped to 9/5; `Logon Mode: Interactive only`). Midday `run_market_refresh` never advances projection log / statcast, so `odds_board.py` scored the stale **09-03** slate vs 9/4 quotes → `matched=0`. Compounding: statcast/features only through **09-02** (2-day gap) blocked a 09-04 build. Recovery (DONE): refreshed statcast→09-03, Level 1–2 features, logged 09-04 slate (32 starters), regenerated board → **4 BET** (Javier U4.5, Gilbert O6.5, Snell U7.5, McLean U6.5), wrote open to ledger, sent alert. Automation self-check had already flagged `last_result: 1` (risk). See FORWARD #8 for reliability hardening (task is the single point of failure).

### FORWARD — plan looking ahead (ordered)
> **Pick up here next session.** Owner tags: **You** = human; **Agent** = do when asked / next coding turn.
>
> **Cursor 2026-09-10 pickup box (this wins over numbered items 10–17 below AND over every `#NN` diary bullet in this FORWARD section).** Items 10–17 and #66–#110 are historical DONE/KILL. Do **not** start a shrink-to-consensus blend, a 25x hist-run, a harness re-judge, a live floor/veto retune, or Streamlit.
>
> **OpenCode next coding job:** none until owner names it. Paste `docs/reference/opencode_handoff.md` (rewritten 2026-09-11). Pack #113 steps 0–4 built. Juiced #123 measured. Totals #124 pulled. Dashboard parked. Cite `golden_metrics.md`. Do not treat the rest of this FORWARD wall as a queue. Do not retune floors from juiced 2026.
>
> **Odds API replay (user-unparked 2026-09-11 — #121/#122 DONE, #123 MEASURED, #124 polish+totals DONE):** lake bought. Spec: `docs/reference/oddsapi_replay_architecture.md`. Ledger: `juiced_replay_ledger.py`. Dated report: `docs/reference/reports/juiced_replay_ledger_2026-09-11.md`. Canonical book = next-US-book (owner). Do **not** retune floors/veto/Kelly from 2026 confirmatory ROI. Do **not** 5-min-tick. Nested juiced v2 remains post-9/27. Featured totals pulled (`featured_totals.parquet`, 220k rows / 4,749 events / 363 days, ~7.3k credits). Next: stop policy coding on this ledger unless owner names a specific floor/veto change.
>
> **NEAR-TERM BUILD ORDER (owner-locked 2026-09-11 — step by step, no skipping):**
> 1. Ops hygiene, $0, always allowed: settle post-game only → weekly pack + stacker gate → closeout re-runs thru 9/27 → tests.
> 2. Owner minutes: NightlyDrift register → dawn probe (carriage + lineup-freshness + quota + fills card) → Novig fills habit → `git push`.
> 3. Display-first logging (one-liner each, no stake change): morning edge-cap flag → robust-refusal flag as a named veto-reason → offset-share flag (already live via #113.2).
> 4. Shadow arms only: stacker stage 2 → offset cap 0.02 as shadow → brake stays shadow.
> 5. October post-9/27, in order: totals join → slate correlation caps research → nested juiced v2 (2025-select / 2026-judge once, consensus-morning-line grouping pre-registered, White/PBO over config count) → optional −30h K open (~47k).
> 6. Enforcement of any new filter only after the October judge + sign-off. On the consideration shelf (not queued): per-book fill-rate model, slate-clustered bootstrap as standard, calibration-drift monitor, day-game policy gate (dawn-gated), versioned model+policy cards.

**You — open sign-offs (parallel, not a queue):**
1. Stacker stage 2 (shadow `p_stacker`) vs wait (#76; stage 3 needs 2 green weekly confirms).
2. Paper figures / PDF re-render (manuscript ready).
3. Confirm 2.5/3.5 probation freeze (data rec: freeze; owner signs).
4. Dashboard v1 unpark (parked until owner says go).

**You — ops (minutes):** NightlyDrift register (elevated setup also activates WakeRecovery) → dawn probe → fills habit → game-cap key (`game_cap_max_bets: 1`).

**Agent — always allowed:** settle post-game only → weekly pack + stacker gate → closeout thru 9/27 → tests → snapshot write-back. No live BET-set change without a named order. No floor/veto/Kelly retunes on 2026 data. No retrains (DEFERRED).

**October (post-9/27, in order):** runbook `docs/reference/october_pickup_checklist.md` (closeout → September judge → v2 execute → re-judge → slate caps → stacker → re-gates → cloud → paper → money truth). V2 executor refuses peeked selection mechanically.

**Phase 10 sequenced workstreams (census DONE; owner-ordered 2026-09-17 — registry: `docs/reference/pipeline_registry.json`):**
> Owner order: outs is the first adapter. September safety package allowed (DST correction, freshness gates, idempotency, monitoring); model, policy, calibration, betting-threshold changes prohibited before October. Extract shared code only with outs proving the contract.
1. `TEST-1` five dangerous invariants (probation→lean order, double-settle, close denominator, training PIT, live poll books) — first; tests-only, safe anytime.
2. `OPS-1A` timezone-aware Modal Cron (`timezone="America/New_York"` + DST acceptance tests) — needs named order (touches schedules).
3. `DATA-1A` hard freshness/schema gates (`SUCCESS_FRESH` / `DEGRADED_NO_PAGE` / `FAILED`) — needs named order (blocks paging/ledger).
4. `OBS-1` shared run manifests + no-bet reason codes — low-risk instrumentation.
5. `OPS-1B` notification idempotency (shared key, ledger-recorded) + laptop/cloud ownership — needs named order.
6. `EVAL-1` canonical eval engine + population identities + artifact header — parity tests first, October.
7. `SIM-1` live-parity replay, then policy sim (modes separated; close enters post-decision only) — shadow-only, October.
8. `NB-1` thin notebooks (deletion only after eval engine exists) — per-file owner eyes.
9. `ARCH-1` generate diagrams/catalogs from registry — docs-only.
10. `PROP-1` strikeout-adapter extraction + pitcher-outs shadow (12-gate checklist) — design + contract tests; no execution before shadow path + gates.
11. `DATA-1B` lineage, retention, correction/regrade policy.
12. `SIM-2` execution + correlated-bankroll simulation (multiple scenarios, never one flattering assumption).
Deferred: external registry (until manifests prove insufficient); plugin framework (until outs validates the interface).
**Locked sequence 2026-09-17 (owner):** smoke (08:00 board + 09:00 hourly + manifests) → DATA-1A (adapted) → commit train (scoped, attributable workstreams only; pre-existing tree gets its own inspection pass first) → pre-commit/CI extensions → Hypothesis property tests on the two canonical modules → capped mutation run (one overnight per module, then close the book) → SLO-1 recorder (folded into drift/grading chain, record-only 2 weeks, no thresholds/alerts yet) → EVAL-1. Shelved: DORA (until commits flow for weeks; lowest value), CI-deploys (one atomic offseason move), uv restructure (declined this month; lockfile-only shelf item).

**Commit-train manifest (prep 2026-09-17, DO NOT execute before smoke clears):** one commit per group, pytest on affected suites green before staging, `git add -p` where noted, conventional messages. Excluded throughout: CRLF noise, cutover leftovers, research scripts, notebooks, backlog living edits (snapshot separately).
- T-A `docs(canonical): rebuilt-panel truth + schedule docs` — README.md, manuscript.md T3 row, diagrams 00/04, golden_metrics.md, model-card.md, cloud README timing table. Verify: stale-claim grep clean.
- T-B `chore(ops): NY-local Modal crons` — modal_app.py schedule hunks ONLY (`-p`: CRON consts, SCHEDULE_TZ, 5 decorators, close-sweep docstring, IMAGE e bump), tests/test_modal_schedules.py, pipeline_registry.json (new file, whole), canonical-map pipeline section (`-p`). Verify: modal tests + AST check.
- T-C `feat(obs): run manifests` — run_manifest.py (new), test_run_manifest.py (new), drift + alert manifest hunks ONLY (`-p`, anchor `OBS-1 run manifest`). Verify: manifest tests.
- T-D `test(grading): population-truth guards` — test_gc_population_truth.py (new). Verify: file green.
- T-E `test(board): safety invariants` — test_safety_invariants.py (new). Verify: file green.
- T-F `test(market): property tests` — test_properties.py (new), pyproject `dev += hypothesis`. Verify: file green (12 tests, ~10s).
- T-G `chore(agents): repo-garbage-collector skill` — .agents/skills/repo-garbage-collector/ (new dir). Verify: no imports from prod/src.
- T-H (deferred, own inspection pass): everything else in the dirty tree. Not part of this train.

**DATA-1A adapted design (prep 2026-09-17, build after smoke):** new `src/Python/serving_gates.py::assert_serving_fresh` raising `ServingStaleError` (no catch, no warn-fallback). Inputs from real files: `recommendations.parquet` frame + recs meta (`built_at_utc`, `edge_floor`, policy signature), `sharp_quotes_latest.parquet` mtime, `kpi_policy.json` + calibrator/model pointers. Checks: board age within cadence+slack (hourly-aware, not a flat 10 min); quotes file fresh; quote rows present when candidates exist; pin agreement (policy/model/calibration versions meta-vs-disk); DK+FD coverage; slate identity match. Choke points: `poll_odds` before ledger write + `send_morning_alert` before page (both raise → no write, no page). Manifest distinguishes `FAILED` (bad data) from `SUCCESS_NO_BETS` (valid). Tests: fails_on_stale_board / fails_on_missing_quotes / fails_on_pin_mismatch / passes_on_valid / raises_not_warns. No schema library.
Enforcement (owner 2026-09-17): gates ship ENFORCING by default, with a tested, documented env-var observe-only switch (`GATES_OBSERVE_ONLY=1`) as a rehearsed kill switch — not a shadow phase. Rationale: a day-one false positive in the alerter costs a slate and teaches distrust; absence-of-page is itself observable, so a real false-block shows immediately. Morning manual check of gate behavior the first two days.
13. `OPS-1C` Windows-scheduler DST/timezone findings (audit only 2026-09-17, zero scheduler edits): triggers use local `-At` (wall-clock stable across DST; no SynchronizeAcrossTimeZone flag in setup); StartWhenAvailable + WakeToRun + battery set (missed-during-sleep runs catch up; powered-off/hibernate cannot wake — documented in setup script); machine-timezone changes shift execution (travel risk, unmonitored); laptop tasks disabled since 9/16 cutover so no dual-paging today (re-enable without NO_ALERT reopens G-OPS-2). No action unless owner orders; laptop retires at cloud cutover anyway.

**On the shelf (not queued):** per-book fill-rate model, slate-clustered bootstrap standard, calibration-drift monitor, day-game policy (dawn-gated), versioned model+policy cards, alt-line measurement (next build), disagreement flag (display-only proposal), GC follow-ups (Python inline-dupe consolidation to canonical helpers, notebook output-strip + chart-label polish, `agentic_stack_final_plan_updated.md` disposition — all evidence-filed, need owner eyes).

> Archived 2026-09-14: sign-offs history, #113 steps, #66–#120 diary, items 10–17 → `docs/reference/reports/forward_diary_archive_2026-09-14.md`. That file is history, not a queue.

### Waiting on user
- [x] ~~**`go step 3`**~~ DONE 2026-09-11 — #113.3 shared SharpAPI fetch + #113.4 regression pack green. Pack waits on first live 8:30 board carrying steps 2+3.
- [x] ~~**Offset cap 0.02 / keep / remove**~~ SHIPPED live 2026-09-11 (owner order).
- [ ] **Stacker stage 2 or wait** (#76) — shadow columns only until you say stage 3.
- [x] ~~**Morning edge cap ~0.20**~~ SUPERSEDED — champion carries cap 0.24 live (2026-09-11 promotion).
- [x] ~~Paper modeling-half headlines~~ DONE 2026-09-10 — live stack (Poisson, WS1c, veto, universe n=19,533) in abstract / Table 3 / §8.5 / resume; betting-half still demoted / non-claim.
- [ ] **Paper figures / PDF** — say go to regenerate `docs/paper/make_figures.py` after you read the new §8.2.3.
- [ ] Register `MLBProps_NightlyDrift` (`setup_automation_tasks.ps1`).
- [ ] Dawn SharpAPI probe ~05:00–06:00 ET (quota first).
- [ ] Item 6/#12 real-bet prices / Novig fills (≥50 before any money-edge claim; **first fill 9/14 Mathews — log price/stake**).
- [x] ~~`git push origin HEAD`~~ remote current (9/14 commits pushed, CI green).
- [x] ~~**Juiced #123 follow-ups**~~ ANSWERED 2026-09-11 — (1) canonical = next-US-book fallback (DK+FD sensitivity only); (2) polish then stop policy on this ledger; (3) featured totals GO (~7.3k credits); (4) live retune from this run **refused** (needs a named floor/veto change + written 2026-peek acceptance). Report: `docs/reference/reports/juiced_replay_ledger_2026-09-11.md`.
- [ ] Confirm 2.5/3.5 over style — **data rec 2026-09-11: freeze current probation** (skip-all is noise; 2.5-over thin-red; 3.5-under thin-red; do not half-size live). Owner still signs.
- [ ] **Dashboard v1** — parked until backend #113 is green; say go to unpark.
- [x] ~~CLV $119 pull decisions~~ DONE 2026-09-08 — cache 70k+; do not re-buy.
- [x] ~~Smoke-check 4.5-over HOLD~~ live (`veto_4_5_over` on Feltner 9/10).

### DEFERRED — approved or parked, but not next
> Agents: **do not start these** to look busy or to avoid ops work. Move an item DEFERRED ↔ FORWARD only when the reopen condition is met **or** the user explicitly asks.

| Item | Status | Why deferred (standing instruction) | Reopen when |
| --- | --- | --- | --- |
| **Item 13 — Local MLflow v1** | APPROVED · DEFERRED | **Wrong tool for this ops cycle.** Helps train/Optuna run compare, not freeze/null/CLV/veto decisions. Do **not** install or scaffold MLflow mid-ops. Never built this cycle. | User resumes Strikeout/TBF train or Optuna, **or** explicitly asks for Item 13 |
| Champion → monotone swap (#9) | EVIDENCE DONE · swap gated | Softens under bleed only; does **not** fix 4.5 overs. No champion-file edit without sign-off. | ≥2–3 settle weeks under live veto **and** unders still soft **and** user signs off |
| Live calib promote (Item 14 research done) | RESEARCH DONE · not live | Open holdout still < market; picking by post-freeze ROI is an overfit trap. Do **not** edit live calibrator. | Holdout Brier skill clears market **and** user signs off a chrono-safe promote gate |
| Asym over-floor live promote | SHADOW ONLY | Shadow-green but may impute stakes; narrower 4.5 veto is first live move. | Weekly pack + **real** stakes support it; user signs off |
| Hard-veto all overs ≤3.5 | PARKED | Starves learning; bootstrap wide. | Pre-registered n + CI gates + user ask |
| Broader model retrain / count-layer 4.5 fix | PARKED | Only if weekly pack says **probs** (not selection) remain the bind after veto. | User asks **or** over Brier skill stays deeply negative with veto on |
| MLflow v2 / W&B | PARKING LOT | After Item 13 only. | Item 13 done + multi-run train loop exists |
| SSAC future-work 9–13 | PARKED | Post-abstract depth; not ops path. | After abstract / user ask |

**Anti-patterns (agent must refuse unless user overrides):**
- Starting MLflow, Optuna sweeps, or "just one more floor search" during a live-betting / settle-pack week.
- Refitting calibration on the post-freeze evaluation window.
- Silent edits to `KING_PROFILE`, floors, staking, or champion without explicit sign-off.
- Promoting a deferred item because it has a green shadow ROI on thin n.

## Strategy Plan — 2026-09-01 (for user hole-poking)

**North star (your words):** improve **ROI**, improve **CLV / beat-close vs books**, and show a **clear positive delta over sportsbooks** — without laundering policy-search evidence as OOS edge.

**Working thesis:** The binding constraint is not “log trains in MLflow” or “sweep another 5k blend×floors.” It is **post-freeze decision policy under a side/line failure mode**, plus **honest market-skill measurement**. Post-freeze KING floor (n=74, 2026-08-22→08-31): ROI **−1.55%**, but **over −24.6% (n=45)** vs **under +29.3% (n=29)**; CLV mildly green overall (~59% beat-close on n_clv=29) while overs can show OK CLV and still lose money. That pattern says: **selection/side error, not “need a higher universal edge floor.”**

Evidence anchors: `docs/reference/reports/postfreeze_pack_2026-09-01.md`; live extract on deduped ledger (same lane). Hot spots: **4.5 over** n=18 ROI ≈ **−41%** WR 33%; **3.5 over** n=17 ROI ≈ **−6%**; unders on 4.5/5.5 look strong but small-n. Higher |edge| overs still lose → not fixed by “only bet monster edges.”

### The nine decisions (scenarios → recommendation)

| # | Question | If we maximize ROI/CLV/book-delta… | Recommendation |
| --- | --- | --- | --- |
| 1 | **Null lane** | A weak null that “KING beats” can fake a story. A harsh matched null that KING loses can kill a real side edge. | **Paper: illustrative / non-claim.** Redesign later as side×line stratified null on the *same* post-freeze opportunity set with real stakes only — only if we want a paper control, not to drive staking. |
| 2 | **Marcel vs Table 2a** | Same-fold ΔMAE helps the *modeling* SSAC half; it does not move CLV/ROI. | **Keep Table 2b non-subtractable** for now. Optional same-fold re-score = paper polish queue, not ops critical path. |
| 3 | **Dual Sharpe / DSR rebuild** | Rebuilding DSR on replay Sharpe cleans footnotes; it does not create edge. | **Live with documented freeze + one manuscript paragraph.** Rebuild only if we re-elevate betting-half claims (we should not). |
| 4 | **Post-freeze interim ops** | Flat 0.12 floor is funding the over bleed. Dual floors in governance (`over=0.10/under=0.08`) point the **wrong** way vs post-freeze data. | **Interim (shadow first, then promote only with gates):** (a) **pause or hard-veto 4.5 overs** (and treat 2.5/3.5 overs as probation); (b) **raise over floor** (candidate 0.16–0.18) while **keeping under at 0.12** (do not ease unders on 10-day luck); (c) **no stake up**; (d) prefer waiting on **real-ticket n** for money conclusions. Write this as a dated interim note; do **not** silently edit `KING_PROFILE`. |
| 5 | **DSR on N=5161** | Using DSR to “prove” post-freeze edge is a category error. | **Framing only:** DSR = selection-breadth diagnosis on *policy search*. Post-freeze KPI = ROI/CLV/side with CIs. No retune from DSR. |
| 6 | **Calibrate further?** | Isotonic already in the frozen stack. Recalibrating on post-freeze tickets = leakage into the evaluation window. Overs losing with sometimes-OK CLV smells like **wrong side / line**, not globally mis-scaled probs. | **Diagnose before recal:** post-freeze reliability + Brier/logloss skill **vs market** by side×line. Only open a *new* calibration challenger on a **pre-registered chrono split** that excludes the test window. Default: **no live recal this week.** |
| 7 | **Change floors?** | Universal floor ↑ cuts volume and may not stop 4.5-over toxicity. Universal floor ↓ dumps more bad overs. | Prefer **asymmetric / line-aware gates** over another global sweep. Run as **shadow ledger metrics** for 1–2 weeks; promote only if: post-freeze (and expanding) ROI improves **and** full-cohort CLV does not collapse **and** n remains usable. Mixed `runtime_floor_calibration.csv` (from 2026-07-31) is **contaminated** — do not re-pick 0.12/0.14 from it as “OOS truth.” |
| 8 | **Various lines** | 4.5 over is the clearest bleed; low-line overs fragile; mid/high unders small-n green. | Attack order: **(1) veto/restrict toxic over lines**, **(2) measure whether under edge survives without over subsidy**, **(3) only then consider line-specific models or count-layer fixes for low lines.** Don’t average all lines into one KPI. |
| 9 | **Where weak / how attack** | Weak: overs (esp. 4.5), policy-search contamination, thin CLV n, no ≥50 real tickets, champion vs monotone still unsigned. Strong: under pocket, mild CLV, modeling audit trail. | Attack sequence below — **policy & measurement first**, model retrain later, MLflow last. |

### Proposed attack sequence (maximize ROI/CLV without self-fooling)

**Phase A — Freeze the lies (0–2 days, mostly docs) — DONE 2026-09-01**  
- Manuscript: null = non-claim; DSR = search diagnosis; post-freeze table stays “not a claimed edge.”  
- One-page **Interim Post-Freeze Ops Stance** (dated): veto/probation rules above; stake flat; KING file untouched until you approve promotion.

**Phase B — Measure the failure mode (2–5 days, analysis) — DONE 2026-09-01 (initial)**  
- Post-freeze slice: side × line × |edge|: ROI, WR, CLV; Brier skill vs market.  
- Shadow score asymmetric floors / 4.5-over veto on the same opportunity universe (no live change yet).  
- Re-run weekly until promote/reject.

**Phase C — User-gated levers (when you decide)**  
- **4.5-over hard veto — PROMOTED 2026-09-01** (live quality gate + scorer); soft probation on 2.5/3.5 overs. Keep measuring via weekly settle pack.  
- **Real-bet prices (#12):** only path to money-truth ROI; prioritize over any sweep.  
- **Champion→monotone (#9):** evidence said unders bleed softens, overs unchanged — aligned with “unders OK / overs broken,” but does **not** fix 4.5 overs by itself. Explicitly deferred unless weekly pack reopens it.  
- Broader asym floor promote still gated: trailing post-freeze n, ROI vs status quo, CLV floor, max over exposure.

**Phase D — Model attacks (only if Phase B says “probs are wrong,” not “we bet the wrong side of a fine price”)**  
- Low-line / over-specific residual analysis; TBF/count-layer stress on 4.5; optional monotone path.  
- **Then** MLflow (Item 13) when that retrain loop starts.

**Phase E — SSAC abstract**  
- Lead modeling + honest demotion. Betting half: post-freeze transparency + no edge claim until real-ticket and/or powered OOS clears.

### Explicit non-goals (this cycle)
- Another 5k blend×floor search to chase ROI.  
- Recalibrating on the post-freeze window.  
- Stake-up on unders’ 10-day heater.  
- Treating mixed pre/post floor tables as OOS.  
- MLflow as a substitute for ops decisions.

### What I want you to poke holes in
1. Is **vetoing 4.5 overs** too aggressive (kills CLV sample / learning) vs only raising over floor?  
2. Should unders’ floor stay 0.12 or **tighten** (protect CLV) rather than “keep”?  
3. Is champion→monotone a **parallel** lever or a distraction until line veto is tested?  
4. Minimum evidence to promote shadow→live (my strawman: ≥3 weeks post-freeze **or** ≥40 shadow tickets, ROI≥0 vs status quo, CLV≥0.52 full-cohort on the gated set)?  
5. Anything I under-weighted (park factors, starter role, juice, book mix)?

## Execution Order

1. **[APPROVED] Commit the current green working tree** (test fixes: `test_pipeline.py`, `test_pitcher_features.py`, `test_registries.py`, `test_train.py`; new observability files from the daily-improvement session). Clean baseline before anything else touches the repo.
2. **[BLOCKED — verified no-op 2026-08-27]** Split/commit `close_watcher.py` (lock hardening vs. aux-market-probe). Verified: working tree is clean, the aux-probe + late-open features are already buried together inside the broad commit `de942c3` (183 new lines in close_watcher.py among 27+ files), the pre-split backup `.py` is gone (only a stale `.pyc` remains), and there is **zero uncommitted delta to split**. The split would require a destructive rebase/filter of already-committed history — not doing that without explicit sign-off. Treat as effectively combined already; resume only if user explicitly wants the `de942c3` history rewritten.
3. **[DONE 2026-08-27 — commit 3947a7a]** Widen CI (`ci.yml`): py_compile all 60 production scripts (dynamic find, was 6 hardcoded), full 270-test suite (was 2 files), install pyproject deps `.[dev]`+scipy (was a no-op `requirements.txt` that never installed polars). Validated: 60/60 compile, 270 tests green, YAML OK.
4. **[DONE 2026-08-27 — commit 2d27ef3]** Dedupe-to-best-line as shared infrastructure. Fixed canonical `dedupe_ledger_props` to key on `(game_date, player, line, SIDE)` (was missing side — kept over/under of the same line merged, a latent bug; 22 such props in data). Wired the one canonical function into the 3 scripts still aggregating raw rows: `line_policy_settled_lookback`, `clv_basis_reconcile`, `build_automation_self_check`. Added 2 tests (over/under kept separate; best book per side). Validated: 272 tests pass; real-ledger settled 1118→643 props.
5. **[DONE 2026-08-27]** Regenerated every previously-cited number on the deduped ledger (old vs new, same ledger, apples-to-apples):
   | Metric | OLD (raw) | NEW (deduped) |
   |---|---|---|
   | Settled tickets | 1118 | 643 |
   | All-time PnL | +$576.15 | **+$84.51** |
   | All-time ROI | +2.08% | **+0.51%** |
   | Stake volume | $27,635.75 | $16,725.07 |
   | Skill-gate n_clv@floor12 | 248 | **150** |
   | Skill-gate WR | 0.534 | 0.5334 |
   | CLV beat>0 | 46.5% | 55.5% |
   | Line 4.5 all ROI | −26.4% (n=413) | −22.2% (n=244) |
   | Line 4.5 profile(≥.14) ROI | −33.3% (n=76) | −34.1% (n=52) |
   | Line 5.5 over ROI | −59.1% (n=22) | −61.8% (n=13) |
   **Headline: the all-time "green" was +2.08%/+$576 on raw; on the honest deduped ledger it is +0.51%/+$84.51 — barely above breakeven. Skill-gate n falls 248→150 (still met) and WR is essentially flat (0.534→0.5334); the edge shows in CLV, not raw WR.**
6. **[DONE (infra) + BACKFILL BLOCKED on operator prices — 2026-08-27, commit 1da4c82]** Built the append-only real-bet ledger: `src/Python/real_bets.py` (separate from paper-sim `ledger.parquet`; decision-time `bet_price`/`stake` snapshot; `append_real_bets` returns `(frame, n_appended, skipped_ids)` so no ticket can silently drop — the 8/24 failure mode); `tests/test_real_bets.py` (6 integration tests proving idempotency, in-batch dup reporting, re-append never-silent, real≠paper path, summary math, stable schema); `production/ops/backfill_real_bets.py` (holds the 8 confirmed identities Valdez U4.5 W / Suarez U4.5 W / Messick O6.5 L / Skenes U6.5 W / Melton O3.5 W / G.Rodriguez O4.5 L / Cantillo U6.5 W / Sasaki O4.5 L; refuses to write rows lacking a real decision-time price/stake — reports instead of fabricating zeros). **SATISFIES THE INTEGRATION-TEST REQUIREMENT.** Backfill WRITE is BLOCKED only on the per-ticket decision-time `bet_price`/`stake`/`pnl` (and the 9th ticket's identity+prices) which live in the user's records — fill `REAL_TICKETS` in the backfill script and rerun; complete set is 6W–3L, +$109.80/$455.
7. **[BLOCKED — no script to rewrite 2026-08-27]** CUSUM `corr_before`/`clv_before` null bug. Verified: the CUSUM/regime **regeneration script does not exist anywhere in the repo** (no source for `regime_model_report.json` / `corr_after` / `clv_after` / `cusum`; only the artifacts remain, and it's likely a deleted one-off probe like `_probe*.py`). Diagnosis captured from the surviving artifact for when the script is restored: `corr_before: null` despite `edge_win_corr_break_pos: 253` (rolling_window 40) and `clv_before: null` at `clv_break_pos: 191` — i.e. the pre-breakpoint sample window is too small/empty, so the "before" stat computes null. The rewrite needs a min-sample guard that emits an explicit `null`/`insufficient` with the n instead of an unlabelled null. Resume when the script is restored or the user provides it.
8. **[DONE (in-repo sweep) 2026-08-27 — external network call remains outside agent scope]** External-outcome recovery sweep. Verified from the ledger and stored artifacts:
   - **8/24 gap is isolated to a single day, not systemic**: `ledger.parquet` has daily rows through 8/20–8/23 and 8/25–8/27 but **0 rows on 2026-08-24** — an isolated one-day ingestion hole in an otherwise-continuous record.
   - `open_projection_quotes_raw`/`open_quotes_canonical` **end in early July entirely** (last rows ~2026-07-10/11), so they never reached August — their "0 rows for 8/24" is trivial; not the right source for recovery.
   - **Melton's real 8/25 O3.5 line is not in the paper ledger** (only other Melton dates/lines are: 8/04 U5.5, 8/15 O5.5, 8/21 O4.5, 8/26 O3.5) — same class of real-ticket data gap as 8/24.
   - **Real-ticket players are present in the paper ledger on different dates/lines** than their real bets — confirming real (real_bets.parquet) vs paper are correctly separate tracks.
   - **Recovery action**: both gaps are isolated ingestion/record flat-spots, so a targeted external odds/score call (not a systemic rebuild) is warranted; executing that network/API call is outside what the agent can run here. 8/24 outcomes are already user-confirmed (PnL not blocked); this is an auditability/completeness backfill only. Side pattern re-confirmed: all 3 real losses were Overs (Messick, G.Rodriguez, Sasaki).
9. **[EVIDENCE COMPLETE 2026-08-27 — swap still gated on user sign-off]** Champion-vs-monotone swap evidence package. The side-conditional breakdown is already fully built in `artifacts/odds_log/_audit_step4_monotone_sidebreak.json` — and it was **never actually blocked on #4-#5**: it operates on the 2025 model open universe (`config.PITCHER_TRAINING_PATH`) with its own `(pitcher+line+date)` dedupe, NOT the 2026 settled paper ledger that #4-#5 dedupe. That BLOCKED-PENDING-#4-#5 status was a false dependency. Evidence verdict (clean, complete):
   - **LODO robustness: passed** — 0/55 date-drops flipped monotone's advantage (`flip_dates: []`), pooled Brier margin +0.0135 for monotone.
   - **Overs: no change** (n=896; WR 0.4766 / ROI@fair 0.1067 identical; calib −0.0307→−0.0301).
   - **Unders: monotone improves calibration (+0.0073 on shared n=835) and reduces the unders bleed — but does NOT make it positive**: deduped under ROI@fair −0.0272 (champion) → −0.0166 (monotone). Softens but does NOT obsolete the unders-floor problem.
   - **Verdict**: swap is coherent and robust, but unders-floor still requires its own decision (not obsoleted). Per Standing Rules, the actual champion swap is NOT an approved action — it needs explicit user sign-off after this evidence is reviewed.

## New Items

10. **[DONE 2026-09-01]** Two-tier test-gating. Added `.pre-commit-config.yaml` (local system hook: pytest on `tests/test_odds_ledger.py tests/test_real_bets.py tests/test_registries.py`). Ran `pre-commit install`; fast subset **28 passed in ~3.9s**. Full suite remains CI-only.

11. **[PROVENANCE AUDIT — COMPLETE 2026-08-27]** Grep for every place that aggregates the ledger; confirm each historical finding's generating script and whether it used canonical `dedupe_ledger_props`, a bespoke grouping, or raw rows; flag what needs recomputation. **Verdict (evidence-backed):**
    - **CLEAN — already on the fixed canonical `dedupe_ledger_props`** (live import → round-trips fixed signature automatically): all `artifacts/odds_log/_*.py` (`_analysis_extra`, `_audit_stratify_by_epoch`, `_audit_stratify_edges`, `_calibration_deep`, `_clv_snapshot`, `_timing_results`) + production `grade_odds_ledger.py`, `build_automation_self_check.py`, `line_policy_settled_lookback`, `clv_basis_reconcile`, `weekly_kpi_report`.
    - **RESOLVED specific concern:** **favorite/dog ROI splits + line×side splits** come from `_audit_stratify_edges.py` (lines 85–124), which calls canonical `dedupe_ledger_props` — **CLEAN, no recompute.**
    - **BESPOKE but side-aware — unaffected by over/under merge bug:** `keep_best_available_lines` (`src/Python/notebook_analysis_utils.py`, keyed on `(game_date, player_name, side)`) used by `build_policy_governance_report.py` + `policy_simulator.py`. Key already includes `side`, so it never had item-4's bug. (It intentionally collapses across LINES per game/player/side — different, intended semantic, not a regression.) No recompute.
    - **AT-RISK — aggregates RAW settled rows, bypasses canonical dedupe (recompute under fixed logic):**
      - `build_runtime_monitoring_snapshot.py` `_compute_floor_table`/`_compute_monthly_regime`/`_compute_edge_deciles` (roi/pnl on raw rows). Quantified: raw settled **1118 / +$576.15**, deduped **643 / +$84.51**. The `daily_kpi_loop_last_run.json` `total_pnl=$+1041.04` is a stale raw-row artifact of this path. NOTE: `_compute_slippage_segments` segments *by book*, intentionally raw — leave it.
      - `edge_floor_sweep_governance.py` `_load_settled_ledger` (line 72): raw settled.
      - `policy_calibration_uncertainty_sweep.py` `_load_settled_ledger` (line 136): raw settled.
      - `build_daily_operator_summary.py` `_ledger_snapshot` (line 40): per-day stake/pnl/roi on raw settled rows (DK+FD pairs double-counted per day).
    - **ORPHAN — generator not in repo (same class as item 7):** `regime_model_report.json` (`is_fav`/`is_over` coefficients; 939/999-bet regime analysis) has **no source script** in the repo — only the artifact remains. Provenance cannot be confirmed; do not cite its coefficients as settled until the generator is restored.
    - **NOT AFFECTED (different data domain):** `build_board_ledger_reconciliation.py` (targets `status=="open"` slate occupancy, not settled ROI); the A1/champion side-strat audit (2025 model open universe, not the settled paper ledger — consistent with item 9).
    - **Next (approved, not blocking):** add `dedupe_ledger_props(settled)` to the four at-risk aggregators above (keeping slippage-segments raw-by-book) + a regression test. Do NOT touch the orphan regime artifact.
    11-followup. **[DONE 2026-08-27 — aggregator dedupe fix]** Wired canonical `dedupe_ledger_props` into the producer + all four at-risk aggregators: `grade_odds_ledger.py` (`--status` PnL sum, `--curve` n_settled), `build_runtime_monitoring_snapshot.py` (floor/monthly/deciles use deduped; slippage segments keep raw-by-book), `edge_floor_sweep_governance.py` + `policy_calibration_uncertainty_sweep.py` (`_load_settled_ledger`), `build_daily_operator_summary.py` (`_ledger_snapshot` daily ROI/PnL). Added regression test `test_dedupe_ledger_props_pnl_not_double_counted` (proves DK+FD pair is not double-counted in PnL aggregation). All 5 files py_compile; `tests/test_odds_ledger.py` 13/13 green. **Live-verified 2026-08-27**: `grade_odds_ledger.py --status` printed `settled=643 total_pnl=+$84.51` (was 1118/+$576.15 raw; stale loop artifact +$1041.04). Regenerated `daily_kpi_loop_last_run.json` via `run_daily_kpi_loop.py --skip-notebooks` → `n_settled=653` (deduped; +~10 settled during run). **Refresh 2026-09-01**: after more settled days, deduped ledger is `settled=790 total_pnl=+$10.55` (ROI ≈ +0.05%); full-cohort CLV reconcile is `n_closed=493`, `price_devig_gt0=0.544` (was 441 / 0.551 on 2026-08-27). Note: dashboard `_dedupe_frame` keys on `book` too — only exact-dupe removal, not canonical cross-book dedupe (potential item-11 follow-up). **(Item 11 follow-up now COMPLETE; reordered to run before item 10 per user approval.)**

12. **[WAITING ON USER] Item 6 backfill** — still needs your decision-time `bet_price`/`stake`/`pnl` for the 8 confirmed tickets (Valdez, Suarez, Messick, Skenes, Melton, G.Rodriguez, Cantillo, Sasaki) plus the 9th ticket's identity, to complete the real-bet ledger write.

13. **[APPROVED — DEFERRED] Local MLflow Tracking (thin v1).** Useful when actively iterating Strikeout/TBF train or Optuna — **not** on the critical path for this ops cycle (wrong tool for freeze/null/CLV/veto work). See Session Snapshot → **DEFERRED**. Resume when you ask or when train iteration restarts; do not jump here mid-ops.

14. **[DONE research 2026-09-01 — promote gated] Granular open calibration challenger (ROI path).** Fit line / line-bucket (and optional side-aware apply) isotonic/Platt on **open 2025–2026** with chrono-safe holdout; score **post-freeze** as pure OOS on skill vs market + shadow KING ROI. Do not refit on post-freeze tickets. Do not edit live calibrator / KING without sign-off. Script: production/ops/fit_granular_open_calibration.py.


## Next Session — Open Questions & Plan (evergreen)
> **Not a second backlog.** Canonical live plan = Session Snapshot → **PAST / PRESENT / FORWARD / DEFERRED** (top of this file). Checklists below are a short mirror only.

**Waiting on user (blocks these):**
- [ ] `git push origin HEAD` so remote has promoted 4.5-over veto.
- [ ] Item 6/#12: per-ticket decision-time prices/stakes/pnl for the 8 + 9th identity.
- [ ] Item 9: explicit champion→monotone sign-off, or leave deferred (not needed for 4.5 veto).

**Ready / operating now:**
- [x] Live `veto_4_5_over` + soft probation 2.5/3.5 — DONE 2026-09-01.
- [x] Weekly settle pack habit — re-run after each settle week.
- [ ] Item 13 MLflow — APPROVED but **DEFERRED** (wrong tool for this ops cycle).
- [ ] Optional polish: regenerate `manuscript.pdf`; dashboard dedupe → canonical `dedupe_ledger_props`.

**Blocked / parked (see DEFERRED + Execution Order):** Item 7 CUSUM rewrite; Item 2 history split; SSAC future-work 9–13.

**Historical polish notes** (2026-08-27 freshness sweep detail, ledger refresh figures): kept in git history; intentionally not repeated here so this section cannot compete with the Snapshot.

## Standing Rules (apply to all of the above)
- Never mutate historical labels (hold→bet relabeling) — always append new records instead.
- Every real-money conclusion requires ≥50 real tickets before being treated as evidence of edge. The current 9-ticket, +24.1% ROI record is now fully auditable (6W–3L, all outcomes confirmed) — a good start, not proof; don't let it drive stake-sizing yet.
- Verify settlement-rule parity (prediction-market vs. sportsbook K definitions, extra innings, early exits) before trusting any grading as final.
- Personal accounts/API keys only — never company-owned tooling for any of this.
- Do not touch `KING_PROFILE_AUG2026`, floors, staking, or the champion file without explicit sign-off after evidence is reviewed.
- **DEFERRED is binding:** Item 13 (MLflow) and other Snapshot → DEFERRED rows are not idle backlog to fill quiet turns. Do not scaffold/install them during an ops/policy week unless the user asks or the reopen condition is met.
- Prefer weekly settle-pack measurement and real-bet logging over new tooling while the live 4.5-over veto is the active experiment.
- A pre-registered sample-size gate that is *exactly* met (not comfortably exceeded) should be treated as "not yet conclusive" — do not size up or treat as validated edge until the sample comfortably clears the threshold with margin.
- **Measurement vs money (owner-locked 2026-09-11):** two jobs, never mixed. *Measurement* (are we right?) = devigged consensus median at the same snapshot (`consensus_cache.parquet`, never re-devig) for Brier/skill/CLV. *Money* (did we profit?) = juiced book-pair at the decision clock (DK else FD else next US book) for ROI/WR. Report both forever; cite which lane every number lives in. Dedupe (`dedupe_ledger_props`) is money-track aggregation only — selection stays per-quote, line×side cells stay per player O/U line.

## SSAC27 Track — Manuscript Critical Review (added 2026-08-27)

> **Status: ACTIVE.** An MIT-professor-style critical review of `docs/paper/manuscript.md` was completed 2026-08-27 (full manuscript, model-card, both diagrams, PAPER_NOTES, and the statistical audit report; figures assessed via `make_figures.py` + captions only — **no images opened**, the model can't read them). The review's core strategic verdict: **the manuscript is really two papers stapled together.** (1) The *modeling* half — leakage-safe rate×exposure forecasting, honest trial-adjusted stats (DSR), clean audit trail — is publishable and is the right SSAC27 submission. (2) The *live-betting performance* half (ROI 0.4363, Sharpe 0.4438 on 26 bets) is underpowered (its own DSR 0.0349), self-selected, and must be demoted to "illustrative" until the policy-freeze question and a placebo/null control are resolved. **Decouple them — lead with modeling rigor.**
>
> This section turns the review's prioritized action list into executable items. Do these **IN ORDER** — they gate a credible Oct 1, 2026 abstract.

1. **[APPROVED — do first] Fix the Figure 2 numbers-to-body mismatch.** `docs/paper/make_figures.py` `fig2_model_comparison` plots Mean/Ridge/LightGBM MAE (0.0854/0.0788/0.0783) titled "248-feature screen" — but the manuscript body (Table 2a, §8.6) reports the **sparse-set (72/58-feature) parity contract** (MAE ≈ 0.0767). The figure does not match the reported numbers and is a stale 248-feature-registry artifact. Regenerate against current sparse-lane artifacts, or delete. A figure that contradicts the body is disqualifying. *(Critical — 1–2 hrs.)*
2. **[APPROVED — do second] Reconcile ECE 0.024 vs 0.0639 conflict.** §8.4 / Fig-2-reliability report "mean ECE ≈ 0.024 with no post-hoc recalibration"; the deployed profile (Table 3/A5) reports ECE 0.0639 / MCE 0.1353. The manuscript never explains why the deployed, isotonic-transfer-calibrated profile looks **2.6× worse** than the raw walk-forward stack. Either they're different lanes/populations (must be labeled as such everywhere: open / manual-26 / walk-forward, and raw vs post-cal) or there's an unexplained calibration regression. Every ECE/MCE/Brier must carry (lane, n, raw/post-cal). *(Critical — 2–4 hrs.)*
3. **[DONE 2026-08-27 — abstract rewritten]** Rewrite the abstract. Lead with the DSR/sample-size finding (currently buried at line 22); co-locate every headline metric with its 95% CI (e.g., "ROI 0.4363, 95% CI [0.03, 0.81]"); drop or heavily qualify the +24.17u / ROI / Sortino framing until item 4 resolves the policy-freeze question. *(Critical — 1–2 hrs.)*
4. **[DONE 2026-09-01 — FAIL]** Resolve policy-freeze vs. evaluation-window separation. **Audit verdict: FAIL.** The `n=26` lane slate dates are `2026-07-30`–`2026-08-17`, entirely before `KING_PROFILE_AUG2026` `frozen_utc=2026-08-21T16:10:00Z`. Live blend selection rule is `best_manual_roi_after_open_calibration_transfer_deduped` (`production/ops/live_krate_ensemble.json`, commit `8ad4681`). ROI/Sharpe/PnL reclassified as **pre-freeze policy-search evidence**; stripped from headline deployment claims in abstract + Table 3 + §8.2. Full write-up: `docs/reference/reports/ssac27_audit_pack_2026-09-01.md`. *(Was critical — completed.)*

5. **[DONE 2026-09-01]** `N=5161` enumerated as eligible blend×floor configs. Report: `docs/reference/reports/ssac27_audit_pack_2026-09-01.md`. Manuscript §8.2 DSR provenance corrected (not feature×model×HP).
6. **[DONE 2026-09-01]** Chronological naive MAE baselines via `models/Strikeout-Model/research/marcel_baseline.py`; Table 2b + lane caveats. Report: `docs/reference/reports/ssac27_audit_pack_2026-09-01.md`.
7. **[DONE 2026-09-01]** Tables 3/A5 CI+lane tags (prior); equity regenerable via `docs/paper/make_figures.py` `fig_equity_top3_vs_top1()`; Fig 3 caption n/date-span/generator; honesty/slippage lineage: `docs/reference/reports/ssac27_audit_pack_2026-09-01.md`.
8. **[DONE 2026-09-01]** Null/placebo lanes: `production/ops/run_null_decision_lane.py`. Report: `docs/reference/reports/ssac27_audit_pack_2026-09-01.md`. Manuscript §8.2.2. KING less red than nulls, still negative → no decision-edge claim.

#### Completed this session — 2026-08-27 artifact inspection (numbers fixed against artifacts)
- **[DONE — item 1] Figure 2 -> body mismatch resolved.** `make_figures.py` `fig2_model_comparison()` was a hardcoded 248-feature-registry MAE bar chart (0.0854/0.0788/0.0783) titled "248-feature screen"; it was **not referenced anywhere in the manuscript** (confirmed zero `.md`/`.html` references) and contradicted the sparse-lane parity contract (MAE ≈ 0.0767). Chose the review's "or delete" option: **removed the function + its call, and deleted the stale `docs/paper/figures/fig2_model_comparison.png`**. Rationale: it could not be regenerated without a same-lane sparse naive baseline (none exists), so deleting beats emitting a mixed-lane figure. `fig4_calibration` note relabeled to the artifact-backed `ece_mean`.
- **[DONE — item 2 partial (labeling)] ECE 0.024 vs 0.0639 reconciled as different lanes.** Confirmed `phase11c_calibration/metadata.json` `ece_mean=0.0243`, `ece_max=0.0403`, `n_rows=4607`, `n_windows=3`, `source_predictions=.../phase11b_walkforward/walkforward_predictions.parquet`, `needs_recalibration=False` — i.e. the 0.024 is the **pre-deployment walk-forward 2024 diagnostic (4607 raw preds, 3 windows, raw no-recal)**, NOT deployed quality. The 0.0639/0.1353 is the **deployed 26-bet lane** (`open_top3_transfer...replay` `best.ece`). Added explicit lane-labeling + a "Lane clarification" block in `manuscript.md` §8.4/Fig 2 and in `manuscript.html`. No number changed — the two ECEs were never contradictory, just underlabeled. Remaining: extend the same (lane, n, raw/post-cal) tagging to Table 3/A5 and every other ECE/Brier mention (item 2's full scope).
- **[DONE — NEW find] §8.3 slippage-table Sharpe column was wrong in both `.md` and `.html`.** The table carried a hand-extrapolated Sharpe series (0.4438/0.4387/0.4336/0.4234) that exists in **no artifact**. The authoritative `artifacts/odds_log/slippage_sensitivity_top3_floor12_aug21.csv` reports 0.4352/0.4301/0.4250/0.4148 for the same 26-bet set. ROI/PnL/Sortino already matched. **Fixed the Sharpe column in both `manuscript.md` and `manuscript.html`** + added a source/freshness note.
- **[DONE — NEW provenance] §8.2 CI bounds ARE artifact-pinned.** The backlog previously claimed the §8.2 bootstrap CIs were "not regenerable from any committed artifact." **Inspection overturned this**: `artifacts/odds_log/quant_honesty_aug21_summary.json` contains `bootstrap_iid_ci` and `bootstrap_block_by_date_ci` that exactly match the manuscript's stated intervals. Updated the §8.2 text (`.md` + `.html`) to cite that artifact. Items 3/7 can now surface these CIs straight from the artifact.
- **[DONE — NEW provenance] N=5161 pinned; citation added.** `quant_honesty_aug21_summary.json` `n_trials=5161` (+`sr_star=0.8544`) confirmed as the DSR/PSR/power source. Added Bailey & López de Prado (2014) *The Deflated Sharpe Ratio* as reference [12] and a provenance note in §8.2. Item 5 remains open only for a per-family **breakdown** of the 5161 (the artifact stores the count, not the enumeration).
- **[DONE — item 4 — 2026-09-01 FAIL]** Policy-freeze audit. 26-bet slate dates `2026-07-30`–`2026-08-17` are entirely before `KING_PROFILE_AUG2026` freeze `2026-08-21T16:10:00Z`; blend selected via `best_manual_roi_after_open_calibration_transfer_deduped`. Reclassified ROI/Sharpe/PnL as policy-search evidence; demoted in abstract/Table 3/§8.2/resume-summary. Report: `docs/reference/reports/ssac27_audit_pack_2026-09-01.md`.

- **[DONE — item 7 tables half] CI surface + lane tags on Tables 3 & A5.** Added bootstrap 95% CI annotations to the decision-lane ROI/PnL/Sharpe/Sortino rows of Table 3 (with footnote citing `quant_honesty_aug21_summary.json`) and a matching CI footnote on Table A5 (including the pre-correction-superseded note on that artifact's Sharpe/DD/Calmar). Also added explicit `deployed 26-bet manual lane, post-isotonic-transfer, n=26` lane tags to the ECE/MCE/Brier/LogLoss rows of both tables — closing item 2's full-scope lane-tagging for Tables 3 & A5.
- **[DONE — parking-lot low-priority] §1.1 Related work + §8.7 illustrative framing.** Added a "Governed decisioning and performance evaluation" paragraph to §1.1 (Bailey–López de Prado [12] + market-skill/CLV methodology). Added an explicit "explicitly illustrative and anecdotal (n=3)" disclaimer at the top of §8.7 so the case narratives cannot be read as statistical evidence.
- **[DONE — item 7 equity half, 2026-09-01]** Equity curve now produced by `docs/paper/make_figures.py`; caption cites picks CSV + n/date-span; honesty/slippage lineage report resolves Sharpe mismatch by intentional freeze (not silent rewrite).

#### Flagged — artifact lineage inconsistency (RESOLVED 2026-09-01 by documentation freeze)
- `quant_honesty_aug21_summary.json` / `slippage_sensitivity_*.csv` keep pre-correction Sharpe/DD/Calmar (0.4352/0.3685/1.1841) as **intentional diagnostic freeze** for PSR/DSR/bootstrap; manuscript headlines use authoritative replay (0.4438/0.1905/2.2903). Report: `docs/reference/reports/ssac27_audit_pack_2026-09-01.md`. Do not silently rewrite honesty metrics without regenerating the DSR pipeline.


#### Strategy note — consensus / CLV / ROI / other-models grounding (recorded 2026-08-27, no code work)
Grounding for how this project holds up against sportsbooks and how results are best evaluated. Drawn from `clv_basis_reconcile.json`, `clv_snapshot.json`, model-card, and manuscript §8.2/§8.5 — recorded so the framing survives, not as new approved items.
- **Vs. consensus / sportsbooks: no demonstrated edge yet.** Full-cohort devigged beat-close rate ≈ **54.4%** (`price_devig_gt0 = 0.544`, n=493 as of 2026-09-01; was 0.551 / n=441 on 2026-08-27) and the reconcile note itself warns most K-prop lines don't move (`|clv| <= 1pp` on ~42% of props), so market-beating is near coin-flip at best; CLV skill gates still read `building_sample`. The honest defensible claim is "no proven persistent edge." To make a *consensus* comparison credible we'd need a defined devigged mid-market/timestamp consensus on the *same* matched slate universe, not just the odds-log prices we happened to capture (thin-market weakness §8.5 concedes).
- **CLV evaluation discipline:** always report the **full-cohort** devigged basis (`price_devig_gt0/ge0`), never the flattering trailing-10 or `>=0` "didn't get worse" basis — those read much higher and are not comparable skill evidence. This is a standing reporting rule, not a one-off.
- **Metric-lane split (keep):** ROI/Sharpe/Sortino = decision-*performance* (small-n, CI-wide, DSR-guarded — do NOT lead with it); CLV = market-*skill*; Brier/LogLoss/ECE + delta-over-naive = the publishable model-skill half. Other-models (naive/Marcel/career-prior baselines, item 6) is what turns "absolute MAE 0.0767" into a real skill claim.
- **Reframed why of the approved items (order unchanged):** item 5 (enumerate N=5161) → DSR auditable; item 6 (naive baseline) → delta-over-naive is the most persuasive cheap modeling upgrade; item 4 (policy-freeze audit) → gating, strip ROI/Sharpe from headlines if the date can't be drawn; item 8 (placebo/null lane) → gives the null distribution DSR rests on. No change to execution order.

**SSAC27 future-work (do NOT start before items 1–8 clear; these are post-deadline depth, not submission-gating):**
- **9.** Negative-binomial + projected-TBF-distribution mixture count-layer challengers (currently not-built per model-card / audit).
- **10.** Formal market-microstructure test for the thin-market hypothesis (§8.5), or **delete the speculation now** (15-min deletion is gating-adjacent, the test is future).
- **11.** Full interpretability atlas (conditional permutation on frozen profiles).
- **12.** Sustained production SLO tracking (latency/rollback distributions).
- **13.** Live-sample accumulation toward the DSR targets the paper itself computes (n ≈ 98 for DSR>0.5; n ≈ 147 for DSR>0.8). The only thing that ever makes the *betting* half defensible. Future — but the abstract must NOT claim betting edge before this.

## Parking Lot (new ideas noticed mid-task — do not act on these until the list above is clear)
- **Cheap agent stack (2026-09-02) — PARTIAL DONE:** ignore-files + agent-context rule shipped. Still parked: Cline/OpenCode + DeepSeek V4 Flash on Mac; kill Cursor Auto/credits; escalate Kimi/GLM; Claude escape hatch. Never route Lenovo work through Chinese APIs. Reopen when user starts Mac harness migrate.
- **Historical K CLV backfill (2026-09-02) — RESEARCH DONE, pull PARKED:** see `docs/reference/reports/historical_clv_odds_apis_2026-09-02.md`. Execute free HF + OddsPapi smoke then optional The Odds API month only when user asks. Do not start paid pull mid-ops week.
- **Book-quality filter — WONT_DO** (user 2026-09-01): books used for lines only; usually synced.
- **Juiced next-book vs DK+FD (2026-09-11 #123):** next-US-book fallback after DK/FD soaked BetRivers (n=1,001 of 2,077, WR 0.429). GO had said median. Waiting on owner which universe is canonical. Do not mix all-books ROI with DK+FD ROI.

- **Live A/B parallel ledgers:** score every opportunity under status_quo / veto_4_5 / asym16 even when only one rule is bet live.
- **Block bootstrap by game_date** for ROI/CLV CIs; do not bootstrap a line cell with n≈18 into a promote claim.
- **Real-bet first:** Item 6/#12 unblocks honest money ROI vs paper.
- **Optional soft veto:** size 2.5–3.5 overs at 0.5u instead of hard skip if learning value matters.
- Three-state action space (bet / prediction-market-watch / hold) as a cleaner replacement for the binary bet/hold flip.
- Calibration-drift monitor (recent-window WR 0.493 vs. all-time 0.534).
- Reliability/calibration bucket report (among props rated 0.7-0.8, what's the real hit rate).
- Cost-asymmetry-informed stake sizing (expected loss of missing a winner vs. betting a loser).
- Side asymmetry on real tickets: all 3 losses to date were Overs; every Under/low-line-Over hit. Worth a real-ticket side-conditional check once ledger reaches meaningful n, not a 9-shot pattern to bet on.
- **MLflow v2 (after Item 13):** Optuna callback → nested MLflow runs; log ensemble-sweep / null-lane decision metrics as separate experiments; optional Model Registry *mirror* of frozen champions (still not auto-promote).
- **W&B** only if you want hosted multi-machine dashboards; not needed for solo local research.
- **SSAC27 (MIT Sloan Sports Analytics Conference) — abstract deadline Oct 1, 2026.** Items **1–8 DONE**. Future-work 9–13 remain parked. Lead submission with modeling half; betting half stays demoted. Low-priority doc polish: move ablation/freshness self-notes to a Supplemental appendix.
- **Phase 10 pipeline-discovery + reusable-prop-platform prompt (2026-09-17) — CENSUS EXECUTED, design queued:** see PRESENT "PHASE 10 EXECUTED" + FORWARD Phase 10 workstreams (ARCH/DATA/PROP/SIM/EVAL/OBS/NB/OPS/TEST). Source file lives outside repo (`Downloads/Pipeline Discovery and Reusable Prop Platform Prompt.md`); content reconciled here, no second roadmap. Conflicts with FORWARD pickup box (no coding job until owner names it) and October sequencing (post-9/27) — design-only at most before then; no live/Modal/policy/model/schedule changes. Verified pre-checks: all 5 Modal schedules use `modal.Cron` (`modal_app.py:132,154,181,197,213`), so the Period-reset timing risk does not apply. Reopen when owner names the census or the October program starts.
