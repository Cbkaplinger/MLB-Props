# Repo quality hold inventory — 2026-09-01

Point-in-time hold list from `/repo-quality-passthrough`. **Not a work queue.**
Live plan: [`docs/EXECUTION_BACKLOG.md`](../../EXECUTION_BACKLOG.md).

| Path / topic | Decision | Evidence | Risk | Owner action |
| --- | --- | --- | --- | --- |
| `artifacts/**` (local) | **keep** (local) / ignore | Entire tree gitignored; contains models, odds_log, projection_log, notebook_exec | Deleting loses provenance / slow regen | Leave local; do not force-add |
| `production/ops/__pycache__/` | **delete** (local cache) | Regenerated by Python; not tracked | None | Safe to remove anytime |
| `.pytest_cache/` | **delete** (local cache) | Ignored; test runner recreates | None | Safe to remove anytime |
| `src/*.egg-info/` | **keep** ignore | Already `*.egg-info/` in `.gitignore` | None | Leave |
| Case aliases `Data/` / `Artifacts/` / `Models/` | **hold** | Windows FS collapses case; both patterns in `.gitignore` | Case-only rename on Windows is unsafe | No rename without explicit approval |
| Closed `docs/research/step*.md` (pre-step11) | **hold** | Freeze lineage; step11 is active anchor | Losing audit trail | Consolidate to summary only after explicit approve |
| `docs/diagrams/*` research status snapshot (2026-08-21) | **hold** | Architecture still useful; ops plan lives in backlog | Stale “next” prose if misread as work queue | Diagrams README now points to backlog for live plan |
| Item 13 MLflow scaffold | **hold / deferred** | Backlog DEFERRED — wrong tool for ops cycle | Mid-ops tooling distraction | Do not install until train/Optuna resumes |
| Dashboard `_dedupe_frame` vs `dedupe_ledger_props` | **hold** | Known polish in FORWARD optional list | Display double-count risk only | Optional; not blocking |
| Manuscript HTML/PDF + resume HTML/PDF | **keep** | Portfolio/share artifacts; `.md` is source of truth | Stale render if `.md` changes without regen | Regenerate when publishing; do **not** delete |
| Byte-identical script wrappers | **hold** | No hash-proven disposable duplicates found this pass | Accidental delete of live wrapper | Revisit only with hash proof |

## This pass — automatic (no approval needed)

- Expanded `.gitignore` for portable `artifacts/`, caches, coverage, `mlruns/`.
- Retargeted skills/rules/READMEs/diagrams to `docs/EXECUTION_BACKLOG.md` as master work-state.
- Local caches wiped 2026-09-01; resume/manuscript HTML+PDF kept.
- No production code, notebooks, or artifact trees deleted.
