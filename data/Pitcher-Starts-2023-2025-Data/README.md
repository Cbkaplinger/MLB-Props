# Pitcher-Starts-2023-2025-Data — SUPERSEDED (do not use for new work)

These CSV/parquet snapshots predate the L3 pipeline
(`data/processed/pitcher_training.parquet`, built by
`src/Python/pipeline/training.py`). Zero code references them
(verified 2026-09-14) — the training frame is the live source.

Kept for provenance only. Safe to archive offline; do NOT delete until
the offseason cleanup gets an explicit owner order.
